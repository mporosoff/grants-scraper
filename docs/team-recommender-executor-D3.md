# Manual D3 preparation/evaluation only

This narrowly scoped continuation supports `voyage-4-large` independent evidence and combined profiles, and `voyage-context-4` explicitly grouped per-person chunks. The same `team-recommender-offline.yml` manual workflow runs protected-main code only. It reads hash-addressed public packets as data and does not check out experiment code. Workflow permissions, schedules, provider credentials, production services and release routing are unchanged.

`tools/team_recommender_embeddings_d3.py` constructs all profile input text from the already trusted frozen claim/summary tables. Names, departments and URLs are attribution metadata, not embedding text. Every contextual document is exactly one researcher's ordered chunks, with no summary duplication. Complete document/chunk/model/endpoint/role identities invalidate all contextual rows when any document content changes. Source queries are restricted to the 35 already prepared D2 development scopes in the hash-bound `queries-d3.json`; no source or holdout expansion. The optional query format adds only the existing source-derived central purpose to its aspect. Context queries use one string per nested list. No automatic chunking or unbounded endpoints/models are accepted.

One authorization remains: `on-demand-team-offline-v2-20260909`. Before D3, the authoritative cloud checkpoint is run 34510982749, $2.279110 charged and zero reserved. Restore and verify it; never initialize a second allowance. D3's finite ceiling is 28 embedding requests / $0.18 conservative reservations, plus 120 judge requests / 1,100,000 conservative input units / 61,440 output tokens / $3.3644. The $3.5444 maximum new D3 reservation plan keeps cumulative Stage 2 <=$5.823510 and at least $4.176490 for later stages. Existing stricter lifetime embedding/request and total $10 / Stage 2 $6 ceilings remain enforced. These are ceilings, not quotas. Prices checked September 10, 2026: large/context $0.12/M input, lite $0.02/M; judge input $2/M, output $10/M, with conservative $2.50/M input reservation. Free credit is not assumed. References: https://docs.voyageai.com/docs/pricing and https://docs.voyageai.com/docs/contextualized-chunk-embeddings.

The prior irreversible dispatch lifecycle and exact successful cache reuse remain authoritative. D3 namespaced vector items distinguish model/role and complete contextual input while deduplicating identical standard queries across E1/E2. Missing-result, failed and uncertain paid work cannot be rebundled to retry. Scientific judge item identity and D1F rubric are unchanged, including old failures. Exact caches/results remain in the paired authoritative checkpoint, not in public release ingredients.

Freeze the actual packet sizes and experiment calibration/comparison plan before dispatch. Use a small reusable wiring batch and then the full 35-scope/155-person prepared directory comparison, preserving all 90+30 reservation denominators. Bounded context output validation checks model, document/chunk indices, shape, finite values and norm. A provider rejection is a retained failure/uncertainty, not permission for arbitrary model substitution or an automatic retry.

This helper cannot publish a recommender, activate a cohort, send mail, enrich researchers or enable old Sonnet work. Stage 3, full E2E/Playwright/accessibility and deployment are not authorized. The experiment must finish its D3 report and stop for user review.
