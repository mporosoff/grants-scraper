# Phase 1: two retrospective Option 1 checks

## Current continuation: exact citation grammar (September 13)

Owner: “Figure out how to resolve the verdicts. Maybe re-run it? Don’t stop until the phase 1 audit is complete.” The prior implementation and failed request remain history below. This resumes the named audit, including a second mechanical repair of the completed group check and the first explanation check; it does not authorize new scientific generation or favorable rejudging.

Run 34774192296 returned HTTP 200/end_turn with 6,866 input and 703 output tokens, charged $0.020762, but failed `contextual_check_invalid_reference`. The rejected reference and answers were not retained, so its precise spelling/ownership fault and grades cannot be reconstructed. The old provider schema allowed arbitrary reference strings while local validation enforced question ownership. The v3 contract makes exact question keys and each question's owned reference enum part of the provider grammar, then normalizes complete accepted answers into the existing canonical array cache. Wrong references yield existing bounded schema-path diagnostics; no raw response/hidden reasoning retention is added. Source/profile/question message bytes and the scientific rubric are unchanged; only mechanical format wording/schema change. No reference is guessed or silently remapped.

The active lock is `config/contextual_team/phase1-checks-v2.json`; v1 remains immutable. It binds all 660 existing ledger rows, $5.779176 charged, zero outstanding, and repair request `53020ccfc01749f3b7971c2ca442102f` (including its exact logical key, body, charge and usage). Its ancestor `b12fa7fbde6e45069d14358b6c73ee79` is preserved in that prefix and v1 lock. New purposes `cb-p1-reference-group` and `cb-p1-reference-explanation` are each claimable once; both prior response-contract purposes are closed to new claims. Any previously claimed result missing its exact successful cache remains recovery-required.

The owner's explicit rerun adds one group attempt to the earlier two-attempt Phase 1 plan: **three total Phase 1 attempts**, one already used and at most two remaining. This does **not** add dollars: the original **$0.25 total Phase 1 ceiling** retains the $0.020762 prior charge, leaving at most $0.229238 for this continuation. The same $10/690 experiment ledger remains authoritative. The minimum residual reserve is $3.991586 /28 lifetime slots (the added group retry consumes the formerly reserved 29th slot). There is no automatic replenishment or uncertain-request replay.

|Current check|Wire bytes|Conservative input bound|Output ceiling|Maximum USD|
|---|---:|---:|---:|---:|
|Reference-repaired group/person|21,224|22,248|2,048|0.064976|
|First explanation|22,239|23,263|2,048|0.067006|
|Combined remaining envelope||||0.131982|

Prices rechecked September 13 at [Anthropic pricing](https://platform.claude.com/docs/en/about-claude/pricing): existing Sonnet 5 route $2/M input, $10/M output, thinking disabled, no provider cache creation. The original full source/evidence is retained. Only protected main may execute after the required clean exact-head review and CI. The existing manual workflow, credentials, global concurrency, checkpoint/accounting, and no-deployment boundary are unchanged. Negative or uncertain scientific answers still complete both technically safe checks. Further new requests are not automatically authorized by this finite lock. Repeated dispatch reuses complete exact successes only.

## Preserved first closeout instructions and failed execution

Owner authority: September 13, 2026, “Finish Phase 1: repair the evaluator contract and complete the existing output checks.” This is only one repaired replacement of the completed schema-failed group/person check and the first explanation check. No application deployment, scientific regeneration or release is included.

The reviewed lock is `config/contextual_team/phase1-checks-v1.json`. It binds the original LPS base graph, ordered Blok/Huang/Singh IDs, registry, approved input, both original and revised body hashes, exact scientific question/evidence hashes, and the original failed ledger row (ID, logical key, body hash, status, usage and $0.016528 charge). Original prompt/rubric remains available for reproduction. Only its mechanical 60-ASCII-character instruction is replaced in this dedicated evaluator; the scientific text and input messages compare exactly. Both requests use Sonnet 5 with thinking disabled and 2,048 output tokens, a 32,768-byte complete-response acceptance bound, exact verdict IDs/count/enums/types and owned evidence references. No rationale truncation or retry is performed.

## Complete reservation before dispatch

Same authorization: `on-demand-team-offline-v2-20260909`. Original 659 request rows must match their immutable prefix hash. Starting charge $5.758414; no outstanding reservation. Ceiling: two new attempts and $0.25, preserving $3.991586 and 29 lifetime slots. This does not renew the old Option 1 allowance.

|Check|Wire bytes|Conservative input tokens (wire bytes + 1,024)|Output ceiling|Maximum USD|
|---|---:|---:|---:|---:|
|Repaired group/person|19,687|20,711|2,048|0.061902|
|First explanation|21,263|22,287|2,048|0.065054|
|Total||||0.126956|

Prices checked September 13 at https://platform.claude.com/docs/en/about-claude/pricing: $2/M input and $10/M output. No provider prompt-cache creation, paid probe or new embedding. The workflow uploads the whole remaining reservation envelope before exposing the existing Anthropic secret to execution. Every individual dispatch then claims the existing atomic ledger under its lock, retaining conservative unknown usage. All restored/repeated executions use this same authoritative cloud checkpoint; local copies are read-only mirrors.

The legacy check purposes are closed to new dispatch. Each new `cb-p1-check-*` purpose may be claimed once; arbitrary body/rekey/old-explanation replay is rejected. Existing successful exact cache reuse remains allowed. A paid response whose result is lost is recovery-required. Charges and original failures are never reset. Failure metadata is taken from durable request rows even when the batch convenience array is empty. Accepted verdicts are cached at each request boundary; a later failure cannot hide the earlier result.

## Trusted execution

Only after clean exact-head independent review, green required CI and protected merge, manually dispatch the existing `team-recommender-offline.yml` on main with `contextual_check` equal to:

```json
[{"scope_id":"332894","graph_id":"a9ce47f194e303b6d4173bf4a8d28685e597df2e31144f45d1d9ce44acbbeb64","members":["urh-000091","urh-000099","urh-000126"]}]
```

Leave all other inputs empty. Main-only checks, global workflow concurrency, checkpoint restoration, immutable artifacts and secret recipients remain unchanged. No experimental code is executed with provider credentials. This offline helper has no Worker deployment, graph publication, application callback, subscriber or source-generation step. Push/PR tests are Python and Node contracts; E2E is manual-only and is not run for this offline change.

Negative, mixed or insufficient-information grades do not prevent the explanation request. A technical failure at the shared boundary stops subsequent execution conservatively. There is no third attempt, automatic repair loop or favorable rejudging. Repeated dispatch can only reuse both complete exact successes; any previously claimed item without its valid result stops for recovery.

Private workflow artifacts retain full accepted reasons and accounting. Do not publish raw provider responses, hidden thinking, credentials or unrestricted caches. Report all seven judgments separately and disclose the single previously exposed source and same-family limitation. No human assessment or universal precision claim follows from these seven questions. Stop for owner review.
