# Restricted contextual Option 1 continuation

This is the narrowly authorized delivery repair, not a new scientific model or public release. The existing audited input file, Sonnet 5 policy, Voyage 4 large/1,024-dimensional representations, original caches and ledger remain authoritative. No provider credentials are added to the Worker or browser.

`config/contextual_team/option1-v1.json` freezes the two original scopes (332894, then 363069), the outside-shortlist person `urh-000120`, exact reusable stage identities, two permitted original failed-request links, and seven possible operations. Complete conservative bounds total **$1.418340**. The continuation ceiling is **$1.50 /16 attempts**, within the original **$10 /690** ledger, preserving **$3.004098 /21 attempts**. Later usage reduces available capacity. Before dispatch, restore the latest authoritative cloud checkpoint and check the whole remaining envelope. A missing compatible stage or vector cannot start a new legacy purchase.

The first scope reuses its valid interpretation, original 12-person adjudication and vectors; only its confirmed truncated verifier may run once with the repaired reference schema and 24,000-token ceiling. The second reuses its interpretation/vectors and repairs its completed 260-character reason rejection, then runs its independent verifier. Its request cannot start until the first has completed without an execution failure. Only the declared person may extend the first graph, after that graph exists. Old release jobs remain read-only, immutable history. The global active slot still spans old and new releases; uncertain requests keep ownership.

## Response contract

The existing transport already sends Anthropic's `output_config.format` structured schema. Unsupported string/array limits remain described to the provider and deterministically checked locally. Reference output v2 removes redundant generated quotations, resolves exact disclosed source/claim references server-side, and retains the shared membership, source ownership, claim revision, outcome and verifier downgrade invariants. The old 220-character reason limit contradicted legacy 15–700-character wording. The repaired interface consistently permits 15–700 characters and a separate 0–300-character gap, without clipping. Full source and audited profile context are preserved.

Interpretation source references use exact field text with scope/parent identity and character offsets; all surrounding original fields remain supplied. No fuzzy quote repair is used. This continuation reuses the two valid legacy interpretations; new interpretation dispatch is outside its inventory. Verifier references prove existence only; the verifier must still judge applicability and may downgrade or remove edges. Researcher evidence is a retained audited quotation/paraphrase, never newly verified faculty text.

Successful exact caches remain reusable. A paid or uncertain lifecycle cannot automatically retry with another key/body/version. The two original confirmed failures are the only repair parents. Each named new operation can reserve once, independently of its body key. Lost results remain recovery-required. Complete refusals reconcile actual usage and remain terminal, distinct from scientific negatives.

## Protected application connection and operation

Only the existing Access-authenticated `/admin/contextual/access` page and script are added. The local validation app explicitly opens this window. It binds messages to the exact `http://127.0.0.1:8876` origin, opener window and random channel; credentials never leave the protected origin. It accepts only canonical IDs and GET/POST operations on the existing contextual jobs API. The server still authenticates every request and resolves the frozen release. Public new-paid-work remains disabled.

Opening the connection reads a clearly labeled `/admin/api/contextual/fixture` diagnostic only: it creates no job, scientific graph or charge. Only an explicit application assessment action sends POST. Polls, reloads, reads and local edits cannot start model work. If the browser cannot establish this authenticated connection, stop before provider execution; do not change Access policies, export credentials or introduce a proxy.

The existing protected-main manual workflow, one concurrency group, reservation/state artifacts and D1 job table are reused. No new schedule, migration, secret, permission, profile publication or team activation. The existing intake deployment workflow runs after the reviewed protected merge; verify actual module/configuration provenance and retain its previous serving version. The release dependency policy classifies the helper/tests as validation changes; do not publish an experimental browser runtime.

After successful real application routing, execute the first locked scope through that application, verify concurrent joining and persistent result reuse, then the second scope and declared extension. Do not dispatch through the console merely to substitute for the application. Only after reliable actual outputs, the existing manual `contextual_check` operation may check the first locked scope with a positive composed group: exactly one group/every-member packet and one explanation packet. No positive group means no check requests. Same-family correlation and previous case exposure remain limitations.

Run focused reference, accounting, recovery, authorization and concurrency tests before independent exact-head review. Run required exact-head CI and obtain a clean terminal review before protected merge. The separately authorized full application browser/E2E/accessibility validation belongs to the experimental product checkout. This helper merge alone does not complete Option 1.

At the boundary write `docs/team-recommender/reports/contextual-stage-b-option1-integration-repair-report.md` in the experimental worktree, preserving the previous report. Distinguish real graphs, valid negatives, fixtures, missing response bodies, failed stages and unrun checks. No public activation or Stage 4.
