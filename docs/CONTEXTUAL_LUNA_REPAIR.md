# One complete Luna assessment and one source-grounded check

Owner authorization: September 15, 2026, `LUNA_CONTRACT_REPAIR_AND_ASSESSMENT_CHECK.md`.
This is a manual offline prerequisite. It changes no Worker, browser, graph adapter,
hosting, provider endpoint, credential recipient, schedule or public activation.
Do not execute the experimental branch with credentials.

The exact protected configuration is `config/contextual_team/luna-contract-repair-v1.json`.
Use only the existing protected-main `team-recommender-offline.yml` workflow with
`contextual_check` equal to one of these data objects, in this order:

1. `{"luna_contract_repair":"preflight"}` — restore the latest authoritative
   checkpoint, establish the exact old HTTP400's terminality, append its authority
   receipt/event, count the complete checker packet once if uncached, and persist
   the complete two-operation allocation. This is not an inference probe.
2. `{"luna_contract_repair":"assessment"}` — one `gpt-5.6-luna` Responses request,
   low reasoning/verbosity, strict output, `store:false`, 24,000 output ceiling.
3. `{"luna_contract_repair":"check"}` — only if the exact assessment cache is
   complete and valid, one `claude-sonnet-5` check with thinking disabled and
   12,000 output ceiling. A complete negative assessment still receives the check.

There is no automatic retry. A failed assessment means skip operation 3. A failed
check means report its missing answers. Exact complete success caches are reusable;
a lost successful cache, failed response or uncertain dispatch is never an attempt 2.
No operation runs interpretation, embeddings, a production verifier or composition.

## Capacity and evidence

The retained ECLIPSE input identity is
`e13acb65364347fdd5e94da992105833d7d3cb7d6bdb2764f15d28674c8c58f8`:
original scope351715, interpretation, full science/conditions, twelve audited
documents in retained shortlist order. No new source/profile evidence is added.
The new version records all 24 person/contribution pairs with required keyed
objects. It has no four-per-role or eight-total relationship cap. Up to three
distinct owned claims support a single decision; source passages resolve on the
server. Overall outcomes are derived from the complete pair decisions.

The checker sees the identical original evidence, not the assessment's labels,
reasons, speed, costs, old grades or desired members. All 24 pairs are predeclared,
including exclusions, with the retained exploratory rubric and four applicability
categories. Its 12 overall person outcomes are derived, not 12 extra paid questions.
This is not an explanation audit, team qualification or human validation.

Hard person/pair/reference cardinalities use required fixed keys and owned enums
in both native schemas. The checker retains the documented Anthropic projection
for string-length limits (local validation plus schema descriptions); its 700/300
reason/gap bounds are not the retired60-character constraint. Both output budgets
allow substantive complete answers; a ceiling is not a request to fill it.

## The same ledger, with one exact exception

Auth `on-demand-team-offline-v2-20260909` remains $10 /690 attempts. Starting state
run34852296018/artifact10350533860 has683 rows, $7.339874 reconciled and $0.146074
unknown held, conservatively $7.485948. The hold is included once. Native count
history is186/190. New authority: at most $0.50 /two paid attempts, at most two new
native counts, preserving at least $1.267862 /two paid attempts overall.

Read-only authenticated run/artifact inspection found the exact old request
fc618603458249348c6834d258264520 terminal with HTTP400, but no usage or retained
error body. The original683 ledger rows and request remain unchanged. A new exact
receipt and one idempotent ledger event authorize ONLY the two new purposes. No
generic uncertainty bypass or provider unblock exists. Another unknown row blocks
these operations. The old logical request remains permanently claimed.

The protected cost plan reserves the entire remaining two-operation inventory
before dispatch. Conservative maximum $0.351005 ($0.061005 Luna + $0.290000 checker),
using current official September15 prices: Luna $0.20/$1.20 and Sonnet $2/$10 per
million input/output. Exact native count plus20%/1,024-token cushion tightens the
checker bound; Luna uses UTF8 wire bytes plus1,024 as a conservative input ceiling.
No paid token probe, old qualification balance or recurring allowance is included.

## Diagnostics and historical limits

Only these two operations opt into `diagnostics/<request-id>.json`, in the existing
90-day restricted accounting checkpoint. HTTP failures retain bounded redacted
status/request-id/error fields and any reported usage before raising. Final answer
text is retained before canonical validation, capped at196,608 UTF8 bytes; the
receipt distinguishes complete capture, truncation, redaction and validation.
Thinking/reasoning blocks, headers, cookies, credentials, raw requests and raw
provider envelopes are excluded. Diagnostic records are NEVER accepted caches or
serving inputs. Original error bodies or failed Luna content are not recovered.

The historical checker request reproduces exactly and shares the successful
Messages/thinking-disabled transport. No unsupported setting has been established
as its HTTP400 cause. Current documented schema complexity limits are respected by
the fixed keyed representation (no optional parameters or union branches); this is
not proof of why the old request failed or a guarantee of endpoint acceptance.

References checked September15:
[Luna](https://developers.openai.com/api/docs/models/gpt-5.6-luna),
[OpenAI structured output](https://developers.openai.com/api/docs/guides/structured-outputs),
[Sonnet](https://platform.claude.com/docs/en/models/sonnet-5/overview),
[Anthropic schema support](https://platform.claude.com/docs/en/build-with-claude/structured-outputs).

Run focused pair/diagnostic/accounting tests and required exact-head Python/browser
CI. Preserve the existing117 browser receipt only for unchanged runtime bytes;
this offline-only repair does not justify another full E2E run. Protected merge
requires a terminal independent exact-head review and passing required CI. No
production service deployment is necessary for this offline helper.
