# Phase 2 native token sizing prerequisite

Phase 1 is closed. The owner authorized three additional ordinary cold workflows and their output checks, at most $2.50 and 18 new **metered provider attempts** inside the existing $10 / 690 experiment, preserving $1.678020 and ten attempts. No public application/registry release or activation is authorized.

The full byte-per-token envelope for the revised references and complete source context exceeds this phase allocation. This prerequisite permits only the existing provider's **free token-count endpoint**, not a scientific Messages request, embedding, judgment or spending authorization. Anthropic documents token counting as free, separately rate-limited, and approximate: <https://platform.claude.com/docs/en/build-with-claude/token-counting>. The exact model remains `claude-sonnet-5`.

Run the existing protected-main manual preparation workflow with `contextual_check` set to the exact JSON object:

```json
{"phase2_token_preflight":"56a102550e8a231ad893bfe97dc94810c66fe2a6a20c433b4a75be00d0a24e9d"}
```

Leave every other input empty. The existing restore/reservation/checkpoint transport owns this operation. It reads the single authoritative paid ledger and verifies that token sizing did not alter one byte. No new ledger or allowance is created. Credentials exist only in the existing intended provider step. The public source packet is hash-pinned; callers cannot supply prose, endpoints, models, imports or scripts.

The finite initial inventory is 155 complete audited public profile documents plus nine scope/stage sizing packets for the three source-only proposed cases: native child `363302:a-1`, Mathematical Biology `341997`, and ECLIPSE `351715`. These 164 ordinary HTTP tokenization reads do not execute model inference or consume metered-attempt slots. They are reported separately; no account credit is treated as free inference. No scope recommendation or evaluation is generated by counting. Counter HTTP attempts are capped at 190, with exact cached reuse and no automatic repeat of an uncertain measurement. One bounded aggregate measurement file avoids expanding the existing checkpoint file-count envelope.

The three current source records preserve canonical identities and catalog hashes alongside newly retrieved official context. The DOE PDF hash matches the original child attribution; its full Topic 1a section and applicable eligibility/cost-sharing text replace the inadequate short summary for these inputs. The official text establishes 20% cost sharing, contrary to the older catalog flag. NSF pages retain complete scientific synopses and material submission conditions. This is a scoped source-context correction, not a profile rewrite, model-selected case, or complete application-compliance certification. The original source records and failed trials remain unchanged.

The new pure cold-demand response adapter carries forward Phase 1's exact provider reference enums and normalizes them into the existing graph shape. It does not yet enable the normal paid runner or change the deployed Worker. Scientific rubric, audited profile representation, model, retrieval and composer remain unchanged. The special Option 1 recovery runner remains intact.

Before any subsequent paid request, use retained native estimates with a 20% cushion, explicit structural/future-input byte bounds and complete output ceilings to produce a finite execution lock covering all three workflows and their checks. An estimate alone is not a guaranteed bill or an allowance. If that complete plan cannot fit both phase limits and protected reserves, stop paid work and report the precise remaining denominator. No truncation, weaker evidence, reduced verification or renewed budget is authorized.

This helper has no Worker, browser runtime, registry publication, schedule, permission or deployment change. A protected-main merge triggers existing required CI and release planning; the latter must remain a no-op for these helper-only bytes. Continue the authorized Phase 2 implementation after sizing; a merged helper is not Phase 2 completion.

## Review history

PR241's completed review of `d3080fa9d2172203ab6b2cc8970382dbe80fda54` found that the initially proposed `measurements/` path was outside the existing checkpoint restore allowlist. That candidate was not merged or executed. The complete cache/receipt/checkpoint path family was inspected before remediation. The aggregate now uses an existing `cache/<sha256>.json` path; no whitelist or protection was broadened. An archive produced by the real checkpoint writer is restored through the real unpacker three times for both completed and interrupted counter states, preserving identical ledger bytes and producing zero duplicate fixture dispatches. The corrected candidate still requires its own clean exact-head review and CI.
