# Conditional Stage 3 protocol after D2 — not authorized

D2 is complete with **REVISE**, not ready for independent validation. This document records a reproducible possible later analysis; it does not start Stage 3, open the holdout, authorize a new helper or deploy anything. Preserve the complete prior [D1 protocol](stage3-analysis-protocol-d1.md) as history. Its evidence, grouping, blinding, currentness, accounting, browser and decision requirements carry forward, with only the candidate and balance changes below.

## Exact candidate and comparator

Use `stage2-ranking-d2-candidate.json` and its exact dependency hashes if the user subsequently chooses to evaluate this frozen candidate. Code is bound to `ed303ab367ccb2f0bec3f0e62acf3ed45e9809a6`, coverage-v2.4 / ingredients-v2.4. Representation R0 retains the 493 D1 evidence/query rows. No R1 context vector, learned asset or MMR is active.

Automatic member quality is `.7*maximum scientific aspect cosine + .3*maximum scientific whole-call cosine >= .50`. Accessible admission stays at the original topic/core .4/.3 or evidenced method/context .5/.5, with group anchor .4. Keep `F(T)=sum_i w_i max_j r_ij`, fixed .5/.3/.2 aspect/core/lexical utility, scoped evidence Jaccard .9, .95 adequacy envelope and smallest adequate 2–4-person size. Within that envelope use weakest quality, mean quality, coverage, then stable identities. Alternative quality rho is .90, maximum eight without padding. Do not reinstate a universal positive removal marginal or a .45 raw group floor. All current real person/aspect evidence mappings remain unconfirmed. No department-distance or novelty reward.

Whole-call A retains .30 whole-call and .25 core floors on identical source/profile snapshots and matched B group sizes. It remains a comparator, not an activation fallback. Report every B abstention instead of inventing a matched group. The D2 candidate is frozen for reproducibility, not a quality-qualified winner. A different future candidate requires an explicit separately recorded choice before any final evaluation; it cannot be tuned against final results and still called independent.

## Unchanged sealed inventory and analysis

Development manifest SHA is `833cbb454369285b381e3083426553d9b836b90ad16d3b62b65f60d2e19cb9c6`; holdout is `6c08baadefcf2bab3da03684a7cc402228a0592ddf53a011816a0542229ccdd0`; source grouping overlay is `f92fc5995a0a1ac01fce18e8366236dd06f45cd01dcec9b0953dbc7bd4558359`. Retain 90 holdout scientific reservations and 30 controls, source-only replacements and final human source overlay `376f5b29d97de20efb4bbe2473ecd43fdcc3ebfba8abf81a6a9343a9aac2999a`. D2 generated and inspected zero held-out recommendations. Source-selected rollout overlap must not become development scoring.

After a separate Stage 3 approval, source-only preparation may reuse adequate original provenance and compatible exact vectors, preserving unknown restrictions/currentness and all unprepared denominators. Source readiness is not proof that a reasonable team exists. The present 35 prepared development scopes and 18/150 rollout ingredients are not the full final validation inventory.

Keep the D1F judge questions, bounded original call/profile evidence, algorithm/score/prior-answer blinding, and separate explanation audits. Use one primary judge and exact cache identity. Paid-failed, cache-lost reconciled and uncertain items are not automatically replayable, including by rebatching. Predeclare every evaluation item, alternatives sample and balanced order before grades. At most four stable-hash comparison pairs receive one order swap; conflicting results stay unresolved. No new prompt tournament, optional-model label campaign or silent substitution of group grades for individual grades.

Report strong/plausible/unrelated/insufficient distinctly, exact observed and missing denominators, list lengths and empties, source-prepared/unprepared, action-admitted/blocked, no-groups, primary members and alternative members by rank, unique source/person questions and repeated occurrences. Deduplicate across methods while retaining those occurrences. Report paired common-source differences and 5,000 source-cluster bootstrap draws, seed 20260910, with missing-label bounds. Machine labels remain machine observations. Independently feasible scopes require evidence independent of returning a group; otherwise feasibility is unknown.

Targets remain ≥80% macro reasonable top-five relevance, ≤5% unrelated automatic-member occurrences, ≥80% useful primary groups, ≥85% yield on independently feasible scopes and useful complementarity over matched-size A without increased unrelated-member harm. D2 did not meet the combined decision rule. Do not lower these targets, drop hard sources or tune the candidate/judge against the holdout or final audit.

## Human, browser and budget boundaries

The sole existing development packet remains 20 requested / zero returned; no replacement or additional development items are authorized. Twenty final audit items remain reserved for Marc, with source-first evidence, judge answers hidden until assessment and inability-to-assess allowed. Requested/skipped items count against the cap. Existing possible exposure and changed-item applicability remain disclosed. Missing answers cannot be called completed human validation.

Full browser/E2E/Playwright/accessibility and physical-device measurements require dedicated Stage 3 authorization. They must use the exact candidate and cover current child/manual/saved/full-slot/add/remove/Team Match paths, stale/currentness transitions and zero-provider cold/warm/edit/retry/error/cache-miss behavior. Ordinary search must not load or calculate team ingredients before the existing team interaction. No cohort publication or Stage 4 follows automatically.

The latest authoritative ledger is cloud run `34510982749`: $2.279110 spent, $0 reserved, $7.720890 remaining under authorization `on-demand-team-offline-v2-20260909`. Restore that same authority; the local mirror does not dispatch. A conditional later-stage allocation is **at most $3.90 Stage 3** ($0.10 embeddings plus $3.80 judging) and **$0.10 Stage 4 contingency**, all from the same $10 ceiling. Using the full $4 later allocation would leave cumulative spending $6.279110 and balance $3.720890, not create a new allowance. D2 remains below its $6 cumulative ceiling.

At September 10 prices, the preserved finite Stage 3 judge ceiling is 200 requests, 1,110,400 conservatively bounded input tokens at $2.50/M reserved and 102,400 output tokens at $10/M (actual current input $2/M); Voyage 4 lite is $0.02/M input. Reserve before every dispatch and include concurrent/uncertain work. The remaining lifetime request capacity is 354 of 690. Reconcile all provider/purpose token ceilings and packet sizes before any future run; unused D2 quotas are not a license to label holdout work as development. If complete evidence does not fit, report missing denominators rather than crop decisive evidence or request more money.

The current trusted executor permits Stage 2 purposes only. Future Stage 3 approval and any necessary narrowly reviewed purpose/allowlist change are separate prerequisites, not authorized by this protocol. No runtime provider fallback or production enablement is permitted.

READY requires the actual combined technical/quality gates. READY SUBJECT TO EXISTING MARC AUDIT applies only when that audit is the sole remaining condition. REVISE records scientific/quality/measurement failures; DEFER records an unavailable execution dependency. **D2 remains REVISE. STOP FOR USER REVIEW.**
