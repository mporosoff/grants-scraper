# D1 controlled diagnostic revision — predeclared before new judging

Authority: `DIAGNOSTIC_REVISION_D1.md`. This is a Stage 2 development revision, not a new scientific phase. Preserve C2 definitions, outcomes and failed judgments. Researcher record contents, development reservations, grouping overlay and sealed holdout are unchanged. No additional human packet.

## Controlled sequence

1. Retain the C2 `bounded-middle` matrix and coverage-v2.2 output for all 90 reservations. The 12 diagnostic cases were fixed in `manifests/diagnostic-cases-d1.json` before changes.
2. Improve call representation: central source contribution replaces award-title core; preserve native alternatives and remove generic study-stage targets. On the revised real inputs run the old selection code with unchanged parameters. This isolates source effects.
3. On the **old fixed matrices**, replace universal positive removal marginal with the existing independent aspect admission, group anchor .4, group floor .45 and scoped evidence nonredundancy. No extra composite-score admission floor. Retain the unsuccessful .4 composite-floor diagnostic: it removes the earlier group-producing case and is not adopted.
4. Combine supported source and selection corrections. Keep aspect/core/lexical weights .5/.3/.2, topic/core gates .4/.3 and group envelope .95. At numerically tied coverage prefer the stronger weakest member, then mean member contribution, then stable IDs. Avoid pairs whose retained scoped evidence sets are mutually near-identical (token Jaccard .9); no disciplinary distance. Exclude unnecessary larger alternatives when an eligible smaller subset reaches .95 of their coverage. Up to eight; no padding. MMR remains off.
5. Expose existing labels/types and research summaries as validated retained registry context. Do not embed taxonomy labels as evidence, add researcher text, or manufacture operation/context metadata. Audit high aspect similarity rejected solely by the core gate. The original matrix has four passage/aspect pairs with cosine >=.5 and core <.3, with zero shared specific two-token phrases. This does not establish a systematic excluded method-specialist population. Recheck after source corrections; retain the existing gates unless the recorded evidence supports a change.

## Corrected question and evidence policy

The trusted `config/team_recommender_executor/judge-d1.md` supplies separate source-suitability, aspect-person, call-person, group-usefulness, comparison and explanation questions. Strong/plausible/unrelated/insufficient-information remain distinct; source suitability uses coherent/broad-unselected/nonresearch/insufficient-information. Comparison ties and order conflicts remain unresolved. An aspect-person label is never substituted by a call-person label. Interests can support a conversation; they are not completed work or equipment. Overlap can be useful. Groups are not being certified for submission.

Use original bounded call context and frozen registry claim evidence, labels/types and summaries. No faculty-page lookup. Exact source spans and claim revisions must validate. Model-generated rationales are absent except in separate explanation audits. Hide scores, algorithm names, expected winners and old verdicts. Identical semantic items share one cache identity and retain all reporting occurrences. A/B comparisons use the same source/aspect inventory, evidence policy, question and matched size.

At most three items per request and 512 output tokens, with concise per-item reasons. Run complete schema/serialization, missing/duplicate verdict, deduplication, occurrence, ordering and inverse-remapping fixtures before paid judging. Begin with one small sanity packet. A failure is retained without a semantic/format retry. A D1 identity is justified by the materially different trusted question/fields; merely renaming an old failed item is forbidden.

## Finite item selection and metrics

Run numerical outputs for all 90 existing scientific reservations and 30 derived controls, with all missing, broad/unselected, restricted, action-blocked, no-group and group-producing counts visible. Source suitability never proves directory feasibility. No source is independently feasible merely because a group was returned.

For every prepared scope compare A whole-call top-five and B admitted top-five, with common returned-person and common-scope differences, unrelated-person occurrences, insufficient-information and missing verdicts. Preserve macro source/group denominators and short/empty lists. Judge the primary group and available alternatives, plus A at each B option's size; deduplicate repeated group/membership items. Compare matched-size primary A/B pairs with reproducibly balanced ordering. Audit the first four distinct comparison keys in hash order with swapped order; never replace an unfavorable or unresolved audit.

For the fixed diagnostics include one aspect-person item for each primary member when a group exists, otherwise the strongest admitted person, plus source-only suitability. Controls retain their declared source-dependent construction and all 30 dispositions. Audit explanations for the first 12 group-producing sources in fixed manifest order; do not select by verdict. If the final packet inventory exceeds the finite envelope, remove optional fitted/MMR work first, preserve A/B and all-case denominators, and report exact missing semantic items rather than claiming full validation.

Useful individual/group means strong or plausible; unrelated and insufficient-information remain separate. Report complete available denominators, missingness, macro scope rates and paired source-group bootstrap uncertainty. No silent missing-as-negative convention. Existing product targets remain unchanged and visible; D1 model estimates are not human validation or proof of precise calibration. Historical C2 grades are used only for the explicitly historical fixed-output comparison and failure trace, never spliced into D1 A/B estimates.

Optional components: consider at most one regularized logistic candidate (C=1, existing declared features and grouped development folds) and MMR lambda .1 only if at least five distinct prepared sources yield model-useful primary groups. Use the same D1 aspect-person labels; do not fit an aspect predictor to call-level grades. No additional paid labels solely for fitting. If labels/groups are too few, record not meaningfully evaluated, not ineffective. Otherwise compare operating points within grouped development folds, stability and runtime. Keep the simpler scorer absent a material supported improvement. No holdout access.

## One budget, execution and stop

Reconciled cloud owner before D1: run 34472949357, 147 requests, $1.082518 charged, zero outstanding reservations. Authorization remains `on-demand-team-offline-v2-20260909`; $10 across four stages, cumulative Stage 2 <=$6. Old task spend is not a credit.

Compatible reuse: all 398 unique frozen researcher vectors and unchanged exact query inputs. Four missing call/aspect query rows form one 3,935-byte public packet, with a 4,409-token conservative bound and $0.000089 maximum reservation. No new extraction model, researcher embeddings, reranker, probe campaign or source-interpreter call is planned.

Corrected judging uses the existing Sonnet 5 route only: at most 200 requests, 1,360,000 conservative input-token bounds and 102,400 output-token bounds. Maximum summed new judge reservations is $4.424. These are ceilings, not request targets. Prices checked September 10, 2026: [Voyage 4 lite](https://docs.voyageai.com/docs/pricing) $0.02/M input; [Sonnet 5](https://platform.claude.com/docs/en/about-claude/pricing) $2/M input and $10/M output, conservatively reserved at $2.50/M input. No prepaid-credit exemption. Worst-case cumulative expense for this plan is $5.506607, retaining at least $4.493393 overall. Actual packet counts, bounds and expected usage must be recorded before dispatch; exact cache reuse can only reduce new work, never create extra authority.

PR #219 helper head d37096ef2a8814a06530350f936714d91d1d0bf9 had clean exact-head review, required CI 34490287076 and protected merge 4884d2b9ca4b6b8bdb6be613d92aef5a8c8e4435. The main-only executor restores the one authoritative cloud checkpoint; local mirrors never spend. Reservations precede dispatch, charge uncertainty is retained, and lost results never cause automatic replay. No workflow or permission changes. No recommender merge or deployment.

Marc's existing 20-item development packet remains unchanged, with zero returned judgments at D1 start; 20 final items remain reserved. Record applicability after source changes without creating replacements. Stop after the diagnostic revision report for user review. Stage 3 and full E2E/accessibility remain unapproved.


D1 packet construction addendum, before any D1 paid judgment: the first all-claims layout required 195 requests and 1,896,082 conservative input tokens and was rejected without dispatch. Using each person's strongest whole-call passage plus best admitted aspect passages (the union for the same source, applied identically to A/B), their exact claim labels/types and FULL unchanged research summary reduced duplication. A second layout still exceeded the envelope at 163 requests/1,401,570 input tokens and was retained as a zero-paid failure. Removing repeated boilerplate already in the fixed rubric yielded 162 requests/1,358,239 conservative input tokens, 82,944 maximum output tokens and $4.225076 maximum summed reservations. No decisive source condition or supplied passage was cut to rescue an over-bound request. One long source-context item (363514) remains explicitly unsubmitted.

The finite inventory is 333 unique questions, 515 reporting occurrences, 332 submitted items: 227 call-person, 14 aspect-person, 42 unique groups, 6 comparison/order-audit, 5 explanation and 39 source-context items (one too large). Three matched primary pairs differ in membership; two are identical and need no preference request. Each distinct pair is order-swapped once. The three-request sanity packet is an exact subset of the full packet, so successful results will be reused byte-for-byte without another charge. D1 asks for no new human judgments.


D1 response-format correction (before full evaluation): sanity runs 34494191625 and 34494473553 each returned one charged, schema-invalid response; four item judgments are lost and remain unresolved, never retried. Cumulative spend is $1.098648. The trusted D1F format preserves the scientific questions/evidence/labels and replaces free-text reasons with schema-enforced finite reason codes. Codes are compact rationales, not additional scientific evidence. PR #220 exact head ae265d5400d4c79e09b26e6814ff91636e3a2fa9 passed 40 focused tests, required CI and clean exact-head verification, then protected-merged as 29737536fa82716dcafdbfa98ffb11121591df5c. One reproduced near-bound historical-key finding and its correction remain in PR history. Both formats prohibit replay of a paid counterpart.

The remaining exact inventory is 160 never-dispatched requests / 328 items; cumulative D1 reserved input bounds including both paid failures are 1,347,679, below the same 1,360,000 ceiling. Maximum remaining request reservations sum to $4.139178. At current actual usage the worst-case cumulative expense is $5.237826, preserving $4.762174. The three-request D1F sanity packet is an exact subset of these 160; successful results are reused when the full packet runs. No new semantic label campaign, task replacement, provider, budget or human packet. Source/context question 363514 remains too large, and the four paid-failed items remain missing. Exact manifests are in receipts/d1f-execution-plan.json.


Post-evaluation diagnostic, explicitly outside the frozen primary A/B comparison: 20 call-person items judged plausible were outside B admission. One zero-provider contributor-core sensitivity (.30 to .25, same aspect .4/group anchor .4, same frozen vectors) admitted 38 additional unjudged scope/person occurrences but changed no primary group and no already judged admission. It is not retained and is not a semantically qualified alternative. The predeclared optional-component condition was not met (four model-useful primary sources, threshold five); D1 MMR/fitted work is not evaluated, not declared ineffective. No extra labels were purchased.
