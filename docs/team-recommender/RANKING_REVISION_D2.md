STAGE 2 CONTINUATION D2 — CALIBRATE RELEVANCE, IMPROVE ALTERNATIVES, AND USE EXISTING PROFILE CONTEXT

Continue Stage 2 ONLY of the on-demand team recommender. Preserve the D1 implementation and results; make one bounded ranking/representation revision. Do not restart, open the holdout, begin Stage 3, or deploy the recommender.

Repository: mporosoff/grants-scraper
Branch: codex/on-demand-team-recommender
Worktree: C:/Users/Marc Porosoff/projects/grants-scraper/.worktrees/on-demand-team-recommender
Reported D1 code/data checkpoint: f148325cc456e8d26d8d71c0541a2b3aad470a5c
D1 versions: coverage-v2.3 / ingredients-v2.3

Read the complete actual PLAN, HANDOFF, amendments, experiment state, D1 diagnostic report, candidate manifest, numerical receipts, judge packets/results, and applicable AGENTS instructions. Resolve the latest local report/state commit and any subsequent work. The reported code checkpoint is a reference, not an instruction to reset the branch.

Record D2 in the existing documents/state and preserve prior evidence. These sections are work within Stage 2, not additional project stages.

1. AUTHORITY, BOUNDARIES, AND BUDGET

The product remains an exploratory collaborator recommender. Suggest relevant people worth a scientific conversation; do not certify qualifications, equipment, willingness, or proposal success.

Preserve:
- On-demand deterministic calculation; zero paid calls on prepared-scope build/edit/retry/error paths.
- Existing presentation, fixed strings, controls, child selection, source links, explanations, saved/manual paths, and separate all-member Team Match behavior.
- Two-to-four-person groups, all admitted candidates accessible through existing controls, and up to eight genuinely useful alternatives WITHOUT padding.
- Scientific relevance and a group-level scientific anchor. No disciplinary-distance reward, random exploration, or novelty bonus.
- Currentness, source ownership, privacy, immutable snapshots, the sealed holdout, and the existing source-selected rollout inventory.

NO RESEARCHER ENRICHMENT. Do not scrape faculty pages, retrieve publications/ORCID, add or rewrite claims, infer capabilities, change eligibility, or recertify the registry. Names, claims/revisions, evidence, labels, summaries, URLs, and registry generation remain unchanged.

D2 DOES authorize a numerical representation of information ALREADY stored in that frozen registry. Encoding an existing label or research summary is allowed; adding factual content is not. New derived vectors may therefore be needed even though the researcher records remain byte-identical. Such vectors are not additional evidence of expertise.

Use authorization on-demand-team-offline-v2-20260909 and its existing authoritative cloud ledger. Reported D1 spend is $2.138302, no outstanding reservations, last reported owner run 34497056168. Verify current state rather than resetting or assuming no later usage.

The experiment ceiling remains $10 TOTAL; cumulative spending through Stage 2 must remain <=$6, preserving at least $4 for later stages. At the reported balance, Stage 2 has at most $3.861698 remaining. These are ceilings, not spending targets. Include all new embeddings, judgments, probes, retries and uncertain charges. Reused work creates no spending credit.

Begin with zero-provider diagnosis and reuse compatible judgments. Before dispatch, record a finite D2 request/token/cost inventory that fits both the remaining Stage 2 balance and later-stage reserves. Reconcile existing cumulative request/token limits as well as dollars. Do not repeatedly expand batches to obtain a favorable result.

The existing trusted executor remains the only paid route. If its allowlist needs to admit the D2 derived representation, this prompt authorizes a narrowly reviewed prerequisite change to construct/validate that exact deterministic representation FROM THE TRUSTED FROZEN PUBLIC REGISTRY. Do not accept arbitrary new researcher descriptions from branch packets.

This narrow authority includes required tests, independent exact-head review, protected merge and bounded manual execution. It does not authorize merging the recommender, changing provider families or permissions, exposing credentials to branch code, adding schedules, or publishing any cohort. Preserve the same ledger and failed-request history. Do not introduce a new judge rubric unless the existing packet audit demonstrates an actual task/mapping defect.

2. ESTABLISH THE REMAINING FAILURE MECHANISMS BEFORE EDITING

Reproduce D1 on its actual ingredients and fixed decision clock. Retain the baseline outputs. Use the 35 prepared development scopes for numerical analysis and continue accounting for all 90 scientific reservations plus 30 controls. Do not expand source preparation or enrich profiles in this revision merely to increase counts.

Investigate three specific hypotheses:

A. CROSS-SCOPE GROUP CUTOFF: Seventeen prepared scopes have an any-size coverage upper bound below .45, including cases with model-judged plausible people. Determine which are rejected solely by this uncalibrated composite-score floor, versus genuinely poor candidate evidence. Separate lexical overlap, aspect similarity and core similarity distributions.

B. STRONG-MEMBER CARRYING: Maximum coverage can give the same score to a strong pair and a pair containing the same strong researcher plus a weak partner. Check each D1 alternative's member relevance and evidence, coverage, weakest/mean member scores, rank, and individual/group grades. Determine how often weak partners pass because coverage ignores them or member quality operates only as an exact-score tie-break.

C. CONTEXT MISMATCH: Verify exactly which existing researcher fields the scorer uses and which the D1 judge saw. The reported implementation retains labels/summaries but numerically scores only the short evidence passages. Confirm this from executable code and packet construction rather than assuming it.

For every suspected cause, report confirmed / not supported / unresolved with a concrete trace. Do not start by lowering all thresholds, changing the evaluator, or writing another large framework.

3. TEST ONE EXISTING-CONTEXT REPRESENTATION

Keep the D1 call text, aspects, source-group weights, directory, source receipts, and evaluation questions fixed except for a demonstrable integrity/projection bug.

Compare:
R0: D1 exact evidence-only numerical representation.
R1: One deterministic contextual representation using ONLY the associated existing claim label/type, exact claim evidence and existing research summary where present.

Choose and record the R1 serialization and aggregation rule before observing its semantic results. Keep the exact evidence passage as the explanation anchor. Use summary/label context to disambiguate meaning, not to claim that every technique mentioned anywhere in a profile supports every role.

Prefer a small separate context channel or a bounded claim-specific encoding over repeating an entire long summary for every claim. Do not select among many rewritten representations. Do not generate missing operation/context descriptors with an LLM. Preserve exact field attribution and a truthful missing-context path for sparse profiles.

Use identical candidate/selection settings in the R0/R1 diagnostic comparison so representation effects can be isolated. Reuse all unchanged vectors; generate only the unique new R1 rows through the trusted route within budget. Do not change or rebuild the general-search embedding corpus.

Test that redundant text, long profiles and generic labels do not buy artificial relevance. A summary supporting a plausible discussion must not activate a confirmed capability label. Prove the underlying researcher-object and claim hashes remain unchanged.

4. SEPARATE PERSON ADMISSION, GROUP COVERAGE, AND ALTERNATIVE QUALITY

Retain the primary coverage objective:
F(T) = sum_i w_i max_{j in T} r_ij

Do not restore a universal positive removal-marginal requirement. Retain legitimate overlapping contributors, a scientific anchor, relevance-based nonredundancy, and deliberate smallest-adequate group selection.

PERSON ADMISSION:
Maintain a broad, reasonable accessible candidate list. Automatic team placement may use a more selective member-quality rule, but do not silently remove plausible candidates from the existing lists or manual paths. No irrelevant person is admitted for diversity.

Use the actual D1 CALL-PERSON labels, with their original input identities, to evaluate call-person admission/ranking. Strong/plausible may define the positive event; unrelated the negative event. Insufficient information and missing verdicts stay separately reported. Do not convert those call-level labels into aspect-level training truth.

Compare a simple fixed-score calibration/operating rule with at most one compact regularized call-person scorer using the declared available features. Use grouped development folds and a small predeclared parameter range. All members of a source/near-duplicate group stay in one fold; repeated verdict occurrences cannot become independent training examples. Fit transformations inside the training folds.

There is NO five-useful-primary-sources prerequisite for this local analysis. Model adequacy depends on the actual label/source-group counts. Do not force a fit if the evidence is inadequate. A Bayesian version is optional only as a small local comparison using the SAME features/labels and no additional paid-label campaign; it is not a completion gate.

These labels are sampled, noisy machine judgments. Report their selection bias and out-of-fold results. Do not claim calibrated population probabilities or human preferences. Do not let in-sample predictions masquerade as independent development performance.

GROUP COVERAGE:
Test replacing the universal .45 raw-coverage eligibility cutoff with the independently evaluated member/anchor admission rule, while using F(T) to rank complementary coverage. An absolute safeguard must still reject uniformly poor pools. Do not normalize every call's best available person/team into a strong match.

Use a small fixed comparison of rule families, not another arbitrary threshold sweep. Preserve the D1 cutoff as the comparator. Show specifically whether plausible optics/imaging candidates can form reasonable groups without restoring the unrelated wave-computing or generic-method matches.

Coverage is utility, not probability of a complete team. Do not compare raw similarity and fitted scores using identical numeric cutoffs and call that a fair operating point; select their operating rules on grouped development data.

ALTERNATIVES:
Every automatically included member must pass the automatic member-quality condition; a strong anchor cannot carry an otherwise weak partner. Within the near-best coverage envelope, weakest-member and mean-member relevance must matter substantively, not only when coverage ties to six decimals.

Record one transparent alternative rule with a small development-tested setting range. Preserve a relevant core and comparable coverage; then prefer stronger member quality and nonredundant combinations. Return fewer than eight when further alternatives materially weaken member quality. Do not force an option count or suppress useful candidates from the individual list.

Keep MMR OFF throughout this revision. Reordering weak alternatives cannot repair their admission. Do not add disciplinary-distance metrics, new interfaces or a new optimizer architecture.

First test selection corrections on FIXED D1 matrices. Then combine only justified selection changes with the predeclared R1 representation. Preserve each isolated comparison and any unsuccessful result.

5. AUDIT EXISTING JUDGMENTS, THEN PURCHASE ONLY MISSING EVIDENCE

Inspect the nine D1 cases where a useful group includes an individually unrelated researcher. First check exact person/source/claim identities, evidence selection, target contribution, task framing, verdict remapping and aggregation.

Classify each as:
- Different legitimate target contributions or evidence contexts;
- A packet/mapping/aggregation defect;
- A group judgment plausibly driven by one strong member;
- An unresolved semantic disagreement on comparable evidence.

Do not assume all nine are model errors, and do not let the favorable group grade override the individual result. Fix demonstrable software defects with deterministic fixtures. Preserve the historical verdicts and their original denominators.

Keep the corrected D1/D1F judge task and original evidence policy fixed unless this audit demonstrates a necessary correction. Scoring R1 may use context the D1 judge already received; that alone does not require buying new judgments of unchanged items.

Reuse exact compatible call-person and group judgments regardless of which algorithm returned them. New members or groups with no comparable existing judgment require separately recorded evidence, not borrowed labels from a different team or aspect. A new display/algorithm ID is not a reason to repay for the same question.

If a material task/evidence correction is necessary, version it explicitly and reevaluate both arms under the same policy. Never splice incompatible old A grades with new B grades. Do not rerun terminal paid failures or conflicting completed answers merely to obtain a preferred outcome.

Evaluate on the full existing development inventory, with prepared/unprepared/action-blocked/no-group dispositions preserved. Judge newly surfaced primary groups, their members, and a predeclared bounded sample of alternatives where cached grades are unavailable. Cover alternative positions and source families; do not judge only the best-looking groups. Record unjudged options and do not claim all eight alternatives have been semantically validated from a sample.

Compare whole-call A, exact D1 B and the proposed D2 candidate on common sources and matched sizes where possible. When one arm returns no team, report that abstention and compare yield; do not drop those cases from the product assessment or invent a matched group.

Report:
- Available top-five relevance, list lengths and empty lists;
- Common-source paired differences and grouped uncertainty;
- Primary-group yield and usefulness;
- Primary members versus alternative members, with per-option-rank breakdown;
- Unique people/source pairs versus repeated option occurrences;
- Unrelated and insufficient-information outcomes separately;
- Explanation faithfulness separately from suggestion quality;
- Action-admitted and all-prepared outcomes, without hiding offline failures;
- Unknown feasibility, unprepared reservations and unjudged denominators.

Preserve existing 80% reasonable-relevance, 5% unrelated-member, usefulness/yield targets and their disclosed limitations. Do not lower targets, select easier sources, or define feasibility as the algorithm returned a group. Development improvements are not independent confirmation.

6. VALIDATION, HUMAN LIMITS, AND COMPLETION

Use focused numerical/reference, full-directory, currentness, identity, adapter, candidate-access, eight/fewer-option, add/remove, cache and error tests on the actual revised ingredients. Recheck real asset size and runtime. Preserve stable outputs and truthful evidence-state mapping.

Prepared-scope cold/warm build, options, replacement, retry, cache miss and failure must make ZERO paid calls. Ordinary search must not load or compute team ingredients. Do not claim a Node/DOM pass is a physical-browser or full accessibility result.

No full E2E/Playwright/accessibility suite, holdout scoring, recommender merge, cohort activation, subscriber mail, old Sonnet service enablement, researcher enrichment or Stage 3.

Marc's existing 20-item development packet remains the only requested development packet. Do not replace items, repeat the reviewer-availability question, add judgments, or use the reserved final 20. Record returned answers if any, and whether changed inputs limit their applicability. Missing answers do not halt safe automated work or become fabricated review.

Use the repository's bounded exact-head review process for authorized helper changes. Consolidate consequential findings in the changed subsystem, keep clean review/CI before merge, and do not stop merely because an older historical round was used. If the same concrete consequential defect survives two distinct focused repair attempts, or a genuine security/authority/budget/provenance barrier remains, checkpoint that exact blocker. Do not broaden the task to theoretical completeness.

Write a NEW full report, preserving D1:
docs/team-recommender/reports/stage-2-ranking-revision-d2-report.md

Update experiment state and freeze exact code/data/scorer/representation/selection/judge identities. Include:
1. Resumed and final checkpoints, actual helper changes and unchanged production.
2. Confirmed versus unsupported explanations for the cutoff, weak-partner and context problems.
3. Isolated R0/R1 and fixed-matrix selection results, followed by the combined candidate.
4. Researcher-field hash proof: no enriched, edited or recertified records.
5. Actual label event, grouping, fit/calibration limitations and optional-model disposition.
6. The nine judgment-disagreement dispositions and any necessary measurement correction.
7. Real A/D1/D2 results with full denominators, unjudged outcomes and uncertainty.
8. Useful overlapping contributors, applicable transfer, rejected irrelevant candidates, no-group and weak-alternative examples.
9. Presentation/zero-paid-interaction tests, actual payload/compute measurements and unrun checks.
10. The single ledger: charges, reservations, exact cache reuse, new vector/judgment counts and at least $4 preserved for later stages.
11. Existing human-packet status; unchanged source/holdout/rollout selection and honest 150-scope readiness.
12. One justified development finalist, or the specific reason none is justified, and READY / READY SUBJECT TO EXISTING MARC AUDIT / REVISE / DEFER.

Finish the bounded automated sequence, not merely its helper implementation or first example. Do not force a pass. If quality remains inadequate, identify the strongest demonstrated remaining bottleneck and the evidence limit rather than automatically proposing another large evaluation cycle.

STOP FOR USER REVIEW. Do not begin Stage 3.