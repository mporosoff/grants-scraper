# Iteration 2 protocol — fixed before new outputs

Authority: the owner's September 21 Iteration 2 request. This protocol consumes the already-installed completion pool; it grants no new credit. Public activation, registry cutover, legacy withdrawal and recurring paid work remain disabled. Detailed scientific/provider reports stay local.

The source-selected development manifest has 12 cases. Its separate 12-case confirmation manifest is sealed before tuning; no confirmation scientific output may be generated or opened in this iteration. Replacement is permitted only under the source-only rule in that manifest, never because a recommendation is unfavorable. Historical ECLIPSE and DOE inputs retain their scientific identities and their current action eligibility is evaluated separately.

Scientific interpretation and applicability use the established Luna low configuration. A separate Sonnet production verifier can only retain, downgrade or remove an assessed relation; it cannot introduce support. Independent evaluation is a distinct blinded task and is not production verification. Original Iteration 1 answers, failed request, charge, saved Luna output and zero-provider recovery remain immutable exposed regression evidence.

For each development case, report source suitability, top-five relevance, primary group, first alternative where one exists, exclusions and explanation judgments. The top five are the first five unique people in the deterministic full-directory retrieval shortlist; missing people/labels are reported, not imputed as successes. Reasonable relevance means strong or plausible independent call-person judgment. Useful group means strong or plausible independently judged group usefulness. Report the unrelated fraction among all automatically selected primary members, with missing labels separately. Feasible-scope yield uses only scopes independently labeled feasible from an adequate evidence frame; a 12-person shortlist cannot establish directory-wide infeasibility. If this denominator cannot be established, yield is unmeasured, not passed. Targets remain 80% relevance, 80% useful groups, at most 5% unrelated automatic members, and 85% yield. Report numerators, denominators and missing judgments; twelve cases do not establish population performance.

The cold sampling unit is one first explicit Build action for one previously unassessed development scope in the actual restricted interface. Attempt every actionable coherent case in the fixed 12-case frame once. Source-blocked and broad-parent selection controls are separate dispositions, not cold successes. Existing exact directory vectors may be reused and must be disclosed as prepared evidence. Interpretation, query, assessment, verifier and graph must be cold for a fully cold observation; any compatible reused scientific result makes the observation prepared/warm instead. Preserve failures and absent completions in the attempted-case denominator and report them separately from completed latency statistics.

Cold elapsed time starts at the user Build click and ends after graph identity validation, production-verifier provenance, durable callback and visible composer rendering. Include queue/setup, source preparation, native counting, providers, persistence, delivery and polling. An acknowledgement, receipt, spinner, partial answer or unverified draft is not completion. Record stage timing for diagnosis, never substitute it for user elapsed time. Median uses the middle observation (average of the two middle values for even n); observed p90 uses nearest rank ceil(0.9*n). Targets: median <=60 seconds and observed p90 <=120 seconds. Report small n explicitly, without SLA claims.

For each completed graph, take ten persisted reload/cached-render observations and ten each of option changes and exclusions/restoration, measuring the observed p90 by the same nearest-rank rule. Cached graph-to-render must be <1 second; local actions must be <200 milliseconds and cause zero new metered inference. Include first composition separately from cached options. Retain existing 48 MiB incremental-memory target; unavailable browser memory telemetry is missing evidence. Use the owner's Windows desktop in-app browser and its normal network to Cloudflare/GitHub/provider services; record actual runtime/hardware metadata available at measurement. GitHub hosted runner and network variability are part of the cold route, not excluded overhead.

Focused zero-provider contracts exercise simultaneous first-build claims, repeated reads/polls/reloads, lost callbacks, stale/corrupt graphs, complete pair ownership, profile/registry/source invalidation, provider failures and budget exhaustion. Targeted real restricted-browser observations complement them. Do not run the full manual E2E/Playwright suite.

If measured infrastructure overhead or scientific stages prevent qualification, preserve the measurements and state the exact boundary. Do not relax the endpoint, hide failed cases, discard disagreements or buy another unchanged answer to obtain favorable results.
