# D3 declaration before new vectors or judgments

This continues Stage 2 from D2 `6b74ea43f9b402c93be323335cc0bbbb50b2cbe1`. The attached D3 plan is authority. Preserve 90 scientific reservations, 30 controls, the same 35 prepared scopes, 155 eligible researchers, all source groups and sealed holdout. Comparison clock remains 2026-09-10T12:00:00Z; interaction decisions refresh between actions. No enrichment, source expansion, new human packet, Stage 3 or deployment.

## Inputs and controlled representations

E0 reproduces the exact D2 candidate. E1 encodes the same 398 independent evidence phrases with voyage-4-large. E2 encodes one actual combined document per person with that same model. E3 encodes the same document content as ordered contextual chunks with voyage-context-4: one optional existing-summary chunk, then each distinct evidence statement with its existing labels/types. Each contextual document contains exactly one person. Interest chunks, not the summary chunk, supply E3 local maxima. No global/context blend is added. All new arms initially use the identical 95 distinct D2 scientific query strings, query role, 1024 dimensions and float output. E1/E2 share exact identical model/query inputs; contextual queries each occupy a singleton nested list. No cross-space reuse. Names/departments/URLs remain attribution only. Existing source/profile text is never rewritten or cropped.

The trusted constructor and hash-bound query table define exact UTF-8 inputs. Preserve whole ordered document/chunk attribution for contextual keys. Changing any contextual document content invalidates every chunk. Cosmetic name changes do not change vectors. A combined input is actually encoded, not an average. Optional purpose-query, hybrid, Bayesian and MMR comparisons are not selected for this finite initial experiment; do not add them automatically after unfavorable results.

## Common calibration and numerical comparison

New-arm call-person quality is q=.7 maximum scientific-aspect cosine + .3 maximum whole-call cosine. It is not a probability. Use the 211 existing D1F strong/plausible/unrelated call-person labels available at declaration, deduplicated by exact scientific item. Insufficient-information and missing labels are excluded from fitting and retained in reporting. They describe usefulness for at least one call contribution, not aspect expertise or human approval. No new D3 judgment retunes thresholds.

Five grouped outer folds use the existing source-group overlay and SHA256 ordering of `D3-outer|group`, round-robin. For each outer training partition, compare exactly three member-floor rules: its positive q quantiles {.10,.25,.50}, NumPy linear quantiles. The broad individual floor is its positive q .05 quantile; the aspect admission floor is its positive maximum-aspect .05 quantile; the scientific anchor is its positive core-maximum .10 quantile. These are absolute frozen thresholds applied across calls, never per-call rank normalization. Each automatic member passes its own q floor and scientific aspect admission. No old D2 raw gate or capped uplift is a prerequisite for a new arm.

Select the largest training positive yield among rules with <=5% unrelated selected labels, at least 10 selections and 3 source groups. If none meets that target, use TP-4FP, then fewer FP, then stricter threshold. This is the unchanged development tradeoff rule, not a lowered product target. Report all three settings, outer-fold outcomes, missing denominators and threshold stability. No fitted coefficients are needed for this fixed-score procedure. Final operating thresholds use the same selection rule on all existing labels and are explicitly development refits. Apply this identical procedure to E1/E2/E3 and to an E0-C control using the retained lite vectors; E0-C isolates calibration/selection changes from model strength while exact E0 remains intact. E1 versus E0-C isolates model strength, E2 versus E1 granularity, and E3 changes contextual architecture/model jointly. There is no claim that E3 isolates model strength alone.

For every new representation use the same source-weighted objective F(T)=sum_i w_i max_j r_ij, with r=.5 aspect cosine+.3 core cosine+.2 original-evidence lexical overlap after admission. Original passage tokens prevent generic-only evidence admission. A global/context score never changes evidence status. The existing bounded optimizer retains 2–4 people, smallest adequate size, .95 coverage envelope, scoped evidence Jaccard .9 nonredundancy, minimum/mean member quality before coverage within the envelope, alternative minimum-quality ratio .90, stable IDs, max eight without padding, MMR off. Report removal marginals without restoring a universal positive marginal condition.

For each representation also rank whole-call vectors independently of aspect eligibility. Calibrate its absolute whole-call floor with the same three positive quantiles and selection rule. Compare top-five lists and matched-size groups, retaining cases with too few qualifying baseline people as abstentions. Full 155-person numerical scoring is required on every prepared scope. No-group and unprepared remain separate. E0-C and all new-arm calibration are development diagnostics, not independent validation.

## Finite semantic evaluation and choice

Keep exact D1F rubric, task identities, source contexts and original D1-selected profile evidence, including unchanged summaries and associated labels/types. Hide representation identities, scores, expected winners and previous grades. Reuse exact D1/D2 judgments; historical paid failures remain unresolved and cannot be rebatched for replay. Record all occurrences even when one item serves multiple arms.

Inventory all-arm top-five, primary-group/member and alternative-group/member questions. New primary/member questions have priority, followed by top-five questions, then alternatives at ranks 2 and 8 where present plus one interior rank chosen by SHA256(scope|option identity), then matched-size comparisons and explanation audits. Rank missing questions within each priority by stable source/item hash, with source round-robin breadth before additional items at one source. Sample at most 25 distinct matched-size comparisons with balanced reproducible A/B ordering, four hash-selected order swaps, and eight source-hash-selected actual explanations. Identical groups are ties without another paid request. Conflicting order swaps remain unresolved. Do not crop evidence to fit; preserve unsent denominators if the finite envelope cannot cover all items. No replacement human packet: 20 previously requested development items, zero returned at declaration, 20 final-audit items reserved.

Report strong/plausible/unrelated/insufficient separately; primary group usefulness separately from each member and explanation faithfulness. Report all prepared and action-admitted outcomes, list lengths, empty lists, alternative rank/unique and repeated-member counts, false-positive and method-transfer examples. Paired common-source differences use 5,000 source-group bootstrap draws, seed 20260910. Missing-label and abstention effects stay visible. Independently feasible-scope yield remains unmeasured, never defined by algorithm yield. Original targets remain 80% relevance, <=5% unrelated members, 80% usefulness and 85% yield on independently feasible scopes.

Prefer the simplest representation with an observed paired relevance improvement of at least five percentage points over exact E0, no worse observed primary unrelated-member fraction, and at least 90% of E0's judged reasonable primary-member occurrences and useful primary-group yield. Require >=80% of each compared arm's primary-member occurrences and top-five occurrences graded to make that selection; otherwise mark evidence incomplete. Among qualifying representations use useful primary-group count, then reasonable primary-member yield, then fewer unrelated primary members; exact ties prefer E1, E2, E3 in that order. Report uncertainty; these development selection criteria do not waive the original acceptance targets. If none qualifies, retain exact E0 as an unqualified fallback and conclude no demonstrated winner. Do not tune again or add a reranker to force success.

## Durable budget and execution

The restored cloud checkpoint is 34510982749: 336 requests, $2.279110 charged, $0 reserved. Ledger SHA256 590e761abeefde54343e83f09341cca931b8149e0ab01d268c7acfd8c109413e; checkpoint SHA256 7119bbaa839c7feeae0e1af92032fc8ed2ec48aaa7c81612a90c4c70c7d44b71. One authorization remains on-demand-team-offline-v2-20260909. Local copies are read-only mirrors. No historical credit, allowance reset or simultaneous spending copies.

D3 finite maxima: 28 embedding requests / $0.18 conservative reservations; 120 judge requests / 1,100,000 conservative input units / 61,440 output tokens / $3.3644 reservations. Maximum new $3.5444 yields cumulative <=$5.823510 and >=$4.176490 for later stages, within the same $10/$6 ceilings. Use actual smaller packets, not quotas. Prices checked September 10, 2026: large/context $0.12/M input, lite $0.02/M ([official Voyage prices](https://docs.voyageai.com/docs/pricing)); existing judge $2/M input and $10/M output with conservative $2.50/M input reservation. All failures, probes and uncertain requests count. Current request shapes follow [the contextual guide](https://docs.voyageai.com/docs/contextualized-chunk-embeddings).

Use reviewed PR #222, head 0b42bc4f718321859747c51f898a31415b3327cb, protected merge 2071a0b850e79af037490dc0a34483c6e4844fde. Required CI 34536474441 and terminal clean configured review reaction cover that exact head. No review findings, workflow changes or production-runtime changes. Freeze hashed packet sizes before dispatch. Start with reusable source-stratified queries (NIH olfactory imaging, DoD optics, NASA instrument concepts) and three frozen person documents, then complete all remaining inputs. A failed paid item is terminal/recovery-required, never an automatic retry. Do not stop after a successful small batch.

Measure actual encoded/compressed candidate bytes, hydration, full-directory matrix, group and cached-edit work using the existing loader if a new representation earns adoption. Preserve full-profile/source hashes, candidate reachability, source/currentness checks, zero paid runtime traffic and stale/corrupt snapshot rejection. Node/DOM observations are not physical-browser or full E2E validation. Write the new D3 report, update exact receipts/state and STOP FOR USER REVIEW.
