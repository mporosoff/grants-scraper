# D2 bounded development revision — declaration before new representations/results

D2 continues Stage 2 from `ec9c717cf1943ac68be4b1a4ba453bbb29dbd931`. It does not authorize Stage 3, a new human packet, source expansion, enrichment, MMR or deployment. The exact D1 numerical reproduction and diagnosis are recorded in `receipts/d2-before-edit-diagnosis.json`. Preserve all source/aspect/weight/currentness inputs, the 155-person directory and the 90+30 development reservations. The fixed comparison clock is 2026-09-10T12:00:00Z; interaction tests still refresh the clock between actions.

## Failure audit and unchanged judge

All 332 submitted D1 item mappings reproduce their exact identity. Seventeen scopes fail the universal .45 floor after pool/anchor checks; fifteen have already judged reasonable admitted people. Eight of twenty unrelated alternative-member occurrences have zero marginal, but twelve have positive marginal; zero marginal is not a semantic test. Nine useful-group/unrelated-member disagreements have identical per-person source/claim/label/summary evidence after removing local evidence aliases. One may reflect strong-member carrying; eight remain unresolved on comparable evidence. No software mapping defect or necessary rubric change was found. Preserve every grade and missing item.

Use the exact D1F scientific questions, source contexts, profile evidence selection, response schema, reason enums and item identity. Existing D1-selected passages remain the shared evidence policy even if R1 changes ranking. Labels, full unchanged research summary, source context and aspects were already in D1 judge inputs. R1 alone does not justify rejudging an unchanged item. Cached exact items are reused across algorithms, with their original execution receipts. Do not replay paid-failed tasks or conflicted grades, including by regrouping them into another batch.

## One R1 representation

R0 uses the exact D1 evidence vectors. R1 adds two compact channels to that same evidence anchor as **one fixed representation**, not a family of rewritten profiles:

- For each distinct retained evidence passage, serialize a canonical JSON object with `evidence` equal to its exact evidence text and `claims` equal to the sorted, deduplicated associated `{claim_type, label}` objects. Use only active claims with that same exact evidence. No names, departments, other claims, inferred descriptors or new factual text.
- Once per person with a nonempty existing summary, serialize `{"research_summary": <the exact existing summary>}` in canonical JSON. The frozen summaries total 21,205 characters, with a maximum of 437, so no truncation or repeated long-summary-per-claim encoding is needed. An absent summary stays absent.
- Canonical serialization is UTF-8, sorted object keys, compact separators, no whitespace rewriting of field values. Exact duplicate input rows are embedded once in the same document-role Voyage 4 lite/1024 space. Derived rows retain exact field/claim/person attribution and are not evidence of new expertise.
- For each call/aspect query and evidence passage, R1 similarity is `.7 * evidence cosine + .2 * associated-claim-context cosine + .1 * summary cosine`. If the summary is absent, renormalize the available .7/.2 weights to sum to one. Cap the contextual uplift at evidence cosine + .05; reductions remain possible. No context vector alone admits a researcher or establishes an anchor.
- The accessible pool retains D1's evidence-based topic/method admission. R1 changes numerical utility/ranking within that pool, not confirmation labels. The scientific anchor still requires an admitted original-evidence core similarity >= .4. The exact evidence passage remains the explanation anchor. Summary statements cannot be substituted as proof that the anchored claim supplies a technique.

Isolate R0/R1 with identical D1 selection and admission settings before combining with selection changes. Reuse the 493 unchanged D1 source/evidence vectors. The only additional embedding work is the unique exact R1 claim-context and nonempty-summary rows; no search corpus changes or new researcher evidence.

## Call-person event and bounded local comparisons

Use the original D1 call-person item identities. Strong/plausible define a positive model-judge event; unrelated a negative. Insufficient-information and missing results are not binary labels. One source/person judgment is one training item regardless of its reporting occurrences. The event is usefulness for **at least one contribution to this call**, not aspect-level expertise, capability, human preference or proposal success.

Five declared features summarize unchanged evidence under R0 or R1: maximum aspect cosine; core cosine at that maximizing aspect/passage; lexical overlap at that same aspect/passage; maximum whole-call cosine; and mean of the best two aspect cosines (one aspect uses its single value). Select the maximizing passage by aspect cosine, then core cosine, then stable claim ID. Original evidence admission remains a separate constraint. No profile length/count, identity or department feature.

Compare one fixed quality rule `q=.7*maximum_aspect_cosine + .3*maximum_whole_call_cosine`, automatic thresholds {.45, .50}, with one ridge-logistic family, C in {.1, 1}, automatic score thresholds {.65, .80}. Learned scores refer to sampled machine judgments, not population probabilities. MMR stays off; Bayesian fitting is omitted unless a single same-feature local check is needed to resolve an actual numerical ambiguity, not as a completion gate.

Use five deterministic outer grouped folds (SHA256 of `D2-outer|<source-group>`, sorted then round-robin), retaining the existing duplicate/successor source groups. Standardize only on training data. Select C and the automatic operating rule using three grouped inner folds within each outer training partition. Never use an outer test label to select its transform, coefficients, C or threshold. Require both classes and at least five source groups for fitting; otherwise record inadequacy.

Within training-only operating selection, prefer the largest positive yield among rules with <=5% observed unrelated selected items, at least ten selected items and three selected source groups. If none meets that target, select the largest `true positives - 4*false positives`, then lower false-positive count, then the stricter threshold. This fallback is a recorded development tradeoff, not a relaxed product target. C ties use lower held-out log loss, then stronger regularization. Apply the same selection criterion to each family's training predictions. Report every declared setting and all nested out-of-fold outcomes, calibration bins, Brier/log loss, discrimination and source-group stability. Final refitting uses all usable development labels only after the family comparison; its in-sample predictions are not independent metrics.

The labels were sampled by D1's algorithms and diagnostics. Report source/label counts, sampling bias, uncovered people and uncertainty; these estimates cannot calibrate the full directory or human preferences. Select the simpler family unless the learned family improves outer-fold Brier by at least .02 and improves positive yield at an unrelated rate no worse than the fixed rule. No extra paid labels solely for fitting. New D2 judgments evaluate the frozen candidate and do not trigger another fit/tuning round.

## Selection and alternatives

First compare on fixed D1 matrices: original D1; fixed member quality replacing .45 group cutoff; and the single selected regularized member-quality rule if fitting is supported. Keep `F(T)=sum_i w_i max_j r_ij`, original evidence admission, group anchor, scoped evidence nonredundancy, .95 coverage envelope, bounded existing optimizer, two-to-four slots, and smallest adequate group. Every automatic member must pass its own absolute quality condition. A uniformly poor pool fails even if it is the best available for a call. Do not restore a positive universal removal-marginal gate.

Within the .95 coverage envelope for each size, sort by weakest member quality, mean member quality, coverage, then stable member IDs. The smallest adequate size remains the default. Alternatives must also retain at least rho times the best weakest-member quality for their size, with the only tested rho values {.90, .95}. This is membership quality, not MMR or disciplinary diversification. Prefer .90 unless .95 demonstrably removes judged unrelated members without reducing judged reasonable member yield by more than 10%; report unknown labels rather than treating them as good. Return at most eight without padding; keep excluded automatic candidates accessible through the existing individual/manual paths.

Run the fixed-matrix comparison, then the isolated R1/D1-selection comparison, then one combined candidate. Preserve unsuccessful settings. If quality remains inadequate, report the strongest demonstrated bottleneck; do not launch another threshold or representation campaign.

## Finite missing judgments and accounting

Starting verified ledger: cloud owner 34497056168, $2.138302 charged, zero reservations, 310 lifetime requests. Existing consumed bounds: legacy judging 136 requests / 1,226,561 input / 69,632 output; D1 judging 162 / 1,347,679 / 82,944; embeddings 12 / 94,941 input. These remain consumed. The same lifetime 690-request maximum and $10/$6 dollar ceilings remain in force.

Before dispatch, materialize the exact context rows and missing-item packets, their hashes, request/input/output bounds and conservative costs. D2's maximum proposed portion of the already authorized balance is **$3.3844**: at most $0.02 for new compatible context vectors and at most $3.3644 for judging (120 requests, 1,100,000 conservative input units, 61,440 maximum output tokens). These are maximums, not quotas. They yield worst-case cumulative Stage 2 spend <=$5.522702 and preserve >=$4.477298. Keep actual context requests within five; lifetime requests would then be <=435, leaving 255 for later stages. The narrowed trusted helper must explicitly account for the separate finite D2 purpose inventory without resetting historical consumption, charges, keys or the lifetime cap. No further expansion is authorized by this protocol.

Reuse exact D1 call-person/group items first. Buy only missing top-five items on the proposed ranking, primary groups and their members, plus a bounded alternative sample. Select alternative ranks 2 and 8 where present and one interior rank by stable source/option hash; preserve all-rank unjudged denominators. Include matched-size A comparisons where distinct and one predeclared order swap on at most four source-hash-selected pairs. Audit up to six actual explanations selected by source hash. Primary/member coverage has priority over extra preference requests. If complete required evidence exceeds the finite envelope, preserve it unsubmitted and report the missing denominator rather than crop or repeat until favorable. New grade packets use the existing D1F rubric; no new source-suitability or human requests.

## Completion and next boundary

Report A, exact D1 and D2 over all 35 prepared scopes and all 90+30 reservations, separately for the 26 action-admitted and nine action-blocked scopes at the fixed clock. Report lists, yield, matched-size abstentions, primary/alternative quality by rank, distinct items and repeated occurrences, missing/insufficient labels, explanation failures and paired source-cluster uncertainty. Use 5,000 bootstrap draws, seed 20260910. Independent feasibility remains unknown unless separately evidenced; never define it from algorithm output.

The original 80% relevance, 5% unrelated-member, 80% usefulness and 85% feasible-scope yield targets remain visible. Freeze exact tested candidate/data/scorer/representation/judge identities and produce `reports/stage-2-ranking-revision-d2-report.md`. No new human packet; the old 20 requested items and 20 final reserve remain unchanged. No full E2E/Playwright/accessibility, physical-browser claims, holdout results, Stage 3 or deployment. **STOP FOR USER REVIEW.**
