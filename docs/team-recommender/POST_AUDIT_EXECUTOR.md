# Approved post-audit semantic check

The user approved one check on September 11, 2026: at most $2 / 60 attempts within `on-demand-team-offline-v2-20260909`, never a new allowance. No deployment, embedding generation, model change, profile collection or runtime activation is authorized here.

Only 34 exact provider bodies, containing 69 unique questions from 26 scopes / 24 source groups, are accepted. The reviewed immutable input file hash is `5c97d02a88834328aaf00ebe4d1cf6b3a1ab6246928205351d797e9a8d40c612`. Corrected retained registry generation is `60169651eaff43c75e0eccd12167371d8131b76ed67592eaed18d3689d188126`. These bounded public scientific records are data, not executable instructions. Source excerpts preserve their older provenance and limitations; no new full-notice verification is claimed. Algorithm scores, identities and old judgments are absent; explanations are separate questions. Three new children share one parent group; six independent children were unavailable.

The exact dispatcher packet is `packets/489e966e57a1101aedba61b93c13864ad825187913993f5c43c2d05fa32632b5.json`. It accepts only existing body hashes, not user-supplied bodies, code, URLs, models or prompts. The existing complete-registry projection reproduces each reviewed body and the unchanged D1 rubric. The existing Sonnet 5 route remains $2/M input / $10/M output (official pricing checked September 11, 2026); conservative input reservation is $2.50/M. Bounds: 447,809 input tokens, 17,408 output tokens, $1.293612; largest complete request 22,878 of 24,000 conservative input bytes. No evidence is cropped. There is no retry allocation or alternate batching route.

The post-audit family remains Stage 2 corrective accounting and shares all historical requests/charges with the $10 / 690-request ledger and $6 cumulative cap. Its additional envelope is at most $2 / 60 attempts, 1,440,000 input / 30,720 output tokens, with a stricter exact 34-body purpose inventory. Historical starting usage was $3.952987 / 595 requests / zero outstanding; verify the actual cloud ledger before dispatch, preserving later usage. Never copy an allowance into a separate local executor. Workflow concurrency, protected-main code, pre-dispatch checkpoint artifact, irreversible logical claims and exact successful-cache reuse are unchanged.

After protected exact-head review/CI and merge, manually dispatch the existing `team-recommender-offline.yml` on main with this packet's committed SHA and hash `489e966e57a1101aedba61b93c13864ad825187913993f5c43c2d05fa32632b5`. Restore cloud state before dispatch; retain every reservation/result/terminal receipt. A paid or uncertain item with missing results stays unresolved and cannot be resent. Fetch private output artifacts for analysis; do not publish provider responses or caches.

Trigger audit: branch pushes have no production trigger; PR opens run the existing Python/Node checks and configured independent review. Protected-main merge starts the existing immutable-release planner. This patch changes only validation/executor dependencies, not source/team/semantic generation, runtime, worker, routing, schedules or permissions. With the currently verified unchanged publication, the existing planner selects validation of retained bytes; publication jobs require a generate/teams/backfill/reuse/publish plan, so validation does not publish or generate. Verify that exact disposition after merge. The manual preparation workflow remains the only metered action caused by this check.

Validation: focused accounting/executor contracts, all 34 exact body bounds, tamper/mixed-registry rejection, complete projection reproduction, exact cache reuse, aggregate budget caps, and crash injections before/after reservation, reconciliation, cache, terminal, receipt and checkpoint writes. Repeated restoration must dispatch at most once. Required exact-head Python/Node CI remains mandatory. No E2E is needed for this helper; the separately authorized experimental browser validation remains separate.

September 11 review correction: the legacy generic packet format retains only its original six purposes. Versioned D1/D2/D3/Stage 3/post-audit purposes require their dedicated contracts; adding a purpose budget cannot grant generic-packet authority. In particular, post-audit always requires the existing exact approved body hash. Regression tests reject every versioned purpose and a future-envelope sentinel before ledger mutation or fixture-provider dispatch. No packet, cap, lifecycle, cache identity or paid result is changed; this repair causes no paid request.
