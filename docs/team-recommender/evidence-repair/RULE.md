# Frozen shared evidence repair rule

Frozen before the repaired full-inventory outputs are inspected. This is one corrective rule, not a model/threshold search. No provider call or human item is authorized.

Baseline: adedd242dc727d85bc2db42776abce5132c882b8. Fixed diagnostic clock: 2026-09-11T12:00:00Z. Preserve all 26 semantic, 150 rollout, 50 rollout and 106 bound legacy identities and the one historical unmapped legacy identity. Registry generation remains 60169651eaff43c75e0eccd12167371d8131b76ed67592eaed18d3689d188126 (158 records, 155 automatically eligible by actual eligibility fields).

Scientific evidence is shared through FUNDING_RETRIEVAL.sourceEvidence. Existing source-field projection, bounded passage windows, field-scope matching, prefix relationship, protected compound/acronym/separation policies and query expansion are extracted or reused by Search and reverse Team Match. Generated facets remain discovery/ranking signals. The requested canonical parent/child and currentness are unchanged.

A supported connection requires an affirmative single source sentence and either all substantive groups of the profile clause within the existing 11-token field span, or a complete registered method/target concept. Registered concept identity must also be present in the source's existing query expansion; its existing evidence policy must pass in that same sentence. A lone literal term/property/sponsor qualifier cannot establish automatic support. Claim labels and evidence are associated with their original stable claim; audited summary sentences participate with existing provenance. No new source/profile text is written. Source offsets are offsets in their named field, and token positions are normalized token positions, not verbatim character spans.

Only a source title or opening description/child-summary sentence can anchor. Later matched clauses may support a member but cannot invent a central purpose. This limited syntactic proxy is not a semantic proof of purpose. Unrecognized terminology, long compound relationships and source ambiguity can remain unresolved; no call/person exceptions are permitted.

Existing scoreProfile returns discovery score components, supportedConnections, automaticEligible and strong separately. Discovery may expose tentative leads; matchTeam retains its intersection of per-profile discovery fit. Automatic construction additionally requires each member's shared automaticEligible and one actual anchor. Recency never supplies expertise. Every new explanation remains unconfirmed; it quotes the matched profile claim/summary and named source clause, without claiming a mandatory sponsor role or capability certificate.

Coverage takes max support per source-owned relationship: registered concepts saturate across clauses, otherwise one complete bounded source clause is one unit. Support values are 3 for anchor clauses, 2.5 for other affirmative clauses; no probability interpretation. Duplicate label/evidence representations cannot add units. Every automatically retained member has its own support. Independently supported overlapping contributors may form the minimum two-person group, but exact duplicated profile evidence does not justify padding. Beyond two, additions require positive gain; final membership is rechecked. All eligible-directory seeds, sizes 2–4, near-best 95% envelope, smallest group first, then coverage/score/stable-ID ties, at most eight options, no MMR or learned scoring. Discovery candidates remain manually reachable.

Caches remain bounded: eight requested-scope/source contexts, 32 exclusion states per scope; profile/query computation reuses identical inputs. Snapshot adoption hashes the complete original canonical JSON using bounded UTF-8 encoding, then freezes reused objects. No fields are dropped. No source/provider work or all-scope team calculation occurs at startup.

The single after-run reports all 38 known pair grades alongside retained/removed/new discovery and automatic membership, unique pairs versus occurrences, source inventory boundaries, and new unjudged outputs. Original positive/negative labels are reused only for unchanged source/profile questions, never for changed groups or explanations. Historical exposure is diagnostic, not untouched validation; no full recall claim. Original 80% top-five, 5% unrelated-member, 80% useful-group, 85% independently feasible-yield targets remain visible and unwaived.

Engineering verification: focused contracts, required Python/Node checks, exact coherent packages, one configured full E2E/accessibility run after integration freeze, affected reruns only for recorded defects, real parent/child memory/timing under the previous resident-catalog baseline, zero-provider browser boundary, offline rollback. No physical-device claim. Extra heap ceiling stays 48 MiB, cold <5 s, warm <200 ms, post-input <1 s. Allocation/step sampling limitations are reported, and GC is never used to erase a failing transient observation.

Stop after one report, commit and required exact-head PR225 review/CI. No merge of runtime/registry/activation, deployment, Stage4, further automatic correction, new human packet or paid confirmation.
