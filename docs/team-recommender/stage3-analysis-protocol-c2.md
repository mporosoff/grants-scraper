# Frozen proposed Stage 3 analysis — approval required

This is a predeclared analysis protocol, not Stage 3 execution authority. Stage 2 ends with **REVISE**, not a quality-gate pass. The automated development run and bounded human packet are complete; the unreturned packet does not make that automated run incomplete. The current evidence does not support opening the holdout or deployment.

## Exact comparison and seal

Use only the candidate/dependencies in `stage2-final-candidate.json`: fixed coverage-v2.2 / ingredients-v2.2, bounded-middle parameters, MMR off, no fitted model, no production fallback. Whole-call A remains an analysis comparator with its own fixed .30 whole-call / .25 core admission, identical 155-person snapshot and matched sizes. Do not treat the retained candidate as a demonstrated development winner.

Preserve the original development and holdout manifests and apply only the pre-scoring source identity overlay `manifests/source-group-amendment-c2-f92fc5995a0a1ac0.json`. It records all 14 holdout replacements and the deterministic source-family/hash replacement rule. Development is 90 scientific reservations in 87 corrected groups; holdout is 90 scientific reservations in 90 groups, plus its original 30 derived controls. The two remapped final-human reservations are in `manifests/human-final-source-overlay-c2.json`. These are source metadata only. No held-out recommendations or final audit answers have been generated or inspected.

Before separately authorized holdout work, resolve or explicitly accept the Stage 2 source/profile evidence and judge reliability limitations. Preserve development history. If preparation or recommender semantics change, version the candidate and test on development before any final evaluation. Never use final human judgments or held-out scores to choose parameters, new aspects, the candidate, the rubric, or replacements. Do not call retuned results independent.

## Analysis rule

Account for every one of the 90 scientific reservations and 30 controls. Report prepared/unprepared, actionable/ineligible, empty/short top-five lists, returned/no-group outcomes, and all missing/insufficient labels. Keep source-only feasibility separate from algorithm yield. Establish feasibility using call and directory evidence independently of whether a team was returned; leave unclear cases unknown.

Compute macro reasonable precision over available top-five results, pooled counts, source-group bootstrap intervals (2,000 replicates, seed 20260910), and explicit bounds for missing labels. Strong plus plausible is the reasonable event; insufficient information remains its own category and is not positive expertise or demonstrated incompetence. Report automatic-member unrelated rates separately for primary groups and all option occurrences, including unique-person/source denominators. Repeated options are not independent trials. Compare A/B primary coherence and complementarity at matched sizes with balanced reproducible ordering. Ties, insufficient information, missing outputs and order conflicts stay unresolved. Retain individual, complete-group, alternative and separate explanation outcomes.

Keep the existing targets: mean reasonable top-five precision at least 80%; unrelated automatic-member rate at most 5%; useful primary groups at least 80% and yield at least 85% of independently feasible scopes; complementary coverage benefit over matched-size A without unrelated-person harm. Mathematical F(T) improvement alone is not semantic evidence. A small or unknown feasible denominator cannot establish these targets. No fabricated identity, ownership, exclusion, source-span, privacy or paid-click failure is acceptable. Unrun gates are not passes.

Use the same fixed E1 rubric and primary Sonnet 5 judge, with the reviewed compact-output route, at most five clearly separable items per request and 512 output tokens. Provide original bounded call and **actually source-verifiable original profile passages**; the Stage 2 retained-claim snippets are a disclosed limitation and must not be silently relabeled. Hide algorithms, scores, expected winners and prior decisions; audit explanations separately. Freeze item selection and balanced order before responses. Preselect up to ten order-swap pairs by source/item hash; conflicts remain unresolved. Identical comparisons share exact cached judgments. Never replay a possibly dispatched logical paid request to recover a lost/invalid result.

No additional model family, tournament, repeated prompt-tuning campaign or paid labeling campaign for learned scorers is proposed. The six development fitted models are retained research artifacts only. No learned scorer enters the frozen runtime.

Marc has 20 final-audit items reserved, after 20 development items requested and zero returned at Stage 2 closeout. Present evidence before model decisions, one compact assessment per item, and allow unable to assess. Count all requests/skips; do not replace skips. Final audit selection uses the sealed source-only reservation rule, never favorable model scores. Disclose any actual prior exposure. Additional human requests need explicit authority.

## Finite dollars and execution prerequisite

The one cloud-owned ledger is `on-demand-team-offline-v2-20260909`, restored from final Stage 2 run 34472949357, actual $1.082518, zero outstanding reservations, $8.917482 left under the overall $10 ceiling. The local ledger is a read-only mirror. Historical spend gives no credit.

Propose **at most $4.00 of that existing balance** for Stages 3–4 combined: $0.10 for missing/changed compatible embeddings and $3.90 for the fixed judge including all audits and uncertain dispatch reservations. Stage 4 has zero planned metered work. At the checked prices ($.02/M embedding input; $2/M judge input and $10/M output; reserve $2.50/M input), an example upper envelope is 5M embedding tokens and 250 judge requests totaling at most 1,048,000 conservatively bounded input tokens and 128,000 output tokens. These are ceilings, not quotas. Exact packets determine what fits; no decisive evidence cropping or silent denominator reduction. Maximum cumulative spend under this proposal is $5.082518, not a new allowance.

The current trusted executor policy is **Stage 2 only**. Stage 3 would require separate user approval plus a narrowly reviewed policy/packet-allowlist change on protected main using the same durable ledger, not a new executor or allowance. Do not dispatch Stage 3 using a Stage 2 purpose label. Confirm current prices before paid work and retain no-dispatch proofs, conservative reservations and crash recovery.

## Browser validation and decision

Only after separate Stage 3 approval, run the dedicated full browser/E2E/Playwright/accessibility task, actual physical-browser resource checks and the unchanged-presentation/currentness/zero-paid-click gates on the exact candidate. Keep ordinary Funding Finder and separate all-member Team Match unchanged. The current Node/DOM evidence does not substitute for those checks.

Decision categories: READY only when the stated technical and quality evidence supports advancement; READY SUBJECT TO MARC AUDIT only when the human audit is the sole remaining condition; REVISE for correctable quality/input/protocol failures; DEFER for an unavailable safe execution dependency. A failed gate remains failed even if spending is low. No deployment, activation manifest, rollout cohort or Stage 4 follows automatically.

**STOP FOR USER REVIEW.**
