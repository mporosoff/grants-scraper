# Stage 3 proposal after D3 — conditional and not authorized

D3 completed its bounded automated comparison with **REVISE / no demonstrated winner**. No Stage 3 candidate is nominated. The exact tested arms and historical unqualified D2 fallback are frozen in `stage2-embedding-d3-candidate.json`; this document neither selects a new winner nor permits tuning against sealed outcomes.

The next action is user review of the D3 report and the existing Marc development packet. The source/quality limitations require an explicit decision before confirmation. Missing human responses do not make automated D3 incomplete. No replacement development items are authorized, and passing the old packet could not by itself qualify the different D3 groups.

If separately approved with an explicitly accepted candidate, keep the original grouped 90 scientific + 30 control holdout, source-group overlay and source-only reserve rules unchanged. Prepare compatible inputs without recommendation scoring; record every prepared/unprepared/current/closed/unknown disposition. Freeze actual packet allocation, evidence projection (including every explanation quotation), scorer and source/vector identities before any holdout output. Do not tune afterwards. Evaluate all eligible prepared holdout scopes over the full directory; compare matched-size whole-call ranking, primary/member and alternative usefulness, scientific transfer, unrelated outcomes and separate explanation faithfulness. Deduplicate exact items but retain occurrences, insufficient-information cases, no-group outcomes and missing denominators. Use the one fixed D1F rubric with balanced ordering and a bounded predeclared swap audit; conflicts stay unresolved. Any projection correction is versioned, not silently made equivalent to older cached questions.

Retain 80% relevance, <=5% unrelated automatic members, 80% useful groups and 85% yield only on an independently evidenced feasible-scope denominator. Unknown feasibility stays unknown. Report grouped uncertainty and machine versus returned human judgments. A failed, incomplete or inconclusive gate cannot authorize Stage 4; no favorable-subset redefinition or replacement based on outputs. Reserve at most the remaining 20 final human items, selected before viewing judge scores; count skips and permit inability to assess. Do not retune using that audit and call it independent.

Run full configured E2E/Playwright/accessibility only under separate Stage 3 authority, along with currentness between actions, source/claim retirement, child ownership, coherent snapshot/stale-response rejection, search lazy-loading, cached/manual paths and zero paid click/edit/error traffic. Targets remain p95 cached group/edit <200 ms, post-input calculation <1 second, cold click-to-interactive <=5 seconds at 20 Mbit/s and 150 ms latency, <=8 MiB ingredients and <=48 MiB extra browser heap. Node D3 observations do not pass these browser gates.

Retain source-selected 50/150 routing preparation and the goal of 150 current scopes across >=80 parents, >=50 eligible children and six research families where supported. D3 established only 8/50 and 18/150 existing ingredient readiness, with 35 development scopes. Report source gaps rather than manufacture scopes; a changed inventory target requires user review. Do not publish or compute a production team inventory.

Budget proposal, not permission or a quota: the single ledger has $6.864667 remaining and 215 remaining lifetime request slots. A provisional Stage 3 maximum is 190 requests: <=170 judgments, <=900,000 conservatively reserved input units, <=87,040 output tokens, $3.120400 at the existing conservative $2.50/M input and $10/M output reservation; <=20 missing-embedding requests with <=$0.10 reserved. Combined maximum $3.220400 would leave >=$3.644267 and >=25 request slots for Stage 4. Stage 4 should reuse assets and need zero paid user interactions; its unused reserve is not a spending target. All failures/uncertainty count. Restore the same authoritative cloud checkpoint before any separately approved dispatch, with no local spending fork or reset. Exact prices and complete packet sizes must be checked again; this envelope is not evidence that all unknown holdout/rollout missing work fits. If it does not fit, report the denominator and stop without asking for more money or quietly shrinking the corpus. The trusted policy currently authorizes Stage 2 purposes only; Stage 3 route/configuration requires its separately reviewed authorization, not an unapproved main change now.

Deliver a complete Stage 3 report and STOP before publication only if that stage is later approved. At this D3 boundary, **STOP FOR USER REVIEW. Stage 3 has not begun.**
