# Post-audit corrective revalidation

The attached `AUTHORITY.md` governs this continuation of `272124fc7d981cf3f438de7e9d133ca00600bee8`. The 158-person audit, registry generation `60169651eaff43c75e0eccd12167371d8131b76ed67592eaed18d3689d188126`, four source gaps and administrative eligibility are unchanged. This is one corrective implementation and validation, not a new model-selection stage or deployment.

## Frozen scientific rules

`shared-team-v2` uses the existing shared matcher, full catalog vocabulary/frequency context, exact canonical parent/child projections and unchanged per-person fit. The only matcher optimization skips proximity work when its existing scoring branch cannot use the result. Exact old/new score and fit equality is checked separately.

Weighted maximum coverage remains a preference. A retained member must add positive coverage **or** independently pass the matcher's existing strong fit and have a distinct, actually matched source-backed claim. Claim fingerprints use shared tokenization of matching labels/evidence, excluding names, IDs, affiliations and URLs; identical evidence does not justify duplicate membership. Two independently relevant people can therefore form a minimum group even when one dominates every weighted dimension. Beyond the two-person minimum, additions must increase coverage. At equal coverage the smaller group wins. No new relevance threshold, discipline reward, MMR or learned scorer is introduced. Every proposed/added person must pass shared fit; all admitted candidates remain reachable. This rule is evidence-based redundancy control, not proof that two admitted people necessarily form a useful team.

Whole immutable inputs are validated/frozen once on adoption. Actions check source object identities, generation and currentness, with one clock spanning validation, mutation and rendering. New source objects invalidate the complete pool, including unselected people. Cache limits remain eight scopes and 32 exclusion variants per scope; no startup score matrix or saved test-team dependency exists.

## Numerical diagnosis and source selection

All originally bound scopes and both original 50/150 rollout inventories are counted at `2026-09-11T12:00:00Z`. Canonical binding, currentness, broad/unselected status, no candidates, singleton candidates, absent anchor, redundancy restriction and produced group remain separate. Old Stage 3 observations are regression history, never new confirmation.

`semantic-selection-v2.json` fixes 24 previously unexposed source groups / 26 scopes. Existing source grouping and all development/holdout reservations plus historical/legacy exposure groups are conservatively excluded. Source-family round robin and stable hash order select specific parents; no recommendation scores or labels select them. Three eligible native child scopes from one new parent exist; six independent child scopes do not. Their three observations share one source-group denominator. The initial source-only selection is preserved as v1: before any new outputs or labels, v2 repaired an inappropriate inherited parent-synopsis filter that discarded these existing child projections.

No source replacement follows an output or grade. Unknown successors/near-duplicates remain a disclosed independence limitation. Existing administrative/nonresearch-looking programs may remain in the source-screened set; suitability is a separate judgment, not silently repaired by the recommender.

## Single semantic check

Use the existing `claude-sonnet-5` judge and D1 exploratory rubric, with complete unchanged corrected registry summaries and all distinct active claim evidence for only the people judged. Call evidence contains original stored official scientific text, applicable conditions and the selected native child projection with its provenance; generated summaries are not falsely called verbatim quotations. No algorithm identity, relevance score, previous grade or persuasive explanation accompanies relevance questions. Explanation questions are separate and contain the exact cited profile evidence.

On every selected scope, inventory all admitted people and rank the available top five by shared scientific score/stable ID. Judge every available top-five person and every primary member (deduplicating identical questions), plus each actual primary group. Also request source suitability, keeping unknown directory feasibility separate. After outputs exist but before labels, select up to four actual rank-two alternatives and four actual primary explanations in stable source-hash order; inspect every sampled alternative member. A two-item order-swap audit is drawn only from existing alternative comparisons, if any. Missing alternatives are not assigned fictitious judgments. Preserve exact item identity, cache reuse, per-scope occurrences, ties, insufficient information, missing responses and terminal paid failures.

Report source-group means and source-group bootstrap intervals using fixed seed 20260911, 2,000 replicates; with fewer than two labeled groups report an interval as unavailable. Also report pooled counts, pessimistic/optimistic missing-label bounds, action-current/all-bound slices and unconditional yield. Keep targets visible: 80% reasonable top-five relevance, at most 5% unrelated automatic members, 80% useful primary groups and 85% yield on independently feasible scopes. Unknown feasibility is UNMEASURED. A small check cannot precisely certify a 5% long-run rate, and numerical coverage is not scientific superiority. No tuning follows these grades.

No new human packet is requested. All 40 prior slots remain accounted for; returned and applicable answers are reported separately.

## Budget and execution boundary

The same cloud-owned ledger has $3.952987 charged / 595 requests / zero outstanding, with $6.047013 and 95 request slots remaining; the latest trusted run is still 34554567315. No local/cloud duplicate spending copy or new allowance is created. The plan calls its proposed $2 / 60-attempt semantic ceiling **not a new allowance** and requires an explicit execution handoff for that ceiling and any trusted-executor changes. Prepare complete bounded packets and the minimal exact-input prerequisite for review before requesting that specific authority; until supplied, semantic dispatch remains unapproved. Engineering and the plan's expressly requested full E2E validation continue without paid calls.

Current official Sonnet 5 pricing, checked September 11, 2026, is $2/M input and $10/M output; existing conservative accounting uses up to $2.50/M input. See [Anthropic pricing](https://platform.claude.com/docs/en/about-claude/pricing). Every retry, failure and uncertain attempt counts. No terminal logical request is paid again under a new batch or key. At the proposed ceiling, at least $4.047013 / 35 slots remain. Materialized packet bounds, not that ceiling, determine the proposed actual spend.

## Engineering and release boundary

Run affected and required Python/Node/browser contracts, frozen-query/scoring/no-drift checks, then one complete configured E2E/accessibility run plus actual corrected 50/150 routing tests and repeated real-browser resource observations. Preserve failures; rerun affected cases after bounded supported-path fixes. Keep unchanged presentation and separate hosted Search/Team Match traffic explicit. No tested candidate is called final while required review/gates remain pending.

Assemble coherent registry/runtime/adapter/routing packages with existing release dependency tooling. Publishing the registry alone invalidates legacy teams and is unsafe as a mixed transition. All 107 affected old proposals remain withheld; exact canonical mapping recovers the EERE self-parent record, but no native `332894:superconducting-qubits` child exists. Package entries do not imply useful groups or a qualified 150-scope cohort. Rollback must use a complete proven published release, not mixed old code/new profiles. No merge/publication/Stage 4 is authorized by this plan.

## Subsequent execution approval — September 11, 2026

The user approved the bounded check and narrow trusted prerequisite recorded in `execution-approval-v1.json`: at most $2 / 60 attempts in the existing ledger, exact corrected-profile/source packets up to 24,000 input bytes. The 34-request / 69-question proposal has a $1.293612 conservative bound. The earlier unapproved proposal remains historical. Independent exact-head review, required CI, protected-main-only execution and irreversible paid-request accounting remain required. No deployment is approved.
