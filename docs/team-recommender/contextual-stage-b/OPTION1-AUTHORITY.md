# Execute Option 1 — repair assessment delivery and finish the application connection

## Decision and authority

Continue the current contextual Stage B implementation in `mporosoff/grants-scraper`. Repair the existing response contract, source-reference handling, output sizing, and application-to-service path. This is an implementation continuation, not a new recommender experiment.

Keep the audited researcher records, current approved retrieval representation, effective scientific model, scope interpretation → adjudication → verification responsibilities, persistent assessment cache, and deterministic team composer. Do not switch to Option 2, move hosting platforms, change models, remove verification, retune scientific relevance, or begin another eight-scope campaign.

The goal is an actual user request producing a valid evidence-grounded assessment through the application, with compatible shared reuse and correct editing/extension behavior. A parsed response is not automatically a good recommendation. A valid scientific negative is not an execution error.

No public activation, recurring production spending, or Stage 4 release is authorized.

## 1. Resume the actual checkpoint

Repository: `mporosoff/grants-scraper`.

Product branch: `codex/on-demand-team-recommender`.

Existing worktree: `C:/Users/Marc Porosoff/projects/grants-scraper/.worktrees/on-demand-team-recommender`.

Reported local closeout: `3e93777`.

Read the COMPLETE local report at:
`docs/team-recommender/reports/contextual-stage-b-demand-implementation-report.md`

Also read its actual request/response diagnostics, deployment/access receipts, current state, effective contracts, applicable AGENTS instructions, and authoritative ledger. Resolve the full report commit, tested implementation commit, actual deployed validation version, and subsequent local work. Do not reset, duplicate the worktree, or substitute an old public branch because the local commit is not remotely resolvable.

The supplied checkpoint reports six execution/validation failures among eight scopes, one valid no-supported-group result, one child-selection requirement, a failed extension, no qualifying teams for the independent output check, and an unresolved application-to-service connection. Treat these as reported boundaries, not a diagnosis of their cause.

Preserve all historical responses and failures wherever retained, audited claims, source identities, scientific results, and paid-request history. Do not repeat the profile audit. The accepted owner review remains model-assisted feedback; no additional human packet is authorized.

If required response bodies were not retained, state that limitation. Do not claim to replay missing responses, fabricate them from a summary, or change the site's unrelated privacy/retention policy to obtain them.

## 2. One finite spending envelope

Latest reported balance: $4.504098 and 37 provider attempts remaining within the original $10 / 690-attempt authorization, `on-demand-team-offline-v2-20260909`.

This implies $5.495902 charged and 653 attempts used, but reconcile the authoritative ledger rather than treating this arithmetic as a fresh account query.

This continuation authorizes AT MOST **$1.50 and 16 additional provider attempts**, preserving at least **$3.004098 and 21 attempts** at the reported balance. This is a subset of the remaining existing authorization, not a new allowance or an additional $3. Count every model stage, embedding request, probe, repair re-execution, and output check.

If later usage leaves less capacity, reduce the task allowance to preserve those reserves. Do not reset counters or take money/request authority from old tasks. Reuse the current authoritative coordinator; no new independent ledger.

Do all available diagnosis, saved-response replay, fixture work, contract alignment, and application connectivity checks without provider calls first. Before real dispatch, size the WHOLE finite check, including complete outputs and the final small usefulness check. Limits are ceilings, not quotas. Do not repeat all eight workflows merely because money remains.

## 3. Identify the first failure boundary from actual evidence

Create one compact table covering the six failed scopes, the valid negative, the child-selection case, and the extension. Include:

- Scope and stage; exact source/profile and request identities.
- Effective prompt, provider schema, transport/parser, and application-validator versions.
- HTTP/API status, finish/stop reason, actual input/output usage, and whether the body/stream is complete.
- Exact failing field or source reference, expected versus returned type/count/length, and the relevant bounded excerpt where safe.
- The layer that failed: input projection, provider output, transport extraction, schema validation, source-reference resolution, semantic verification, cache/state transition, or application delivery.
- Confirmed cause, remaining uncertainty, proposed general repair, and whether a saved-response replay can test it without a provider.

Inspect successful and negative responses too. Do not infer researcher suitability from a schema or transport failure.

In particular, distinguish provider `max_tokens` truncation from a complete response rejected by a local field-length/array bound; distinguish an absent/fabricated quote from a mismatch between the exact source representation supplied to the model and the one checked later.

Compare the effective legacy contract with the current request construction to find accidental differences. Reuse existing functions/contracts where practical, but remain on Option 1. If a broader hosting or engine replacement is actually required, report the concrete prerequisite rather than silently executing Option 2.

The table is an implementation note, not a stopping phase. Continue into the justified repairs.

## 4. Repair the output contract as one coherent interface

Make the effective server-owned prompt, provider-supported structured-output schema, parser, application schema, and canonical input projection agree. Use the provider's actual supported API configuration; verify it reaches EVERY relevant stage rather than existing only in a test fixture or one request path.

Parse the correct response blocks. Do not concatenate thinking, tool, or status blocks into JSON. For streaming, retain the actual terminal status and complete validated message before adopting a result. Never publish incomplete JSON or partially verified edges as a successful graph.

Keep the schema compact and stable across ordinary scopes. Do not create a distinct giant grammar containing every current researcher ID merely to obtain ID validation; bounded identifier fields plus exact server-side membership checks are acceptable. Apply supported provider constraints and keep necessary unsupported size constraints in deterministic application validation.

Prefer model outputs containing scientific decisions and stable references, not regenerated copies of data the server already owns:

- Source passages receive deterministic IDs bound to exact source text, scope ownership, and fingerprint.
- Researcher/claim IDs and revisions identify existing audited evidence.
- The model returns contribution descriptions, source references, claim references, support category, and a concise substantive rationale.
- The server resolves quotes, attribution URLs, revisions, and display metadata from the exact approved snapshot.

Adopt this reference-first format where the diagnosis justifies it, with a minimal versioned adapter. Do not build a new source extraction/annotation platform. Preserve full relevant surrounding source context and governing conditions in the model input. References must point to evidence the model actually received, not to undisclosed source material.

A reference resolving successfully proves existence/ownership, not scientific support. The independent verifier must still evaluate applicability. It may downgrade or reject an edge. Do not replace semantic verification with an ID-membership test.

Where exact quotations are retained, compare against the exact representation presented to the model with a documented, reversible normalization map. Do not use fuzzy matching, expand quotes after the fact to rescue an unsupported answer, or combine unrelated child passages.

The scientific standard is unchanged: a plausible contribution or credible transfer is allowed; unsupported capability claims, adjacent-only matches, and missing information remain distinct. No new requirement that the researcher previously performed the identical project.

## 5. Make output sizing realistic before another paid request

Use actual failed-response shapes and the current complete candidate packets to size each stage. Count source interpretation, maximum emitted relationships, necessary explanations, identifiers, and any enabled reasoning budget. A fixed small output allowance must not contradict what the prompt asks the model to produce.

First remove redundant output: repeated names, URLs, source text, profile descriptions, and metadata already recoverable by reference. Keep concise reasons sufficient to explain the scientific connection and limitation. Do not cut the evidence needed for a decision or arbitrarily omit retrieved scientific contributions.

Retain the pinned model and substantive reasoning settings initially. Do not disable reasoning or verification merely to make a latency or format test pass. Change a setting only if the actual failure demonstrates a contract/budget mismatch, and record the exact change.

Use a justified finite output ceiling that fits the remaining monetary envelope. Validate at-boundary and over-boundary cases with zero-provider fixtures. If complete substantive work cannot fit, return a truthful bounded-execution limitation rather than a truncated scientific negative.

No automatic “fix your JSON” model loop. No silently accepted partial array. No lowering evidence requirements to make the run green.

## 6. Reuse paid results safely; permit only documented repair re-execution

Run retained raw responses through the repaired parser/validator locally first. Keep the original request and historical failure immutable. A recovered interpretation is a new attributable artifact, not an erased failure or refunded charge.

A saved response may be adopted for the current graph only when exact input identity, contract compatibility, completion, and every required scientific-verification stage can be established. Otherwise it remains diagnostic evidence only. Never promote an adjudication answer to verified status because it parses now.

This instruction explicitly authorizes at most ONE recorded re-execution of a failed logical stage after a demonstrated request/output-contract repair, within the 16-attempt/$1.50 task ceiling. Link the new execution to the original paid request, the approved repair, and the actual changed contract or limit. Preserve the original terminal state and both costs.

A different key, display label, batch layout, or arbitrary version increment alone is NOT authorization to repeat the same request. An uncertain dispatch with no confirmed response remains recovery-required; do not duplicate it just because the browser timed out. Unfavorable complete scientific answers are not repair retries.

Preserve shared-job deduplication, one authoritative reservation owner, stage checkpoints, and cross-user reuse. Do not resurrect every historical failed item. Select only the requests needed for the bounded check below.

## 7. Finish the actual application-to-service path before broad paid validation

Trace the intended validation browser origin → gateway → authorization → canonical release resolver → job coordinator → status/result → panel adapter.

Use the actual protected validation application build and its audited snapshot. Identify the first failing boundary from browser/server evidence. Check endpoint/base path, preflight/CORS where relevant, Access/authentication, method/headers, release identity, status polling, and result parsing. Do not assume that a working CLI/admin request proves the browser path works.

Fix the smallest actual defect. Do not put provider keys, reusable administrative bearer tokens, or service credentials into public JavaScript. Do not disable authentication or allow every origin to make the check pass. Missing user-mediated authorization is a specific prerequisite, not permission to bypass the access boundary.

Perform provider-free health/status, rejection, and explicitly labeled fixture-result checks through the real route first. Fixtures validate routing only; they are never reported as real scientific graphs and never published as team evidence.

Only an explicit assessment request can initiate paid work. Status polling, page reload, prefetch, cache hits, retries of status reads, and routine panel edits cannot. Closing/changing the panel must prevent stale display without silently purchasing a new workflow.

No live public serving. A narrowly reviewed backend prerequisite may be merged/deployed with public new-paid-work and public team activation disabled, using the existing protected validation mechanism. No new service platform, permissions expansion, billing upgrade, or registry-only production release.

## 8. Prove a small end-to-end path, then stop

Before new outputs, select TWO substantive scopes from the already declared eight: one failed coherent case that exercises the repaired boundary and a second with a different observed failure mode. Select one canonical researcher outside the first scope's assessed shortlist for the explicit extension. Record the choices without prescribing a desired team or replacing cases after unfavorable results.

Use the previously valid child-selection/no-group records as negative controls when exact compatible reuse is legitimate. Otherwise exercise those state transitions with clearly labeled fixtures and disclose that they are not fresh scientific outcomes.

Execute the first substantive cold request from the actual application. Two simultaneous browser sessions should join the same job. The result must reach and render in the panel, with stage provenance and costs. A compatible later visitor must reuse the persisted assessment without new model work.

If the same integration/format defect remains, stop further paid dispatch, reproduce it offline, and use only the narrowly authorized correction/re-execution if warranted. Do not continue into the second scope just to collect more instances of the same broken path.

When the first path operates, continue to the second scope and the explicit extension within the planned envelope. A valid extension may assess the new researcher as supported, adjacent, or unsupported; success means an honest completed assessment and correct UI behavior, not forcing admission.

For a positive graph, demonstrate at least one actual automatically assembled group, available alternatives when present, removals, option switching, and assessed-member add/re-add with zero new provider requests. Do not substitute an old stored team or handcrafted graph for this evidence.

A valid no-supported-group result is an acceptable scientific result for a scope. If neither substantive scope produces a usable positive graph, report the limitation; do not shop for easier replacements or claim the positive team path has been validated.

Only after reliable actual outputs exist, use at most TWO provider requests for one bounded independent output check, included in the same 16/$1.50 allowance. Supply complete audited evidence; separate individual applicability, group usefulness, and explanation faithfulness. Disclose any same-family correlation and prior case exposure. No new human packet and no new broad judge campaign.

Maintain a failure denominator separating scientific negatives, incomplete evidence, parser/transport failures, and application delivery failures. Two cases establish integration evidence, not a universal accuracy estimate or the original 5% unrelated-member target.

## 9. Measure latency and complete affected engineering verification

Instrument monotonic per-stage durations: application acknowledgement, source/cache resolution, queue wait, scope interpretation, query embedding/retrieval, adjudication, verification, validation/persistence, result delivery, and local composition/rendering.

Record input/output tokens, reasoning configuration, stop reasons, cache type, and observation count. Distinguish complete successful cold workflows, early negatives, partial-stage reuse, compatible full-cache responses, extensions, and local edits. Failed/early-stopped requests do not estimate successful end-to-end latency. Provider prompt/grammar cache is not the application's completed-assessment cache.

Report first response/status separately from the first usable verified team. Do not expose unverified relationships as final teams merely to report a faster time. Streaming can show truthful progress, not premature scientific acceptance. No one-second cold-inference promise.

Run focused response/schema/reference/state-machine/access/concurrency tests during implementation. At the final combined code/data/backend version, run the affected real-browser workflow checks and the configured browser/E2E/accessibility suite, with focused reruns for corrections. Preserve unrelated unresolved failures; do not expand into an unrelated redesign.

Report actual memory under the existing measurement policy. Latency tolerance does not silently waive the memory budget. Keep the existing interface and minimal status indicators; scientific logic and reliable delivery are the priority.

## 10. Deliver one completion report and a release decision boundary

Write:
`docs/team-recommender/reports/contextual-stage-b-option1-integration-repair-report.md`

Preserve the original report. Update the existing state/handoff; do not create another competing planning framework.

Include:

1. Actual resumed, tested, reviewed, and deployed-validation identities.
2. The complete original-failure table and root causes, including what could not be replayed.
3. Exact response/reference/output-limit changes and why they preserve scientific meaning.
4. Replayed/recovered artifacts versus new paid executions, with linked original requests.
5. Application-to-service repair and evidence from the actual protected browser path.
6. Results for the two scopes, concurrent requests, persistent reuse, extension, ordinary edits, and negative controls.
7. Real assessment examples and any substantive scientific failures, separate from format success.
8. Measured cold-stage/cache/extension/edit latency and actual cost per completed workflow, with small-sample limitations.
9. Full ledger reconciliation and preserved balance/request reserve.
10. Applicable browser/resource results, any unresolved checks, and coherent future release/rollback requirements.

End with one disposition:

- INTEGRATION REPAIRED; POSITIVE TEAM PATH DEMONSTRATED; READY FOR A SEPARATE BOUNDED RELEASE/QUALITY DECISION.
- INTEGRATION REPAIRED; SCIENTIFIC USEFULNESS OR POSITIVE TEAM PATH STILL INSUFFICIENT/UNMEASURED.
- BLOCKED BY A SPECIFIC CONTRACT, ACCESS, BUDGET, OR RESOURCE FAILURE.

A successful parser is not a scientific pass. A valid negative is not a broken request. No disposition authorizes deployment or another automatic campaign.

Continue through the authorized implementation and bounded check rather than stopping after a helper merge. Respect real security/authority barriers and required exact-head review. If Option 1 cannot be completed without a new hosting architecture, identify that prerequisite and stop—do not silently begin Option 2.

**STOP FOR USER REVIEW. No public activation, Stage 4, recurring paid serving, or additional experiment.**
