# Iteration 1 completion authority — September 21, 2026

The owner explicitly approved the attached five-iteration plan's **additional
pooled $50, 600 metered attempts, and 300 native counts**, with execution of
**Iteration 1 only**. These are ceilings, not spending targets. The per-iteration
allocations are planning guides. The new $5 contingency remains protected until
the integration/validation stage or separate owner authorization.

The same exclusive protected-main executor and ledger identity
`on-demand-team-offline-v2-20260909` remain authoritative. Before any new paid
execution, its prepare step verifies run `35616677156`, artifact `10645414639`,
all checkpoint bytes, 687 historical requests, 188 native-count rows, and all
historical events. It appends the amendment and the exact terminal-failure
disposition in one atomic ledger write. It preserves every request, cache,
charge, unknown hold, and original identity. Local rehearsals are copies and
never become spending owners.

The effective lifetime ceilings are $60 / 1,290 requests / 490 counts. New work
also remains within $50 / 600 / 300 from this verified checkpoint, including all
new rows regardless of their purpose label. The existing $1.267862 and two-request
reserve remains protected in addition to the new contingency. Initial conservative
usage is $8.161490: $7.346880 known and $0.814610 in four unresolved holds. No hold
is refunded. Historical routes retain their closed inventories and old sublimits.

The exact latest failed request `c1d338226c57473ea464d002121b0167` is permanently
closed to replay. Its HTTP 400 grammar-rejection receipt and diagnostic are
verified using their actual byte hashes and canonical JSON linkage. The sole new
strict operation is `cb-fc-shared-check`, using transport
`complete-pair-shared-rows-strict-v1`, model `claude-sonnet-5`, all 12 people and
24 questions, the complete original evidence and rubric, and 12,000 output tokens.
The saved Luna assessment is validated and reused locally; its grades and
rationales are never sent to the independent checker.

## Predeclared bounded fallback

If, and only if, the strict operation reaches an authenticated terminal failure
with a retained HTTP 400 **compiled grammar is too large** diagnostic, one
`cb-fc-json-fallback` operation may run. It uses the same model, original inputs,
rubric, output capacity, and strict application validator, with the separate
`complete-pair-shared-rows-json-text-v1` identity. Its strict predecessor's full
unknown hold stays charged and cannot be replayed. A missing response, timeout,
refusal, invalid answer, or scientific disagreement does not authorize fallback.
An unchanged rejected schema is never resubmitted. If this fallback also fails,
preserve its evidence and report the exact blocker; do not begin another variant.

Each exact request reserves $0.20 in full before inference, using a native count
with 20% plus 1,024 tokens of input cushion, capped at 40,000 input tokens. The
count is sizing evidence only. The model rates were rechecked September 21 at
the [official Sonnet 5 pricing page](https://platform.claude.com/docs/en/models/sonnet-5/overview).
Real reported usage determines known charges; missing usage retains the full hold.

The application requires exactly one answer for every original question, no
duplicate or extra answers, owned active claim IDs/revisions, exact fixed source
ownership, bounded reasons/gaps and positive support. It reconstructs and validates
the complete original canonical result. Engineering tests, live provider acceptance,
and scientific comparison are separate receipt states. Fixtures and successful
token counts cannot establish a completed independent check.

Necessary implementation, diagnosis, bounded remediation, exact-head review,
protected merge and product reconciliation are authorized. Repository convergence
stops remain in force. Public recommender activation, registry cutover, legacy
withdrawal, recurring public paid usage, later iterations and full E2E/Playwright
execution remain disabled. A successful check is an offline independent result,
not production verification or a release decision.

## Exact retained-response recovery

The real strict request in run `35631615659`, from protected code
`5868429daec2128aaa0ded1f503b62573638048d`, returned HTTP 200 and a complete
`end_turn` answer. The provider accepted the shared grammar. Its 24 rows used
original canonical claim IDs and revisions instead of the requested short
addresses; local validation correctly stopped at `answer_claim_owner_or_revision`.
The original request `2953527aba83416aa82288888e002d4b` remains failed, with its
known $0.077466 charge, original receipt and complete diagnostic unchanged.
This failure does not qualify for the predeclared grammar fallback.

The owner's diagnosed-correction authority covers the separate
`iteration1-canonical-claim-alias-recovery-v1` validator. It authenticates the
terminal run and pins the complete original checkpoint, ledger, request, receipt,
diagnostic, scientific inputs and retained response. Every canonical reference
must map uniquely to the original question's person and exact active revision.
Only that serialization mapping changes. All 24 grades, reasons, limitations,
claim order, scientific acceptance rules and fixed source mappings are retained.
The unchanged complete-pair validator remains mandatory. An absent, incomplete,
redacted, altered, foreign-owner, retired or ambiguous reference fails closed.

The trusted selector is `{"completion_iteration1":"retained-response"}`. It
reserves zero dollars, zero metered attempts and zero native counts, requests
no provider credentials, and never calls a provider. A distinct derived cache,
recovery receipt and ledger event link original generation evidence to this
validation identity. The original failed cache key is not re-keyed for paid
execution or changed to valid. Conflicting recovery artifacts fail closed;
interrupted deterministic writes can be completed only from the same pinned
original evidence. All historical requests, holds and caches remain intact.

This recovery changes neither the successful strict transport's identity nor
the predeclared fallback. Scientific disagreements in the saved answer remain
results; they cannot authorize another scientific request.
