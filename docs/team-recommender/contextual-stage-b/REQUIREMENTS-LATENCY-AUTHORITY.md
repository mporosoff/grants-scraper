# Repair scientific requirements and reduce demand-assessment latency

## Owner decision and execution instruction

Continue the existing Funding Finder team-recommender task and product worktree. Keep the feature private. The owner explicitly rejects the observed five-minute cold interaction as ready for release. Do not prepare, register, or activate the 50/150 public launch in this task.

Implement two bounded changes: (1) correct the distinction between sponsor requirements, necessary contributions within one chosen approach, and alternative/optional research directions; (2) reduce scientific-assessment latency by comparing ONE explicit lower-effort Sonnet configuration with ONE existing Luna configuration on identical audited evidence. Then, if a credible faster route exists, demonstrate one corrected end-to-end workflow. This is not another embedding, ranking, or team-composition research campaign.

Preserve the working contextual assessment and separate verifier, audited profiles, canonical source/child handling, local deterministic composition, persistent shared assessments, and provider-free ordinary edits. No advance generation of teams or all-catalog assessment prewarming. No public activation, recurring spending allowance, new model family beyond the named comparison, profile audit, MMR, Bayesian fitting, or lexical-threshold tuning.

## 1. Resume and establish exact authority

Repository: `mporosoff/grants-scraper`.
Product worktree: `C:/Users/Marc Porosoff/projects/grants-scraper/.worktrees/on-demand-team-recommender`.
Product branch: `codex/on-demand-team-recommender`.
Existing helper worktree: `.worktrees/contextual-team-demand-service`.

Read the completed Phase 2 report, effective contracts and request receipts, original science/registry artifacts, current plan/handoff/state, and applicable AGENTS. Resolve actual heads and uncommitted work; do not reset to the reference identities below or overwrite unrelated main work.

Reported reference identities:
- Phase 2 product before report closeout: `3f39db91fc17b5a6397181032779e431d4abb8e5`.
- Tested browser: `b2d5412e2488725bed8b9da69f663f407a55c409`.
- Trusted helper merge: `2e7729b690815730aa0d78b71d4919c8835cd9a8`.
- Audited registry: `60169651eaff43c75e0eccd12167371d8131b76ed67592eaed18d3689d188126`.
- Actual demand service: existing `funding-finder-researchers` Worker and its coordinator/trusted runner. The separate `workers/ai-gateway` contains an existing Luna adapter; it is not automatically the demand-service implementation or spending owner.

Retain all previous successful/failed calls, graph versions, paid-stage caches, reviews, and accounting. Phase 2's finite allowance is consumed. This document is a new limited authorization inside the original lifetime cap, not permission to reopen old request purposes.

Inspect the historical Luna results in `docs/OFFLINE_AI_EVALUATION.md` and their named receipts. Record that Luna was previously tried on older source/profile/prompt contracts and failed scope gates; it has not been validated in this current audited demand workflow. Do not repeat that old study or treat its estimated savings as a forecast.

## 2. One ledger and a finite new allocation

Authorization: `on-demand-team-offline-v2-20260909`.
Reported latest owner: run `34795980969`.
Reported totals: $7.232138 charged, 680/690 attempts, no outstanding reservations, $2.767862 and 10 attempts remaining.

This task authorizes AT MOST $1.50 and EIGHT new provider attempts, including every model, embedding, comparison/check, failure, and probe. Preserve at least $1.267862 and TWO attempts at the reported balance. Reconcile actual newer usage first; reduce the task allowance rather than consume that reserve. No additional overall dollars, request slots, provider-account credit, or unrelated gateway allowance is granted.

Lock the full allocation and conservative complete-input/output cost before dispatch:
1. One ECLIPSE applicability assessment using Sonnet at medium effort.
2. One identical ECLIPSE applicability assessment using Luna at low effort.
3. One bounded, blinded evidence check of those assessment outputs.
4–7. At most one corrected DOE workflow: interpretation, requested query embedding, applicability assessment, verification.
8. One final evidence check of the corrected DOE requirements, primary group/members, and explanations.

Reuse unchanged profile vectors and exact query rows where compatible. Unneeded embedding calls are not a license for an extra experiment. All accounting must remain in the existing authoritative reservation domain. Do not spend through the generic gateway's unrelated daily budget to evade the experiment ledger. A named nonoverlapping allocation is required if the existing provider adapter runs in a different trusted coordinator.

No automatic semantic reruns or provider fallback. Once-per-operation logical identities must include the approved new contract/model/effort. A request whose outcome may be unknown cannot be silently dispatched again. Preserve old charged failures. This task authorizes intentional, versioned comparison requests and a scientifically revised DOE workflow, not cosmetic re-keying of old failures.

If the complete plan cannot fit, finish safe code/analysis and report the exact constraint before any partial campaign that would strand its checks. Public paid serving stays off.

## 3. First inspect the actual critical path, with zero new provider calls

Use the retained Phase 2 request bodies and usage/timing receipts. Report per stage:
- Effective model and endpoint, reasoning/thinking/effort fields actually sent, output allowance, and response termination.
- Input tokens, provider-reported reasoning tokens where available, final-response tokens where available, and total billable output. Unknown breakdowns stay unknown; do not retain or expose private reasoning text.
- Input preparation, provider elapsed time, queue/job startup, native token counting, checkpoint transfer, persistence, callback, polling, and browser rendering. Do not invent subspans absent from historical traces.
- Duplicate source/profile/schema material and model-generated fields already owned by the application.

The observed bottleneck is assessment, not embeddings or local composition: DOE assessment ~199 seconds, ECLIPSE ~232 seconds; their output totals were 15,891 and 22,458 tokens. Math's failed 16,000-token response included 14,304 reasoning tokens. Verify these against receipts. A large output ceiling is not an instruction to generate less reasoning.

Review the normal service call graph. Identify any GitHub Actions setup, artifact round-trip, or polling interval on the user-facing critical path. Do not assume the Worker itself performs inference merely because it accepts the Build request. Remove straightforward duplicated preparation or avoidable delivery delay without weakening persistence/accounting. Do not replatform the execution environment in this task: record any remaining workflow-runner overhead separately and say whether it prevents the latency goal.

Avoid another general profiling project. This inspection must lead directly to the bounded repairs below.

## 4. Correct the scientific requirement contract and all its consumers

Use DOE child `363302:a-1` and its retained official source as the concrete reproducer. The old graph marks six roles required, including alternative sour-gas, NGL and catalyst directions. Preserve that original graph unchanged as historical evidence.

Trace the existing `required` field from interpretation through retrieval queries, graph validation, verification, composition, missing-skill calculation, and displayed explanations. Correct the meaning once across those consumers.

Represent these distinctions with the smallest adequate versioned schema:
- A sponsor-stated requirement, with exact source support and applicability/conditions.
- A planning contribution necessary for the ONE proposed coherent approach.
- An optional contribution or an alternative direction that is not a missing skill in that approach.

Administrative eligibility/cost-sharing rules remain in the existing conditions system; do not turn them into researcher specialties. Multiple allowed approaches do not create a requirement to cover their union. Alternative methods inside a coherent topic do not automatically make the topic invalid or require inventing new child records.

Interpretation chooses an approach from the source BEFORE seeing named candidates, or explicitly records supported mutually exclusive alternatives. Do not choose a favorite team's approach after seeing the researchers. One legitimate contribution is allowed; no universal role template or artificial splitting to satisfy a count.

The composer must calculate required coverage/gaps only for applicable source constraints and the selected approach's necessary contributions. Other permitted directions must neither penalize the selected team nor supply a spurious complementary role. Do not merely set every flag to false. Do not relabel all alternatives as mandatory planning contributions under a new field name.

Retain exact scope/child/source and claim ownership checks, source citations, downgrade-only verification, honest gaps, and uncertainty. Model interpretation is not a certified sponsor obligation. Preserve the existing interface; only inaccurate requirement/gap wording may change minimally where needed.

Before paid requests, add zero-provider tests for: genuine conjunction; optional alternatives; conditional applicability; coherent single-contribution science; distinct parent/child ownership; legitimate missing contribution; and preservation of shared/nonexclusive methods. Use general source structures, not expected named teams.

Version this semantic change and its dependencies. Never mutate an accepted old graph or claim its earlier checks validate the corrected interpretation. Unrelated cached graphs remain valid when their exact contracts permit it. Resolve compatibility rather than invalidating every graph merely because a package timestamp changed.

## 5. A two-configuration performance comparison, not a model tournament

Reuse the current ECLIPSE `351715` interpretation, the exact twelve assessed candidate documents, source/conditions and query retrieval result. This isolates the expensive applicability step. Both requests receive semantically identical complete evidence and the same applicability instructions and canonical response contract. Dynamic provider JSON-schema wrappers may differ only as required by their APIs.

Configuration S:
- `claude-sonnet-5` with adaptive thinking and EXPLICIT `output_config.effort = medium`, after checking current supported syntax.
- Do not rely on defaults; do not use a deprecated manual thinking-budget field.

Configuration L:
- `gpt-5.6-luna` through the existing trusted OpenAI Responses adapter, with `reasoning.effort = low`, low output verbosity, strict structured output and `store = false`.
- Inspect/reuse `workers/ai-gateway/src/index.js` and the existing offline OpenAI adapter. Do not simply change an Anthropic model-name string: response parsing, schema conversion, usage, cost reservation and completion status must be provider-correct.
- Existing gateway retries and fallback routes are NOT inherited automatically. This comparison permits one call per configuration.

Keep a sufficient finite maximum output allowance for complete structured responses, including reasoning. Reduce work through explicit effort and concise decision-oriented output; do not cut max tokens blindly and recreate the earlier truncation failure. Output contract, prompt prose bounds, native schema and local validation must agree.

Maintain all twelve people and all relevant evidence. Do not win the comparison by assessing fewer candidates, dropping difficult records, omitting conditions, or returning no supported relationships. Keep source/claim IDs instead of regenerating known quotations, names and URLs. Concise rationales must still explain applicability and meaningful limitations. Do not remove the real semantic decision or manufacture support from retrieval scores.

Freeze any small shared output/prompt cleanup BEFORE both requests. No model-specific scientific hints or desired answers. Reuse the existing good high-effort ECLIPSE response as an exposed reference, not a gold label or a new paid baseline. No none/low/medium/high sweep, extra model, or repeated favorable sampling.

Check both outputs mechanically, then use the existing offline checker once on a bounded union of returned relationships and disagreements with the retained reference. Give it complete relevant evidence, hide model/configuration identity and speed/cost, and require evidence-grounded explanations. Preserve truncation, unsupported upgrades, missed reasonable connections, honest uncertainty and unresolved differences distinctly.

This is a directional engineering comparison on an exposed source, not a statistical noninferiority claim. If the bounded check cannot assess every relationship, show which were unchecked. It cannot justify an automatic general claim that the model is safe or fast.

Select at most one route for the next end-to-end case. Scientific adequacy is necessary: no fabricated references or requirements, no newly established unsupported automatic member, reasonable supported candidates retained, and complete valid output. Among adequately supported configurations, prefer materially lower latency and then lower cost. Record disagreement or missing evidence instead of manufacturing a winner.

Performance goal: a 15–30-second verified cold interaction, with 60 seconds as the provisional upper product guardrail for the measured proof. These are owner-facing engineering targets, not promised service levels or permission to release. A substantially faster assessment is useful evidence even if the full workflow later misses the target.

If neither configuration materially reduces assessment time (roughly a factor of two relative to the observed ~232-second reference) with credible scientific output, stop further paid workflow work. Report the remaining mechanism; do not automatically add more variants. The code repair and comparison remain completed work.

## 6. One corrected end-to-end DOE workflow

If a credible faster configuration exists, use it explicitly for interpretation, assessment and the separate verifier on DOE `363302:a-1`, with the repaired requirement contract. Record that only applicability has been compared head-to-head; the full run is where the other stages are first exercised under the selected configuration.

Use the retained full official context and audited directory, recheck authoritative action eligibility, and retrieve against all eligible people. Keep the existing deterministic contribution-round-robin maximum of twelve assessed people. Do not alter the shortlist bounds to make the time better. Reuse existing individual vectors; generate requested query rows only if the corrected interpretation changes their exact inputs.

This is a new versioned scientific assessment, not a retry of the old completed graph. Preserve old facts, graphs, grades and costs. No deletion of a valid cache to make work appear cold. Show exactly which source/profile/query stages were reused and which ran anew.

Execute through the real restricted Build path, retain joined-job and persistent-cache behavior, and return only a completely validated and verified graph. Do not display an unverified speculative team as the final result or quietly remove verification to meet a latency target.

One final bounded checker request must inspect:
- Whether the source's optional/alternative directions were correctly separated from applicable requirements and the chosen approach.
- The proposed primary group's usefulness and every primary member's actual contribution.
- The fidelity of those primary explanations, including transfer limits and genuine gaps.

Use the corrected complete-question/reference schema and adequate output capacity. No extra person/alternative regrading campaign. A favorable team score cannot override an incorrect requirement or an unrelated member. Historical Müller/Basdogan or Barnett/Porosoff grades do not preselect the new membership or replace the new-source-interpretation check.

Measure Build-to-verified-render wall time, not acknowledgement, time to first token, or a manually assembled sum of incompatible observations. Separate provider times from runner/queue/persistence/polling overhead. Also measure a later compatible Build and local edit without purchasing another assessment. A small sample cannot establish p95 reliability.

Do not treat lower price as proof of lower latency or a faster but scientifically weaker response as success. No public activation even if the single run meets the target.

## 7. Cache, execution and evidence safeguards

Preserve stage-specific scientific identities, model/prompt/schema/effort identities, currentness checks, complete audited evidence, roster-aware invalidation (including previously unselected people), manual access and zero-paid ordinary edits.

Reference/schema correctness remains strict. API format/completion failures are not scientific negatives. Paid requests never execute within replayable database transactions. One current job and its durable accounting survive client detachment; a late result cannot overwrite another scope. No browser credentials or arbitrary-source/prompt/model override.

This task may reuse the existing OpenAI route's serializer/usage handler but may not change other gateway features' retention, service limits, retries or budgets. A new trusted demand-route binding must undergo normal review and restricted deployment. Do not route experimental branch code through an unrestricted credential-bearing endpoint.

Provider prompt caching may be analyzed as a later opportunity; do not enable paid cache creation or change the comparison between arms midway. It reduces reusable input processing, not all generated reasoning. Likewise, do not commission parallel candidate batches or a progressive-team architecture in this bounded test. Report these only as conditional next options if the measured bottleneck remains.

Keep old live/prebuilt workflows unchanged while the replacement is private. Do not publish corrected profiles next to stale legacy team evidence. Do not register or prewarm new public cohorts.

## 8. Finish with a single report and a clear latency decision

Run focused contract/required-scope/composer/provider-adapter/cache/accounting tests during implementation. Independent exact-head review and required CI are mandatory for the narrow trusted helper. Restricted prerequisite deployment with public paid work OFF is authorized; production application/registry release is not.

Reuse prior full-browser evidence only for unchanged bytes. Run affected real-graph browser checks and the necessary final configured suite when browser/runtime semantics changed. Do not repeatedly rerun the entire suite after offline-only changes or call a test pass a scientific acceptance.

Write:
`docs/team-recommender/reports/requirements-and-latency-repair-report.md`

Include:
1. Exact starting/final product/helper/service identities and actual code changes.
2. Old versus corrected requirement interpretation, source support, chosen approach, and consequences for gaps/composition.
3. Effective before/after model, effort, schema, input/output allowances and actual measured usage.
4. Historical Luna findings, clearly separate from this new current-input comparison.
5. Identical-input assessment outputs, complete/partial quality evidence, observed timings and actual costs for S and L.
6. One end-to-end corrected workflow, or why paid continuation was not justified; exact freshness/cache status.
7. Verified-render time and independently measured orchestration overhead; known/unknown reasoning-token breakdown.
8. Cache-hit/edit behavior, source/profile/roster invalidation, testing and protected deployment evidence.
9. All eight or fewer attempts, failure history, remaining dollars/slots and proof of the single ledger.
10. An explicit disposition: REQUIREMENTS FIXED AND FASTER CANDIDATE DEMONSTRATED; REQUIREMENTS FIXED BUT LATENCY/QUALITY STILL UNACCEPTABLE; or NAMED TECHNICAL BLOCKER.

No disposition authorizes release, a provider tournament, another automatic attempt, or a new spending allowance. If the faster path still takes minutes, say so and identify whether the remaining cause is reasoning, serial verification, input preparation, or the workflow-serving boundary. Do not redefine a spinner, a queue acknowledgement, or an unverified early draft as a completed team.

Complete the bounded task and STOP FOR USER REVIEW. Keep public activation and Stage 4 disabled.

## Reference basis for the agent to verify at execution

- Latest Phase 2 report: `docs/team-recommender/reports/phase-2-cold-demand-and-release-readiness.md`.
- Effective current science: `tools/contextual_team_demand_contract.py`, `tools/contextual_team_references.py`, `tools/contextual_team_contract.py`, `tools/contextual_team_phase2.py`, and their versioned configuration.
- Existing Luna adapter: `workers/ai-gateway/src/index.js`; older team comparisons in `docs/OFFLINE_AI_EVALUATION.md`.
- Sonnet settings: https://platform.claude.com/docs/en/models/sonnet-5/overview and https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-sonnet-5
- Luna model/settings/pricing: https://developers.openai.com/api/docs/models/gpt-5.6-luna
- Latency principles: https://developers.openai.com/api/docs/guides/latency-optimization

As checked September 14, 2026, standard rates were Sonnet 5 $2/$10 per million input/output tokens and Luna $0.20/$1.20. Recheck current supported parameters and actual account route before dispatch. These prices do not establish task quality, actual generated token counts, account access, or latency. Do not reuse historical prices or claim hypothetical price substitutions as measured savings.
