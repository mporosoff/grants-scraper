# Integrated browser checkpoint — September 13, 2026

The complete configured 117-test Playwright suite ran at product commit `2a62f6c072a9fcfe37a5b8857acee11f4b9b1a65` using the ordinary local application and retained schema-3 package, before any trusted contextual deployment or real assessment. **113 passed, four failed**, in 18.7 minutes. These results do not validate a live contextual graph or replace the earlier scientific failures.

Preserved private evidence: `outputs/contextual-stage-b/browser-run-2a62f6c/` (JSON results and failure traces), plus `outputs/contextual-stage-b/full-browser-validation.log`. The static server used by this run was stopped after reporting stalled cleanup; the runner then returned exit 1. No unrelated process was stopped.

Failures retained for bounded diagnosis:

- Institutional Intelligence accessibility path: the expected question-answer section stayed hidden, so its later accessibility scan was not executed. This is not evidence of an axe violation.
- Team Builder closed-context test: its legacy team-options filter was hidden/disabled because the active package has no compatible pre-generated proposals.
- Phone/enlarged-text polish: measured vertical quick-action spacing 116.75 pixels versus the asserted maximum of 1.
- Team Match directory/history workflow: the 90-second overall timeout recurred at history restoration. Profiling and a bounded repair remain required; increasing its timeout is not a fix.

The private contextual preview uses the existing application with a version-bound index and explicit network instrumentation. It blocks unrelated remote services. Public HTML, routing and data remain unactivated. The contextual client's canonical JSON POST uses a simple text content type to avoid introducing an unauthenticated Access preflight exception; server validation and authenticated identity remain mandatory.

Four focused client/drawer fixtures passed after that transport adjustment. They use real audited input shapes and fictional provider/graph responses, and prove no hidden dispatch in the exercised paths. They are not real semantic outcomes or complete browser validation. The prerequisite has incurred no new provider spending at this checkpoint.
