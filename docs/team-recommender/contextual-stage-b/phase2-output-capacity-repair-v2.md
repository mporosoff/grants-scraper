# Phase 2: one completed output-limit correction

This amendment preserves Phase 1 and the Phase 2 source/scientific lock. It
implements the owner's Phase 2 authority for **at most one** corrective
re-execution after a proven completed mechanical failure. It grants no money,
request slots, model changes, public activation, or recurring allowance.

## Observed boundary and immutable ancestry

Protected run `34791671860`, code
`a389b3fdb4923600e7e4970fb69068046175ebed`, failed Mathematical Biology's
assessment after HTTP 200 / `max_tokens`. Request
`d3d56cfb377a42499955555378c96eff` used 19,521 input and 16,000 output tokens
(14,304 thinking); its $0.199042 charge is reconciled. It is not an unknown
dispatch or an unfavorable scientific verdict. Interpretation and three query
vectors succeeded and must be reused exactly. No verification or graph exists.

The amendment binds the full 671-request history and exact failure receipt.
Before the sole replacement assessment, trusted code reconstructs and compares
the original complete body hash, then changes only its output allowance from
16,000 to 24,000. The original request and failed job remain immutable. The new
fixed job records `repair_of`; repeated Builds join it, and any uncertain,
failed or reconciled-with-lost-cache corrective request cannot be purchased
again. No new repair key is generated on restoration. Exact successful caches
remain reusable. The transport ceiling for these 24,000-output assessments is
420 seconds, proportional to the observed 214.7-second truncated response.

ECLIPSE's still-unissued first assessment receives the same mechanical output
capacity. Its prompt, source, model, retrieval and scientific decisions are
unchanged. This does not authorize repairing another paid failure.

## Complete remaining allocation, before dispatch

The sole ledger is `on-demand-team-offline-v2-20260909`; restored owner
`34791671860` has $6.494248 / 671 lifetime attempts / zero outstanding.
Phase 2 has used $0.672268 / nine attempts, including the failure and DOE's
two completed output checks. The original $2.50 / 18 Phase 2 ceilings remain.

| Remaining operation | Maximum dollars | Attempts |
| --- | ---: | ---: |
| Named Mathematical Biology replacement assessment | 0.288900 | 1 |
| Mathematical Biology verification | 0.324000 | 1 |
| ECLIPSE interpretation, query vectors, assessment, verification | 0.783000 | 4 |
| One paired group/person check, both remaining independent scopes | 0.157440 | 1 |
| Separate primary-explanation checks, one per remaining scope | 0.110720 | 2 |
| Total remaining envelope | **1.664060** | **9** |

Worst cumulative Phase 2 cost is $2.336328, leaving $1.841692 and ten lifetime
attempts after all 18 Phase 2 attempts. This protects the required $1.678020
and ten attempts. Unneeded operations are not spending targets. Actual usage
and uncertain reservations continue to control permission before dispatch.

The paired relevance request contains both complete, independently identified
source contexts and full documents for the selected people. It uses the same
primary / deterministic second-option / unique-member questions. It contains
no generated explanations for positive relevance questions; explanation checks
remain separate. Scope citations are context-owned enums. The two individual
group operations are withdrawn before use, not re-keyed after failure. DOE's
already-completed eight answers are not rejudged. Negative cases retain their
actual assessed-set gap question, without invented teams. Maximum paired
reservation is 48,000 input / 6,144 output tokens and 128,000 wire bytes. The
predeclared worst separate positive packets measured 79,864 bytes; actual
complete packets still undergo native sizing before metered execution. No
decisive evidence is clipped to fit. If complete evidence cannot fit, stop that
operation with the missing denominator recorded.

Prices remain the 2026-09-13 official Sonnet 5 $2/$10 per million input/output
and Voyage 4 large $0.12 per million input. The native counter uses the existing
free count ceiling and crash-persistent exact cache. No new provider is added.

## Protected execution and validation

Only the narrow helper is proposed for protected main. The existing merge
trigger deploys the Access-restricted intake Worker; public activation remains
false. The reviewed 61-file preview bundle, source package, registry, composer,
permissions, schedules and other services are unchanged. The main release
planner must remain validation-only; no catalog generation or publication is
authorized. The original three normal jobs plus this one named corrective job
are the entire restricted coordinator inventory.

Required evidence: focused executor/accounting/response tests, crash-boundary
restoration with zero duplicate fixture dispatches, exact-head CI and independent
review, protected merge and serving-module proof. Only afterward may a deliberate
normal browser Build claim the named correction. The completed failure is never
silently deleted or called a fresh success. Any second failure receives no
automatic paid retry. Continue the remaining authorized checks where safe.
