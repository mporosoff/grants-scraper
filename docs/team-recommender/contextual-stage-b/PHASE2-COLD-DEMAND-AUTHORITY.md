# Phase 2 — ordinary cold demand and restricted release readiness

Owner instruction received September 13, 2026. This continues the completed Phase 1 checkpoint `c5d01706ebc8eea22afc25e83cc0b643e3b7c5f7`. Phase 1 stays closed; its accepted LPS assessment and seven model verdicts are regression evidence and must not be purchased again.

The existing product worktree and branch remain authoritative. The existing `contextual-team-demand-service` helper worktree now continues from protected main `3ebe0466c202903b44c099d2b0803e2436f04e37` on `codex/contextual-phase2-cold-demand`. The prior helper branch and all private outputs remain intact.

Authorized packages, with factual checkpoints followed by continued work:

1. Complete the ordinary missing-stage runner and audit the complete provider/reference/application contract using zero-provider fixtures.
2. Lock three additional current source-complete scopes before assessments, including a native child; execute them through ordinary restricted-preview Build, then their predeclared bounded output checks. Demonstrate durable shared jobs and local editing.
3. Reconcile original 50/150 routing inventories, coherent restricted release/rollback packages, performance, and the final configured browser/E2E/accessibility suite.

Use the same scientific model, full audited profiles, retrieval, contribution rubric, verifier and deterministic composer. No scientific retuning after results, paid extension, new human packet, pre-generated teams or catalog-wide assessment.

## Single budget and ownership

Authorization remains `on-demand-team-offline-v2-20260909`; cloud run `34775743077` is the verified latest owner at resumption. Starting charges are 5,821,980 microUSD, 662 lifetime attempts, zero outstanding reservations. Local copies are read-only mirrors.

Phase 2 permits **at most 2,500,000 new microUSD and 18 new attempts**, inside the unchanged 10,000,000 microUSD / 690 lifetime ceiling. Preserve at least **1,678,020 microUSD and 10 attempts**; any later spending reduces Phase 2 authority. Lock complete-packet input/output costs and output-check capacity before dispatch. Exact compatible vectors and scientific caches retain their original provenance; reuse creates no spending credit.

One corrective re-execution across Phase 2 is permitted only for a proven completed mechanical failure, after a recorded fix, with explicit `repair_of`, and capacity inside both phase limits and reserves. No ambiguous replay, re-key loophole, unfavorable-science retry or further automatic retry cycle.

## Approval and release boundary

Narrow trusted-helper/service changes may receive required CI and independent exact-head review, protected merge and restricted deployment. Public activation must remain disabled. Existing review, persistence and permission protections apply unchanged. The final combined candidate's complete configured browser/E2E/accessibility suite is explicitly authorized; ordinary implementation uses focused checks.

No public application/registry release, Stage 4, subscriber activity, account change, recurring allowance, new provider/model or profile audit is authorized. Source-only bounded official retrieval may close concrete gaps; it does not authorize rewriting profiles or selecting cases by favorable outcomes.

Final deliverable: `reports/phase-2-cold-demand-and-release-readiness.md`, plus updates to the existing PLAN, HANDOFF and state. Report every executed, reused, missing and failed denominator, actual charges and the remaining reserve. Stop for owner review after Phase 2.
