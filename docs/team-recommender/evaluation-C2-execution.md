# C2 execution protocol supplement

The fixed E1 rubric, trusted prompt/schema, Sonnet 5 route, evidence blinding and human cap remain unchanged. C2 supersedes historical statements that execution is unavailable or optional comparisons are omitted. PR #215 is merged at `829eada2a08d55bede43d0828b2a5684e49ac0c9`. No recommender was merged. Prior reports/protocol text remain historical evidence.

Before the first judge response, the complete real development comparison, five-entry declared parameter grid, source-only grouping overlay and 20-item human selection were saved. The full judge plan contains 398 unique semantic items, 1,589 algorithm/control occurrences, and 395 submitted items in at most 168 bounded requests. Three source items lack complete bounded original evidence. The 90 scientific reservations and 30 derived controls remain in every denominator; controls deduplicate their common source-context judgment and require separate deterministic perturbation checks. Source-control grades are not proof of independent directory feasibility. No-group outcomes cannot establish infeasibility.

All 155 eligible researchers were scored for each of 34 prepared sources. Fifty-six other reservations retain explicit unprepared dispositions. Scores on prepared sources with submission prerequisites are offline development observations; the runtime currentness policy still blocks their actions. The fixed whole-call baseline uses all directory rows with whole-call/core floors .30/.25, independent of coverage admission. Its team size matches each actual coverage group. Where coverage produces no group, a descriptive two-person baseline is not labeled a matched-size group comparison.

The original engine missed feasible pairs under greedy starts. Initial numerical results are preserved, and bounded pair enumeration corrects this invariant before the submitted comparison. No semantic threshold was added to the frozen grid. Only one source produces groups under three grid settings; this severely limits group, complementarity and MMR conclusions. The MMR comparison uses the actual qualified alternatives at lambda 0 and .1, with membership overlap only. Identical lists require no new judgments. Five unique comparison items were source/item-hash selected for order swaps before any responses; cached identical judgments remain one observation.

Individual judgments pool exact top-five person items across all five coverage settings and whole-call ranking, plus a reproducible directory control for each prepared source. Full original active public profile passages are provided, with identical formatting. Generic or missing information is not rewritten into either expertise or incompetence. Training uses the same noisy individual labels and declared eight-feature event representation; insufficient-information cases remain visible and are excluded from binary fitting. Grouped folds use corrected development source groups. Any calibration is relative to this selected machine-judge event, not to human preferences or all directory pairs. Optional learned scorers earn inclusion only with a meaningful benefit and without unrelated-rate harm; otherwise retain a simpler fixed candidate with honest quality limitations.

The independent human packet contains 12 individual and eight complete-team/comparison items, selected before the model verdicts. Initial Stage 1 slot reservations are retained; no earlier real packet or judgments were requested. C2 explicitly asks for real useful examples. Group items necessarily concentrate in the one source with groups, which is disclosed. Marc sees source/profile evidence first and no model verdict. At delivery, these 20 consume the entire development request allowance regardless of skips; 20 final-audit items stay reserved. No response is required during this execution.

The four-request judge wiring batch consists of exact items already in the full packet, covering source, individual, group and separate explanation output contracts. The full run reuses those results through the same durable ledger/cache. It is not an extra evaluation campaign. Complete evidence is never silently cropped. Exact cost bounds and missing items are in `receipts/c2-development-judge-plan.json`; the conservative reservation is $4.2762975, while actual billed usage is expected to be lower. Prior embedding expenditure is $0.000264. The $6 cumulative Stage 2 stop and $4 later reserve remain enforced.
