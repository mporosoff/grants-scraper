# Stage 2 report — on-demand team recommender

**Recommendation: DEFER Stage 3. The safe local Stage 2 implementation is complete; real preparation and semantic development evaluation remain blocked.** No paid request, holdout recommendation, production mutation or Stage 3 activity occurred. The proposed $69 budget was rejected, preserved as history and replaced by the user's single $10 authorization. This is an engineering checkpoint, not a usefulness or deployment pass.

## 1. Identity and authority

- Repository: `mporosoff/grants-scraper`, origin `https://github.com/mporosoff/grants-scraper`. Continued branch `codex/on-demand-team-recommender` in `C:/Users/Marc Porosoff/projects/grants-scraper/.worktrees/on-demand-team-recommender`. The actual plan/report package is `docs/team-recommender` in that checkout.
- Resolved and preserved the unpushed Stage 1 checkpoint `b6f71ae0396bd2670043291bc676ee8af0d222b2`, tree `094156f98f12645ffc4c77015f2b92737c67819d`. It was the clean experiment HEAD, with no later work or uncommitted changes when Stage 2 began. Exactly one registered worktree owns this branch. No reset, duplicate checkout or replacement from public main occurred. Other worktrees and the starting checkout were left untouched.
- Protected main observed for this continuation: `6adca6a795d4ab52e216e2ff7032d0e393db76b1`, tree `9a2011915aac09437a1d5a2215474a4e68091224`, the existing experiment's parent. The GitHub branch response reported protected; no protection was changed. No experiment PR or pending review was present. No review was requested.
- Read the complete actual PLAN v2.0, HANDOFF, E1 amendment, Stage 1 report, state and applicable AGENTS. Stage 2 and bounded readiness corrections are explicitly authorized by the user's B1 instruction. B1 supplies dollars, not a trusted-main prerequisite, new provider, Stage 3 or production permission. AGENTS remains unchanged.
- The original Stage 1 report is byte-preserved: SHA256 `cee7bf5159d6978bc80439d9289a743517766ab5a9e01f30f07ffa0ca75d4713`. Its 74 checks and $0 spend remain historical. Current code is bound by file hashes and the dependency digest in `stage2-candidate.json`; current receipts are sealed by `stage2-receipt-index.json`. Resolve the containing local commit with Git; the preceding checkpoint SHA is not substituted for changed-code validation.
- Registry generation: `59ccfbe8999eeb4c78441e16daca468c694fc5798b03587c72468390939dc96a`. Legacy team generation: `07f1b0b86c534b64ddd8e3dc4305c1e36d1f6652604581e48edfb5b6fdab8453`. No real v2 ingredient generation or observed new embedding-space fingerprint exists.
- The fresh bounded public read still identifies live candidate `5927df0f5835c5a06df60d6bedea9667c9aec76994174452e01fa15a2e1642f0`, generation SHA `05cec746d1c1f2e9c27ee32f34fcea7415bd7c8a`, run `34360404301` attempt 1, assembly SHA `d8533c5f6391eda0a49c1963bdfbf71eb7651492`. Live candidate-file SHA256 is `aa52b80f5fa6f403b822148f352082d79be5d4ac9483a015c62aa640caba71ee`; live search-release SHA256 is `d7b284d5ef14ed4d0e444bd82b02533b1b72b9f091e9558213c85f487ee656c8`. Live engine/panel hashes remain the Stage 1 bytes. See `receipts/stage2-live-read.json`; this is not authenticated Worker or full live-browser validation.

## 2. Work completed

### Disposition of the three readiness findings

| Finding | Correction and evidence | Remaining limitation |
|---|---|---|
| Currentness integration | Implemented through the existing authoritative retrieval and submission helpers. One fresh decision clock per action; parent and selected child eligibility, publication ownership, forecast/rolling support, restricted submissions, expiry after open, cached state and late responses are covered by focused contracts. Failed resolution revokes the previous action decision. | Physical-browser integration remains unrun. Ordinary search and the separate all-member Team Match policy were not changed. |
| Source-validation readiness | Implemented exact original-document/extracted-text receipt checking, source spans/ownership, original retrieval time, coherent approach/conditions flags, claim revisions, retired evidence, complete vector-space identities and fail-closed snapshot validation. Missing/stale/ambiguous/unvalidated inputs cannot become a valid no-group result. | **0 real v2 source receipts and 0 compatible new vectors proved available.** Original-source coherence/conditions/successor grouping audit is unfinished; no real prepared package is claimed. Schema validity and old model approval are insufficient. |
| Trusted paid-execution route | Inspected the existing protected-main-only workflow, old phase allowlist, credential scope and old checkpoint identity. Reused its local accounting/atomic-lock pattern; added no provider dispatch or permissions. | **Blocked.** No existing route admits this experiment's intended code and ledger. The smallest prerequisite is a separately authorized, narrowly reviewed main-only preparation/evaluation entrypoint accepting allowlisted public packets and the restored experiment ledger. Branch code must not receive secrets. No prerequisite PR or main change was made. |

### Implemented locally

- `assets/team-recommender.js`: scoped passage/aspect relevance, central scientific anchor, absolute gates, fixed weighted coverage `F(T)=sum_i w_i max_j r_ij`, deterministic 2–4-person selection, final member marginal checks, stable ties/options, up to eight near-best alternatives and complete admitted-person reachability. No department/novelty reward. Exact small-pool optimization and bounded full-directory anchored search share the same objective.
- `assets/team-ingredients.js`: immutable compiled snapshots, strict source/claim/vector validation, original-source validation receipts, explicit evidence mapping, same-origin bounded static loading, atomic adoption, private typed vectors and bounded caches. It adapts numerical results into the existing views without promoting similarity into confirmed evidence.
- Existing `opportunity-team.js` now dispatches explicit v1/v2 routes, verifies lazy runtime hashes, rejects stale loads and exposes directory loading independently from optional team data. Existing v1 validation remains. `opportunity-team-panel.js` changes only the declared resolve/load/action orchestration for currentness, stale responses and errors.
- `tools/build_team_ingredients.mjs` builds content-addressed assets from already-public recipes, exact compatible vectors and original-source validation records. It validates before atomically naming a new output directory. It makes no extraction, model or embedding calls. Only synthetic packages were built in tests.
- Offline source validation, metadata-only reuse inventory, development-only evaluation packet assembly, independent Python numerical reference and bounded resource measurements were added. The evaluation helper blinds/deduplicates items, preserves occurrences, shares original evidence and enforces request bounds; it is not a judge executor.
- The new durable experiment ledger uses existing atomic accounting, one authorization identity across providers, conservative unknown-request reservations, restored attempt counts, stage/total/token/request/preparation/retry limits and one-time actual reconciliation. Unexpected usage above reservation is recorded and blocks future reservations; it is not hidden by rejecting the receipt. These paths ran only against temporary zero-provider test ledgers.
- Local release dependency hashes were rebuilt and checked using the existing package tool. The two HTML files change only asset version hashes and Team Match's directory-loading seam. General search corpus, purchased search vectors, Worker allowlist, real profiles, saved-state behavior and the all-member Team Match algorithm were preserved.

### Documents and history

Added B1 and the current finite budget/ledger. Updated PLAN, HANDOFF, the plain-English summary and the current/later evaluation supplement to carry that authority without implying Stage 3 approval. Updated the evaluation protocol and compact fixed judge packet/schema to use bounded same-scope batches. The original Stage 1 report and rejected budget are retained, together with exact prior state/protocol/prompt/schema copies. The Stage 1 experiment specification is explicitly historical; the implemented candidate is frozen separately. AGENTS was not edited.

## 3. Results

### Actual inputs, reuse and 150-scope progress

| Asset or observation | Actual count / disposition |
|---|---|
| Catalog | 1,422 parents; 223 published children |
| Eligible public researchers | 155; all retained in full-directory candidate consideration |
| Active claims / normalized within-person passages | 427 / 402; IDs/revisions retained, no invented profile evidence |
| Source union across development, holdout and rollout | 227 source IDs with cached public original summary text; 227 parents current at capture |
| Submission access | 208 open/rolling/not-listed; 19 require restricted/verification treatment |
| Historical role candidates / quoted candidates | 443 / 403; 386 quotes match current public summaries exactly |
| Original v2 source-validation receipts | 0 compatible receipts; matching a summary quote is not a newly verified original source |
| Compatible query/aspect/claim vector cache hits | 0 proved; unique missing vector-row upper bound 3,840, not an executed inventory |
| Existing purchased search vectors | 1,629 × 1,024 Float16 document rows; not reusable under the new query/claim Float32 contract and existing reuse prohibition |
| Retained historical provider cache | 481 exact keys inspected as metadata; 0 response values reused as E1 labels or public ingredients |
| Real prepared / activated new rollout scopes | **0 / 150 prepared; 0 activated** |

Receipts: `stage2-source-inventory.json` and `stage2-reuse-metadata.json`. Historical roles may become aspect candidates only after original-source/coherent-scope validation; they are not automatically sponsor requirements. Public cached summaries and profile passages were reused for inventory/compatibility checks, not silently restamped as fresh sponsor retrievals. No private cache response values were exported.

The original grouped manifests remain unchanged: 90 scientific reservations plus 30 derived controls per split, 240 case slots and 180 recorded parent groups. Controls do not add independent calls. Source coherence and subtle successor/near-duplicate grouping remain provisional. The 150 reservation remains 100 parent scopes plus 50 children across 103 parents; the first cohort remains 30 parents plus 20 children. Source-based reserve selection is preserved. Fifty rollout IDs overlap the sealed holdout and were not scored. No difficult case was dropped to improve a result.

### Method and quality evidence

| Comparison/check | Executed evidence | Interpretation |
|---|---|---|
| Whole-call A versus fixed coverage B | One hand-declared equal-size numerical contrast: B improves complementary coverage; no real recommendation outputs | Objective/component evidence only; real paired usefulness and unrelated-rate differences unknown |
| Independent exact optimization | 32 deterministic eight-person cases agree with a separate Python exhaustive implementation on feasible counts and maximum objective | Correctness at tested sizes; no large-pool global-optimum claim |
| Deliberate group size | Separate 2-, 3-, 4-person fixtures meet final marginal checks | Numerical contract, not proof those sizes suit every scientific call |
| Optional MMR | One bounded numeric overlap arm at lambda .1 stays near-best and preserves the primary in the fixture | No semantic benefit established; omitted from finalist (lambda 0) |
| Regularized/Bayesian scorer | Not fitted or evaluated; no suitable new labels | No learned preference/calibration claim |
| Real development/holdout recommendation evaluation | **0 / 0 cases**, 0 new model verdicts, 0 human judgments | No real yield, useful, unrelated, precision or calibration estimates; no confidence interval can be inferred |

All original targets remain visible: top-five reasonable precision >=80%; unrelated automatic-member occurrences <=5%; useful primary groups >=80% and group yield >=85% of independently feasible scopes; paired complementarity improvement without unrelated-rate harm. Report full eligible and independently feasible denominators, unknown feasibility, short/empty lists and clustered uncertainty when evidence exists. Current values are **unmeasured**, not zero performance and not passing.

### Changed-code validation and resources

- **74/74 focused Node contracts passed**, including actual loader/panel DOM fixtures, v1 compatibility, currentness, registry, new ingredients, independent reference and presentation freeze. **49/49 focused Python tests passed**, including currentness, registry/model and new accounting/source/evaluation tests. These are this Stage 2 code's receipts, not the old 74 Stage 1 checks.
- Package dependency check passed after updating source hashes. Original general-search vector hash remains `4888ab1d0c6d7bc044f4d1e8725838b0d533dddeda0f672905274f5a94403522`; Worker allowlist hash remains `0ca755ad269f0a1d604c5602dea6ad81f5301fa4f8c59d5906e9dee1790b6401`.
- Reference workstation: Windows x64, Node v24.19.0, Intel Core Ultra 7 165U, 14 logical processors. Synthetic full shape: 155 people, 402 passages, eight aspects, 1,024 dimensions, five warmups and 30 samples. **p95 matrix 32.39 ms; group calculation 187.47 ms**, maximum 84,185 coverage evaluations against a 300,000 bound. Eight options in this constructed pool. This meets the workstation pure-compute targets with limited headroom; it is not a physical-device/browser pass.
- A separately hydrated synthetic one-scope/155-passage package measured **725,207 raw bytes**, 138.99 ms cold hydrate+matrix+group, cached view/options p95 0.67 ms over 50 samples. Local gzip was 6,397 bytes because the fixture is highly repetitive; **do not use this as a real asset-transfer estimate**. Observed heap delta 20,935,104 bytes includes later harness/GC allocations and is not a browser-heap acceptance result.
- The prior conservative 150-scope envelope of 8,649,994 raw bytes exceeds 8 MiB by 261,386 bytes. Actual 150-scope encoding/wire size, 48 MiB extra-browser-heap target, long tasks, cold 5-second network target and physical-device latency remain unmeasured. The loader's 8 MiB safety cap is not evidence that a complete real package fits.

## 4. Examples and failures

All new examples below are anonymous synthetic contracts, not judgments about actual researchers:

1. A method-transfer fixture admits an evidenced operation/context match despite weaker topic similarity. It keeps an unconfirmed evidence state unless an exact reviewed relation receipt supports the source/claim link. Departmental difference contributes nothing.
2. Generic or unrelated profile overlap fails the absolute topic/method gates. Duplicate and verbose generic text cannot buy extra utility; duplicated aspect weights conserve their source-group budget. Candidate exclusion persists through options and edits.
3. Empty/one-person/poor-relevance pools produce a valid no-group outcome with the existing individual additions and Team Match path. Missing receipts, stale sources, retired claims or bad vector spaces instead fail preparation; those conditions are never counted as scientific no-group decisions.
4. Full 155-person fixtures retain every admitted person either selected or reachable, including filled-role/unconfirmed cases. Eight real alternatives are returned only when present; smaller pools return fewer without padding. Four occupied slots still use the existing remove-before-add interaction.
5. The first large optimizer prototype exceeded its 300,000 work bound. The corrected algorithm retains every person's anchored greedy start, then bounds improvement passes to eight starts per size. Subsequent group timing remained about 500 ms; arithmetic hot-loop correction reduced the measured p95 to 187.47 ms without changing floors or removing candidate reachability.
6. Independent Python/JavaScript comparison exposed floating-point disagreement exactly at the .03 marginal boundary. Both now use a documented one-millionth quantized comparison; 32 reference cases pass. No semantic threshold was lowered.
7. A source-inventory prototype initially treated rollout ID strings as records, producing an invalid count. It was corrected by resolving the actual source manifest records before the retained 227-source receipt was written. This did not inspect recommendations or redraw manifests.
8. Stricter original-source validation made one old unknown-state fixture fail earlier at receipt inventory. The fixture now isolates the intended enum failure. The earlier 73/74 Node output is preserved in `stage2-node-before-fixture-correction.txt`; the corrected combined run is 74/74.
9. The bounded final accounting audit found unenforced preparation/retry sublimits and a usage-overrun receipt that could be lost. Both were repaired together: phase/token/dollar/attempt limits are enforced, unexpected actual usage is retained with a stop marker, and subsequent reservations fail. Regression tests use temporary ledgers; the real ledger has no requests.
10. Real source validity, paid execution, semantic superiority, judge reliability, actual browser transfer and source-group final sealing remain unresolved. They are the first real-readiness boundaries; passing synthetic vectors or a cached historical model answer cannot bypass them.

## 5. Interface and privacy

The nine presentation functions are byte-identical to the Stage 1 checkpoint: panel shell, member card, role row, state label, proposal, child choice, unavailable, failure and Team Match link. The other 36 frozen files retain their hashes. HTML normalization proves only content-hash changes and the declared directory-loading call changed. Markup, styles, fixed wording, controls, drawers, focus/accessibility behavior and workflows were not redesigned.

Focused DOM fixtures cover confirmed/direct/method-transfer/unconfirmed labels, child ownership, empty-group/manual paths, all-admitted reachability, fewer/eight options, full slots, additions/removals/restoration, stale retries, corrupt/mixed packages and expiration between actions. They execute the actual lazy loader and panel with intercepted static assets. They are **not a real browser, full visual parity, E2E, Playwright or accessibility audit**.

Numerical scores cannot set direct proof or clear unconfirmed roles. Exact source/claim relation receipts are required for confirmed coverage; profile “direct” labels alone do not suffice. Explanations quote actual maximizing profile passages and retain uncertainty. Source/schema fields are allowlisted; identity, lengths, hashes, original spans, ownership, revisions, vector norms/roles/canaries and URLs are validated. Private vectors stay in compiled snapshot storage; raw private caches, reviewer notes and provider responses are absent from public ingredients. No real profile was edited.

Startup/ordinary search and directory-only loading do not load or calculate teams. Prepared-scope cold load, build, edits, retry/error, cache and stale-response fixtures permit only bounded static assets and record **zero provider traffic**. The runtime has no model, embedding or reranking dispatch path. Production browser-network confirmation is still unrun and is not inferred from these tests.

## 6. Resources

### Single authorized ledger and finite execution plan

Authorization identity: `on-demand-team-offline-v2-20260909`. Durable file: `budget-ledger.json`. The same ledger must be restored across sessions and providers; a missing checkpoint fails closed. Actual Stage 1 spend **$0**; actual Stage 2 spend **$0**; cumulative **$0**; outstanding/uncertain reservations **$0**; provider requests **0**; input/output tokens **0/0**; paid cache hits **0**. **All $10 is unspent, including at least $4 preserved for Stages 3–4.** No credit or old Sonnet balance is added.

| Planned work, including retries | Request/token bounds | Conservative maximum |
|---|---|---:|
| Stage 2 missing embeddings, after exact reuse and actual source validation | Voyage 4 lite; <=3,840 unique rows, <=80 attempts including four canaries, <=10M input tokens; <=128 rows/batch and at most two attempts/batch | $0.20 |
| Stage 2 single-judge development packets | <=350 requests; <=1,433,600 input /179,200 output tokens in aggregate | $5.376 |
| Stage 3 single-judge final packets, only after separate approval | <=260 requests; <=1,064,960 input /133,120 output tokens in aggregate | $3.9936 |
| Stage 4 planned metered work | 0 requests | $0 |
| **Whole remaining plan** | <=690 provider attempts, including 610 judge attempts | **$9.5696** |

Stage 2 planned maximum is **$5.576**, below its cumulative $6 ceiling. The $0.4304 difference from $10 is unallocated inside the same ceiling, not another allowance. No extraction LLM, paid reranking, optional fitted-model campaign or paid click/smoke fallback is planned.

Prices checked September 9, 2026: Anthropic Sonnet 5 base input/output $2/$10 per million; conservative input reservation $2.50/M covers the existing higher input accounting path. Voyage 4 lite input $0.02/M. Sources: [Anthropic pricing](https://platform.claude.com/docs/en/about-claude/pricing), [Voyage pricing](https://docs.voyageai.com/docs/pricing). No cache or free-credit discount is assumed.

Each judge packet has <=12,000 conservative serialized input tokens/bytes including prompt/schema/framing and <=512 output tokens. It reserves up to $0.03512; the aggregate allocation averages <=4,096 input tokens per maximum planned request, not 12,000 for every packet. Actual packet fit remains unmeasured. Larger substantive evidence consumes the same fixed phase envelope. If useful evidence/output cannot fit, report the missing denominator and defer; do not crop constraints, shrink to five favorable calls, expand the cap or claim execution. Ten global transport/format retries and ten predeclared order swaps per phase are included; no semantic reruns until favorable.

Reuse audit found 41 overlapping historical ledger snapshots, never summed. The latest inspected old-task snapshot `34402343578` records 543 requests and **$11.971342 charged under economical-ai-20260908**. This is historical attribution, not an invoice for this experiment, refund or extra credit. Its 481 cached response keys are metadata-only reuse evidence and incompatible with the new E1 judgment event. No account-wide invoice was read; no experiment requests exist in the inspected old identities.

### Evaluation and human provenance

The fixed primary offline judge is `claude-sonnet-5`, E1/B1 v2 prompt/schema/transport hashes frozen in the candidate. It receives original bounded source/profile evidence, hidden algorithm IDs/scores/prior verdicts, deterministic balanced ordering and deduplicated exact items with external occurrence maps. Contribution labels are strong/plausible/unrelated/insufficient-information; complete-team comparisons permit ties/unresolved. Explanation faithfulness is separately judged. Exact cached judgments are retained once.

Planned maximum slots across development/final phases: 1,800 individual, 540 complete-group, 180 matched-size comparison, 90 source-only, 60 control and 60 explanation slots. Unique items are unknown until actual outputs can be deduplicated. The explanation sample is explicitly 30 source-selected cases per split. These are neither executed labels nor independent-call counts. Original source/evidence feasibility remains separate from the algorithm's willingness to produce a group.

No aspect-generating LLM is planned, so the judge does not share a newly used extraction model. It still shares a family with historical Sonnet work; separate context and a different purpose do not guarantee independence. Any later same-family extraction would require disclosure, not an automatic new provider.

Marc remains the sole confirmed human reviewer. Requested/completed/skipped **0/0/0**. Remaining allowance is **20 development +20 final**, each 12 individual and eight compact team-comparison items. The existing evidence-first packet format and source-only preselection are retained. No recommendation packet is requested now because no real outputs are ready; missing judgments do not block safe code implementation or count as review.

Assess judge agreement using Marc's hidden-first confusion table, reasonable/unrelated disagreement counts, unable-to-assess and all-requested denominators, small-sample uncertainty and systematic method-transfer/generic-evidence error slices. The ten fixed order swaps retain conflicts as unresolved. None of those agreement analyses has an observed denominator yet. Ordinary in-task reasoning is not a separately executed judge receipt. No model or human labels were manufactured; any future machine training labels remain noisy development data, not expert ground truth or independent final validation.

## 7. Production and risk

Production mutations: **none**. No push, PR, duplicate review, merge, deployment, workflow dispatch, permission change, subscriber mail or real-profile test edit occurred. No old Sonnet service was enabled and no historical result/ledger was overwritten. Existing old-task workflows were inspected, not repurposed.

Live remains the existing legacy package: 107 legacy scopes, including 16 needs-revalidation states; zero new-engine live scopes. The existing partial-production-restored/source and Cov4 service states remain as recorded; team scope-repair qualification remains pending. The live candidate above remains the rollback reference. No live rollback or authenticated Worker equivalence test occurred.

The experimental v2 route is implemented but not activated in the real static index. Local dependency-hash edits are not a production release. Any necessary future push must re-audit workflow triggers first; this checkpoint requires no push. Required exact-head review/protected gates are not claimed to have run.

Principal remaining risks are unavailable source receipts/compatible vectors, insufficient original evidence for some profiles, unfinished grouping/coherence audit, lack of real development superiority and judge/human reliability evidence, potential real 150-package size overflow and untested physical-browser behavior. The arithmetic floors are uncalibrated engineering constants. The >12-person optimizer is bounded rather than globally exact; its work limit produces an explicit error, never a silent partial “best” claim.

## 8. Decision and next step

**DEFER Stage 3 and usefulness/deployment claims.** Preserve this Stage 2 engineering checkpoint and its full balance. The primary engineering finalist is **B: fixed aspect-level relevance plus weighted coverage**, version `coverage-v2.1`, adapter `ingredients-v2.1`. No deployment fallback is nominated; whole-call A remains the mandatory simpler comparison baseline. Optional learned/Bayesian scoring and MMR are excluded for lack of development evidence.

Frozen parameters: topic .40, core .30, evidenced method/context .50/.50, anchor .50, absolute group .55, final marginal .03, near-best .95; relevance weights aspect/core/lexical .50/.30/.20; MMR 0. Central aspects share .70 when supporting aspects exist, optional total <=.10, with conserved source-group weights. Sizes 2–4; <=8 alternatives; <=200 people, <=8 aspects, <=16 passages/person; <=300,000 coverage evaluations; exhaustive pool <=12, full-directory greedy starts plus <=8 improvement starts/size otherwise. Stable ASCII IDs and one-millionth threshold/tie quantization. Bounded caches: 1 snapshot, 8 matrices, 32 option contexts, 1,600 row computations. Exact dependencies and no real ingredient identity are recorded in `stage2-candidate.json`.

The smallest prerequisite is a separately authorized narrow trusted preparation/evaluation route for public packets and this ledger; no main change is authorized by this report. Before opening holdout, original-source/coherent-scope/grouping validation, compatible real ingredients and the missing development A/B evidence must be completed under the same finite budget. A real compact development human packet can then be requested within the remaining 20 slots. If this work cannot fit the cap or permitted route, preserve missing denominators and defer. Do not disguise incomplete Stage 2 evaluation as independent Stage 3 validation.

Proposed Stage 3, only after explicit approval and those readiness conditions:

1. Verify frozen code/parameters/source-group/registry/vector/judge identities and unchanged source-based 50/150 routing manifests. Resolve original-source validity before scoring; no result-driven replacement or holdout tuning.
2. Evaluate the frozen primary B versus A on all 90 held-out scientific reservations and 30 derived controls, within the reserved <=260 judge attempts/$3.9936. Keep the 20 preselected final human items evidence-first and separate from model labels; do not replace skips. Stage 4 plans zero metered calls.
3. Apply the unchanged relevance/usefulness/yield targets and paired complementarity criterion, with all-case and independent-feasibility denominators, uncertain cases, explanation failures and clustered intervals. A failed or inconclusive primary stays failed/inconclusive; there is no new tuned winner or automatic fallback promotion. Identity, privacy, source integrity, scope exclusions and paid-interaction failures are blocking regardless of semantic scores.
4. Under that stage's explicit dedicated authorization, run real integrated browser/E2E/Playwright/accessibility, cold/warm/corrupt/stale network checks, physical/resource checks and both 50/150 configurations plus offline rollback. Do not treat this Stage 2 DOM suite as that gate. Any changed finalist needs user-reviewed handling of its independence, not rebranding old evidence.
5. Report exact completed and unrun evidence, actual remaining ledger/human allowance and a Stage 4 recommendation. No production work until separately approved.

**STOP FOR USER REVIEW.**
