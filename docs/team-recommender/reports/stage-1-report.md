# Stage 1 report — on-demand team recommender

**Recommendation: REVISE before approving Stage 2. Stage 1 is complete as an inspection/prototype/report task; no later stage has begun.** The existing interface can present the required proposed views. Approval should resolve the precise nonvisual panel currentness seam and the separate trusted preparation/evaluation prerequisite. The source manifests are provisional until source/coherence/grouping audit; no recommendation-quality pass is claimed.

## 1. Identity and authority

- Repository verified: `https://github.com/mporosoff/grants-scraper`; remote `origin`. Execution-time protected main was fetched and checked via the GitHub branch API: `6adca6a795d4ab52e216e2ff7032d0e393db76b1`; tree `9a2011915aac09437a1d5a2215474a4e68091224`. It remained unchanged at the later read. No open PRs were returned. `protected=true` was observed; the legacy branch response alone is not a complete ruleset audit, and no protection settings were changed.
- Experimental branch: `codex/on-demand-team-recommender`. Actual checkout and plan path: `C:/Users/Marc Porosoff/projects/grants-scraper/.worktrees/on-demand-team-recommender/docs/team-recommender`. Exactly one worktree owns that branch; it was newly created from fetched main. This report's evidence-capture HEAD is `6adca6a795d4ab52e216e2ff7032d0e393db76b1`, tree `9a2011915aac09437a1d5a2215474a4e68091224`; runtime code is unchanged. A subsequent documentation-only commit may contain this report; its identity is in Git history, not substituted for a tested runtime SHA.
- Starting checkout: `C:/Users/Marc Porosoff/projects/grants-scraper`, branch `codex/freeze-nofo-e2e-fixture`, HEAD `dabaed933b71fd2572403371ae6c94090ed7cbe9`. Its unrelated untracked `.worktrees/`, planning documents, `docs/team-recommender/`, and `outputs/` remain intact. All 56 registered worktrees were read under the owner account: nine had untracked entries, none had tracked edits at observation. The separately owned local `main` checkout was `099c29d709010ee42f376b7ba2666d912a7848c0`; it was not reset or used as the execution base. Receipt: `receipts/worktree-status.json`.
- Read the real complete supplied PLAN v2.0 (64,283 bytes), HANDOFF and applicable AGENTS. Only PLAN/HANDOFF/PLAIN_ENGLISH existed in the starting package. The promised original stage prompts, report template and checksum file were absent; none was invented. Only the three nonsensitive supplied documents were copied into the isolated checkout before new Stage 1 documents were written.
- Original PLAN SHA256: `ae0a872aca1826d7ba0322b464e6f1373e0b45f2aa0b6c6cf710a96a71ce8dd3`. Amended PLAN SHA256: `d2b22b76732b246f46d76d6361cd5eb91ee06e97a31de5219f7e51b6befc4a90`. During this task the user explicitly supplied evaluation amendment E1. Original documents and superseded draft annotation/budget are preserved in `history/`; precise supplied-document changes are in `receipts/*-E1.diff`. Active authority is PLAN v2.0 plus E1 and the user's Stage 1-only limits. E1 is incorporated into PLAN, HANDOFF, summary, protocol, packet and the new limited `prompts/EVALUATION_AMENDMENT.md` supplement. Only affected evaluation provisions were changed in the supplied documents.
- The starting and protected-main AGENTS differ on review convergence. Both were read and hashed in `receipts/authority.json`; direct user Stage 1 limits take precedence. No AGENTS, permission, workflow or global configuration was edited. No PR/review was requested or pending for this experiment.
- Public live candidate observed at 2026-09-09T22:50:34.244916+00:00: `5927df0f5835c5a06df60d6bedea9667c9aec76994174452e01fa15a2e1642f0`; original generation SHA `05cec746d1c1f2e9c27ee32f34fcea7415bd7c8a`, run `34360404301` attempt `1`, assembly SHA `d8533c5f6391eda0a49c1963bdfbf71eb7651492`. This is distinct from execution-time main. Live `release/candidate.json` SHA256 `aa52b80f5fa6f403b822148f352082d79be5d4ac9483a015c62aa640caba71ee` matched the local published receipt. `release/candidate-source.json` was not public (404); no provenance was fabricated for that path.
- Live search release SHA256 `d7b284d5ef14ed4d0e444bd82b02533b1b72b9f091e9558213c85f487ee656c8`; team generation `07f1b0b86c534b64ddd8e3dc4305c1e36d1f6652604581e48edfb5b6fdab8453`; registry generation `59ccfbe8999eeb4c78441e16daca468c694fc5798b03587c72468390939dc96a`. Six selected public assets/receipts were independently fetched and hashed; engine/panel bytes match this checkout. This is bounded package identity inspection, not complete authenticated Worker equivalence or a deployment validation. Receipts: `live-read.json`, `live-candidate.json`.
- New ingredient/model identity: no prepared v2 package, no claim/aspect vector generation, no fitted scorer. Proposed embedding `voyage-4-lite` and offline judge `claude-sonnet-5` are specified but unexecuted. No paid approval exists. Report generated 2026-09-09T23:11:12.822127+00:00.

## 2. Work completed

**Inspected:** all engine consumers, static v1 schema/index relationship, registry eligibility and retained claims, live package, currentness, provider/caching boundaries, historical exposure metadata and workflow triggers. Team Match shares `loadData` for its directory; search uses the eager index for Build a team availability, team-only filtering and export selection. The directory's 12-result search limit is not the optimizer's separate hidden top-12 restriction.

**Implemented only as Stage 1 probes/documents:**

- `probes/seam.test.mjs`: actual unchanged panel with synthetic mock views, and diagnostic fixtures against the actual v1 engine.
- `probes/math.test.mjs`: hand-declared numerical relevance/coverage examples; no fitted semantic model.
- `probes/numerical-cost.mjs`: full-155-person anchored search cost kernels using anonymous synthetic vectors, with zero source/person recommendation outputs.
- `probes/inventory.mjs`: source-only counts/grouping, split and source-based rollout reservations. No team calculations on real evaluation scopes.
- `manifests/interface-freeze.json`: 40 tracked presentation/dependency files hashed. `source-groups`, `development`, `holdout`, `rollout`, `historical-exposure`, and `human-audit` manifests; schema proposal; finite evaluation protocol and judge prompt/schema; human packet/template; budget/execution proposal; report/state and receipts.

**Executed:** 30 existing focused Node/browser contracts, 10 seam probes, 4 numerical examples, 30 focused Python tests, existing vector-shape/finite/norm checks, source manifest assertions, byte comparison, and the cost probe. All completed checks listed below pass after the recorded probe fixes. These are not full browser/E2E/Playwright suites. No external automated code review, model judge, human judgment, fitting, production adapter, rollout activation, merge or deployment occurred.

**Intentionally unchanged:** runtime/presentation/assets, canonical data and old results, old Sonnet qualification ledger/gate, source/Cov4 configuration, general search, private/manual profile handling, alerts, Team Match semantics, release owner, Workers and permissions. Only `docs/team-recommender/**` is added/edited on the experimental branch.

## 3. Results

### Real inputs and prepared-data gaps

| Observation | Measured value / qualification |
|---|---|
| Catalog parents / current under existing rule at `2026-09-09T22:44:26.317Z` | 1422 / 1398 |
| Indexed publication-eligible children / children with current parents | 223 / 223 |
| Source-screened scientific candidates / conservative automatic groups | 1007 / 904; provisional, not independent scientific validation |
| Public registry | 158 people; 155 auto-proposable (120 main, 35 standby); 3 reference-only/unrankable |
| Retained claims | 433 total; 427 active eligible claims; 402 normalized exact text rows within people; dedup must retain all IDs/revisions |
| Claim quantity / text | min 1, median 3, max 9 per eligible person; 14,321 characters across deduplicated passages |
| Evidence levels among active eligible claims | 289 direct, 62 corroborated, 76 administrator-reviewed; these are not labels for new aspect links |
| Existing legacy team projection | 107 scopes / 75 parents; 16 needs-revalidation, 82 proposed, 9 no explicit review_state; 443 roles, all with URLs/rationales, 403 with source_quote |
| Search vectors | 1,629 × 1,024 Float16 document rows; 1,406 parent / 223 child rows; finite, nonzero, byte/shape checks pass |
| Compatible new claim / aspect vectors proved available | 0 / 0; no direct claim rows, Float32/query/aspect provenance differs |
| New ready inputs / evaluated groups | 0 / 0; no fake empty member records to satisfy v1 |

Search vector fingerprint is `8c080778811e5c47d012f0d5db1f0d9c8cac63ae604eb9a12e7892202095f06d`, vector hash `4888ab1d0c6d7bc044f4d1e8725838b0d533dddeda0f672905274f5a94403522`; the manifest explicitly has `reuse_permitted=false`. Same dimension cannot authorize mixed caches. No `.vectors.json` files were found in the experiment/default starting/old economical-release cache paths inspected; no remote private provider caches were downloaded or assumed reusable. Existing public profile phrases are often very short; no operation/context descriptor or expertise claim is invented to compensate.

### Source corpus and rollout

Each split reserves 90 scientific candidate scopes from 90 parent groups plus 30 origin-bound synthetic/edge control slots. Total: 240 case slots, 180 independent parent groups under recorded grouping, 60 derived controls that do **not** add independent-call count. Group overlap and historical-exposure overlap in holdout: zero under the recorded grouping. All twelve old confirmations are retained as exposed history; they are not fresh holdout evidence. Source coherence, subtle successor/near-duplicate ownership and explicit conditions still need the bounded offline source audit. Until then the manifests are provisional reservations, not a fully validated 180-positive corpus. Control materials are planned, not all executed.

The 150-scope reservation has 100 parent scopes and 50 children across 103 actual parents. Child distribution: parent 344592 has 37, 345241 has 9, 361526 has 4. Eight automatic source-family buckets are present; scientific family coverage is not yet verified. First cohort: 30 parent scopes + 20 children = 50. Reserve order is deterministic and source-based, not selected for favorable groups. Source closure/unsupported structure must cause an audited reserve substitution or honest shortfall. Current corpus/rollout union is 227 source IDs. Fifty rollout IDs overlap sealed holdout; **Stage 2 must not score them for demonstrations**. Preparing source inputs is allowed; inspecting their recommendations is not.

### Engineering measurements

Existing directory: 388,824 raw / 38,732 locally gzipped bytes. Existing lazy legacy teams: 1,012,069 raw / 167,858 gzipped bytes. Eager team index: 10,817 raw / 1,128 gzipped bytes. These are measured local encodings, not browser observed wire transfer.

New v2 Float32 payload estimate: 5,947,392 bytes at 150 × (6 aspects + core) plus 402 profile rows; 7,176,192 bytes at the eight-aspect maximum. The measured maximum metadata-shaped serialization is 1,084,978 bytes; with current directory and uncompressed vectors the conservative envelope is 8,649,994 bytes (about 8.25 MiB). Placeholder metadata compression is not used as a realistic transfer claim. Existing long source notices/search index are not copied into this bundle. Actual prepared aspects and vectors are missing; actual wire bytes/heap still require measurement.

The 8 MiB transferred/48 MiB extra-heap targets remain visible and unchanged. The conservative uncompressed maximum exceeds 8 MiB by 261,386 bytes: normalized/deduplicated source references and actual HTTP compression must prove the target, or report a stage-boundary revision. No removal of legitimate candidates or semantic source requirements to make the number pass. Ideal 20 Mbit/s + 150 ms transmission lower bound is 3.61 s before decode/render; this is not a measured 5-second cold-click pass.

Reference: Windows x64 Node v24.19.0, Intel Core Ultra 7 165U, 14 logical processors, available developer workstation. Synthetic cost run: 5 warmups + 50 measurements, 155 researchers, 402 passages, 8 aspects, 1,024 dimensions, 3,293,184 dot-product multiply-adds. p95 matrix kernel 4.88 ms; p95 anchored group kernel 44.62 ms; p95 replacements 0.091 ms. About 212,351 coverage evaluations including replacement measurement. The initial probe also measured 6.55/58.37 ms matrix/group p95; the recorded later run followed corrected source metadata serialization. No recommendation quality is inferred from either.

Typed vectors for the single-scope probe occupy 1,683,456 bytes; matrix 9,920 bytes. Observed process RSS increased by 11,587,584 bytes and JS heap by 1,490,544 bytes; Node/GC measurements include harness effects and exclude a real loaded 150-scope browser package. No physical phone, browser long-task, decode, network throttling or browser heap observation was made. Synchronous selected-scope calculation looks practical on this workstation; acceptance on the declared midrange/browser reference remains unproven.

### Checks and quality denominators

- PASS: 44 focused Node contracts (30 existing + 10 seam + 4 arithmetic), 30 Python contracts; exact receipts under `receipts/`.
- PASS: 40 frozen-file hashes unchanged; 240-case grouping/reservation assertions; 50 subset of 150; zero known historical holdout overlap; 40 human slots (12 individual + 8 group per phase), all unrequested; JSON/budget arithmetic checks.
- PASS as **diagnostic reproductions**, not fixes to production: v1 empty-group rejection, gap/adjacent candidate suppression, filled-role candidate trap, three-option cap and lack of action-time clock validation.
- NOT RUN: real recommendation yield, useful/unrelated rates, all arms A/B/C/D semantic comparison, calibration/Brier/reliability, held-out estimates/intervals, real judge reliability, actual human agreement, optimization-loss/exact-reference/cross-browser parity for the future engine, full integrated browser/network/memory checks. Their denominators are zero evaluated cases, not 240 successfully evaluated cases.

## 4. Examples and failures

1. **Useful-transfer illustration (exposed legacy evidence, not new validation):** `344592:ab-0013`, Synthesis and Processing of Materials, retains Kathryn Knowles (`urh-000082`), claim `urh-000082-c002` revision 1, with the public phrase “VO2 nanocrystal synthesis and phase behavior” and a method-transfer link. This is a plausible scientific discussion example under the new purpose; it is not a newly human-confirmed exact-role/facility claim. The exact source/claim receipt and a second legacy example are in `legacy-development-examples.json`; no reviewed flag is carried into a changed aspect without a new exact relation check.
2. **Excluded unrelated outsider:** the hand-declared optical-measurement fixture excludes a synthetic tourism-only profile through low relevance/generic overlap and honors an explicit exclusion even at high similarity. This validates numerical gating behavior only. No actual researcher was semantically graded or excluded by a new model in Stage 1.
3. **No-group:** a uniformly poor two-person numeric pool fails absolute relevance floors; one excellent researcher fails the two-person minimum; a redundant third member fails final marginal contribution. The unchanged panel renders zero members with “Insufficient internal role coverage,” still offers individual additions and Team Match handoff. The actual v1 validator rejects an empty members array at `validateData`, the first failing boundary. Versioned v2 translation is required.
4. **Discovery reachability:** the actual v1 `candidatesForRole` and `proposalView` suppress adjacent/gap candidates and only inspect unfilled roles. Mock v2 views prove the same dropdown can expose every admitted remaining person even when modeled roles are supported; remove a fourth member to reveal choices as today. This does not mean the actual engine has been fixed.
5. **Option/clock conflicts:** `proposalOptions` prunes each role to 12 candidates and caps returned options at three; the renderer can render eight and resolves a stable clicked index in the probe. Panel `current.now` is captured at open; later actions do not recheck currentness and lack a catch boundary. New engine exceptions alone would leave stale actionable UI. Exact proposed orchestration edits are in experiment-design; no visual change is requested.
6. **Source-manifest audit correction:** an initial grouping key stripped all URL queries, falsely merging distinct solicitation URLs. It now preserves meaningful query parameters and only removes tracking fields; year-neutral title grouping is separate from claim-text dedup. Grouping manifests were regenerated before any semantic labels/results. This is a consequential source-only probe defect, repaired before sealing proposal receipts; finer source audit remains pending.
7. **Probe/environment failures preserved:** initial renderer test import traversed one directory too far; fixed and 10/10 passed. Toy gate returned undefined for absent descriptors; explicit Boolean conversion fixed it and 4/4 passed. One metadata print used Windows default decoding and failed; UTF-8 read corrected it without data changes. Sandbox Git ownership checks blocked reads of existing worktrees; read-only owner-account execution verified all 56 without safe-directory/global permission changes. CPU CIM read was denied; Node OS metadata supplied the declared reference. Public candidate-source GET was 404; actual candidate.json was retrieved.

## 5. Interface and privacy

Forty frozen files remain byte-identical. The 10 seam tests execute the actual unmodified panel with mock data: direct/method-transfer/unconfirmed labels, empty groups, eight stable options, all-covered candidate reachability, four-slot remove/add/restore, no-group handoff and failure/retry. Existing focused tests execute child choice/ownership and closure/late-load behavior. This is contract/DOM evidence, **not** real-browser visual parity or proof of layout at all widths.

Permitted future data/content changes: scientific ordering, real explanations/source passages, bounded supported options and truthful evidence mapping. No DOM/CSS/fixed wording/control/focus redesign. Keep all presentation functions unchanged. The precise proposed exception is nonvisual orchestration in `resolveCurrent`, `loadCurrent` and existing variant/remove/add/retry event branches in the panel: fresh action clock and generation tokens, eligibility check and existing unavailable/error renderer on failure. Stage 2 acceptance of this seam is requested at this report boundary, not implemented here. Team Match shared directory loading must remain compatible and survive optional ingredient failures.

Relevance never sets directEvidence/filled/complete. Registry “direct” claims cannot by themselves certify a new contribution. Unconfirmed admitted people use alternative IDs with false proof flags. All IDs/claims resolve to one eligible registry snapshot. Quote/link escaping stays in existing renderers; new loaders must validate schemes/hosts, hash/length, finite norms, source ownership and revisions. No private profiles/CVs, labels or reviewer notes go into public ingredients. No user messages to others, private documents, credentials, behavior logging, provider caches or private selections were read/copied into this package.

Provider traps reject fetch/XHR/WebSocket in the new renderer fixtures; cold mock load, error and retry record zero provider attempts. Actual legacy static-loader contracts were also exercised. These do not replace future integrated browser-network proof of cold/evicted/corrupt ingredient paths. No paid fallback is designed. Source/registry changes need atomic snapshots and layered cache invalidation; options must rekey when competitors change, even on other scopes. Rollback remains offline Stage 2/3 work.

## 6. Resources

**Actual Stage 1:** zero provider/model/embedding/reranking/judge requests, zero provider tokens, $0 charged/authorized, zero provider cache hits. This excludes the ordinary assistant conversation itself; no application provider API was invoked. Static public HTTP/GitHub metadata/documentation reads and local computation are recorded separately. Actual new human/model judgments: 0/0; Marc confirmed availability/policy, not a relevance label. Human requested count remains 0 of 40.

**Amended evaluation:** one fixed proposed offline Anthropic Sonnet 5 judge in an evaluation context separate from implementation, with original call/profile evidence and no algorithm names/scores/prior verdicts. Explanation faithfulness is a separate request. Four relevance labels, ties/unresolved cases, deterministic balanced ordering and a 20-development/20-final order-swap audit. Conflicts are retained, never rerun until favorable. Compare with Marc via small-sample confusion/disagreement tables and uncertainty; systematic false expertise/constraint/transfer failures are explicit. No claim that a second model or agreement with machine training labels proves independent human preferences.

**Human allocation:** Marc only; maximum 20 development + 20 separately preselected final items for the whole experiment. Per phase: 12 individuals and 8 complete-team comparisons, not dozens of hidden subjudgments. Evidence first, verdict hidden until Marc answers; unable-to-assess and every requested skip consume a slot, no replacement. Entire estimate 112–168 minutes if all 40 are requested, not hundreds of ratings. `human-audit.json` contains unrequested source-only slots; the final audit remains sealed. No recommendation-labeling request was made.

**Proposed single stage-spanning budget:** `on-demand-team-offline-v2-20260909` in `preparation-budget.json`: **$69 total**, unapproved, no remaining spendable allowance. Judge subcap $68: <=2,710 initial unique-item slots (including <=330 source-only checks), 40 swapped-order requests and <=275 global transport/format retries, <=3,025 requests total; <=18,150,000 input and 3,097,600 output tokens; <=6,000/1,024 per request. Modeled worst-case at current $2/$10 per million is $67.276, with no cache/free discount assumed. Actual unique items are unknown until outputs exist and are deduplicated; maximum slots are not fake executed labels. Optional C/D or absent output slots remain unused, not repurposed for a wider sweep.

Embedding subcap $1: Voyage voyage-4-lite/1,024, <=96 requests including canaries/retries, <=3 million billed input tokens, <=3,840 unique text inputs; current-rate token ceiling costs $0.06. No extraction LLM, per-person proof pipeline, per-click model or recurring backfill is budgeted. Original public-source aspect work is reused or explicitly source-backed; missing structure remains unprepared. All attempts/uncertain charges need new durable pre-dispatch reservations, exact caches and no reset on resume. The earlier unapproved $1-only draft is historical and superseded, not additive.

Prices were verified against current official [Anthropic pricing](https://platform.claude.com/docs/en/about-claude/pricing) and [Voyage pricing](https://docs.voyageai.com/docs/pricing). A stale search result for the scheduled Sonnet increase conflicted with the canonical page; the current page explicitly says the increase was canceled. Recheck rates and exact payload token bounds before any separately approved dispatch; exceeding a bound defers the item, not permission to crop material evidence or spend more.

**Actual route limitation:** the main-only offline workflow and `tools/offline_ai.py` provide a trusted boundary/ledger/cache pattern, but every dispatch/checkpoint phase is attached to the old experiment. No independent new judge/ingredient route is ready. A separately authorized narrow protected-main prerequisite must add only preparation/evaluation for allowlisted public packets and an independent new budget ledger, no arbitrary branch code with secrets, old qualification dispatch, source refresh/publication or service-state mutation. Stage 1 neither creates that main PR nor dispatches it. Old Sonnet balance is untouched and cannot fund this experiment. Safe untrained development can proceed after Stage 2 approval while the paid route is unresolved; machine-label fitting/semantic judging waits for actual authorization and route readiness.

## 7. Production and risk

Production mutations: **none**. No branch push, PR, automated review, merge, workflow dispatch/cancellation, provider call, deployment, alert/email, real profile edit or permission change. Workflow trigger audit was completed: Tests runs on PR/main; refresh and deploy pushes are main-only; Pages is workflow_call-only; E2E manual dispatch-only. No experiment push was needed. A future push must re-audit execution-time triggers.

Configured protected-main state remains `partial_production_restored`, `automatic_generation=enabled`, `cov4=qualified_enabled_entrypoint_exercised`, `teams=scope_repair_qualification_pending`. The recent old offline run's operational success is not a new qualification/quality pass. Source/Cov4 and historical ledgers/results were not modified or resumed. The live package continues legacy routing: 107 indexed scopes, 16 marked needs_revalidation (91 remaining index entries before currentness), **0 new-engine scopes live**. No full currentness-qualified live cohort count or Worker serving equivalence is claimed from this bounded inspection.

Rollback reference for future offline proof: live candidate `5927df0f5835c5a06df60d6bedea9667c9aec76994174452e01fa15a2e1642f0`, derived from `982d97210d2b161398f9dd60df8e54449f08d90f462e48762804ef53a5a53207`, with public candidate/search/team/registry identities above. No live rollback was attempted. Known-good package receipts are retained in the existing release lifecycle; this task did not revalidate every serving byte or authenticated Worker configuration.

Residual risks: source/near-duplicate/coherence audit pending; children concentrated in three parents; terse public claim text; zero compatible prepared vectors; new v2 validators/atomic cache/currentness orchestration not implemented; no independent human preference calibration; judge family shares historical failure risks; no verified task-specific credentialed route; uncompressed maximum bundle above the provisional transferred ceiling; no browser/device latency/heap measurement. Group quality/yield, method comparisons, holdout results, judge agreement and human audit are **not run**, not passes. Existing failed probe receipts and corrected source-group behavior are retained. No full-suite/E2E evidence from another SHA is reused as new-engine validation. Git whitespace inspection flags preserved Markdown hard line breaks and retained diff/test-output whitespace; those source/history receipts were not cosmetically rewritten. Generated document line endings were normalized to repository LF before content hashes were sealed.

## 8. Decision and next step

**REVISE the execution readiness before approving Stage 2**, while accepting that the inspected synchronous/data-to-existing-view seam is plausible. The concrete revisions are: accept the narrow nonvisual panel orchestration seam; finish source/coherence/grouping audit under the amended finite model-led protocol; and separately decide whether to authorize the exact $69 preparation/evaluation record and its narrow trusted-main prerequisite. Marc's availability is settled; no second reviewer or hundreds of manual labels is a prerequisite.

Smallest recommended Stage 2 scope, only after explicit approval:

1. Implement pure deterministic numerical baseline and version-aware ingredient/legacy routing; preserve `data.faculty` for Team Match, source/child ownership and all existing presentation. Add independent admitted-candidate discovery, <=8 stable options, honest no-group views and exact evidence-state mapping.
2. Implement only the named panel orchestration guards, atomic source/registry/vector manifests, bounded caches, exclusions/restore/currentness and safe URL/vector validation. Exercise focused zero-provider fixtures, deterministic reference/small-pool comparisons, controlled perturbations and actual package byte/decode/compute measurements; no full E2E.
3. Complete original-span/source-group audit without recommendation-based selection. Materialize development controls and prepare required public inputs only through a separately authorized route. Keep 50 rollout/holdout overlaps unscored until Stage 3.
4. Run finite development-only A/B comparisons; C/D and MMR only if their machine-labeled development evidence justifies them. Use the single blinded judge only if its route/budget is approved. Offer at most the preselected 20 compact development human items after real packets exist. Report machine versus human provenance and judge systematic errors. No promises of calibrated human preferences.
5. Freeze one primary and at most the already-counted simple fallback, judge rubric/model/transport/protocol, parameters, group/scope identities and final analysis plan. Return stage-2-report/state and stop before sealed evaluation. If judge quality or paid route is unreliable, preserve the failure and report a baseline-only/inconclusive result instead of expanding providers, labels or loops.

Stage 2, spending, any prerequisite main PR, holdout scoring, final human audit, Stage 3 full-browser work and Stage 4 release are **not authorized by this report**.

**STOP FOR USER REVIEW.**
