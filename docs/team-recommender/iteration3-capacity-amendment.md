# Iteration 3 capacity-only amendment

The owner-supplied September 22, 2026 work order authorizes 200 additional
metered attempts and 100 additional native token counts in the existing pooled
completion program. It adds **no dollars**. The work-order hash and exact prior
authoritative run, artifact, ledger, checkpoint and historical prefixes are
recorded in `config/contextual_team/iteration3-capacity-amendment-v1.json`.
Detailed scientific evidence remains local.

| Dimension | Original completion cap | Amended completion cap | Lifetime ceiling |
| --- | ---: | ---: | ---: |
| Dollars | $50 | $50 | $60 |
| Metered attempts | 600 | 800 | 1,490 |
| Native counts | 300 | 400 | 590 |

The original completion starting checkpoint remains 687 actual request rows and
188 native counts. The capacity installation is bound separately to owner run
35750028417: 718 actual requests, 200 native counts and 55 events. One event is
appended; the request and native-count histories are not rewritten. The original
failed checker, accepted recoveries, successful caches and all failed requests
remain unchanged. Historical unknown holds and Math's $0.713400 / four-attempt /
one-count unknown exposure remain charged conservatively. Math stays permanently
closed to replay. The original $1.267862/two-attempt reserve and new $5 contingency
remain protected.

At that exact checkpoint, installation yields $48.520369 / 765 attempts /
387 counts remaining, with $43.520369 before the protected contingency. This is
capacity, not a spending target or authorization for generic dispatch.

## Trusted installation

After protected review and merge, the existing `team-recommender-offline.yml`
workflow may be manually dispatched from protected `main` with only the
`contextual_check` input `{"iteration3_capacity":"install"}`. No contextual job
or packet input may accompany it. Preparation restores the latest sole-owner
checkpoint, authenticates the pinned predecessor, verifies every checkpoint
file and atomically installs the event with the ledger caps. The existing
`text_provider=none` execution branch receives no provider credentials. Its
reservation and result explicitly report zero new dollars, metered requests and
native count calls.

Installation is complete only after the real trusted run has persisted its state
artifact and a read-only audit has verified all historical files, rows, holds,
effective limits and the zero-provider result. A local test or merged module is
not an installed allowance. Record the new authoritative run/artifact and full
hashes in the local receipt; update CURRENT at the Iteration 3 checkpoint.

The original authority configuration and events remain immutable. Effective
limits require exactly one matching capacity event at the pinned history
boundary. Duplicate/conflicting events, unrecorded cap changes, altered native
history (including on cache hits), another owner or a mismatched restored file
fail closed. An interrupted atomic ledger/checkpoint transition can only recover
the exact authorized transition; it cannot rehash unrelated changed evidence.

This amendment does not enable paid builds, public activation, registry cutover,
legacy withdrawal, recurring paid usage, confirmation scoring or manual E2E.
