# Manual public preparation/evaluation executor

This narrowly authorized Stage 2 prerequisite does not contain the recommender runtime, activate teams, publish a catalog, deploy, enable old Sonnet work or add a schedule. It uses only protected-main code and existing Anthropic/Voyage credentials. User authority: Stage 2 continuation C1 after experimental checkpoint 7ae6f1dce6e655a905ebd7d9d0c915cace1ab48b.

The sole ledger identity is on-demand-team-offline-v2-20260909: $10 maximum through all four stages, cumulative $6 through Stage 2 and at least $4 reserved for later stages. The exact existing zero-request ledger is pinned as the initial handoff. This is a transfer of execution ownership, not a second allowance. Before first dispatch mark the experimental local ledger read-only for spending; all paid work thereafter uses this workflow and its restored checkpoint. Local copies/results are mirrors only. Do not dispatch any independent local/cloud executor.

Push a bounded public JSON packet to docs/team-recommender/packets/HASH.json on the experimental branch. The filename is its complete SHA256. Dispatch team-recommender-offline.yml at main with the full packet commit and hash. The workflow fetches that one data file; it never checks out, imports, executes or obtains endpoints/prompts/models from the experimental branch. The request schema, fixed rubric/model routes, source-ID allowlists, public profile snapshot and all budget limits are trusted main dependencies.

Supported operations are embeddings and development-judge only. Embeddings preserve exact text, query/document roles, 1024 dimensions, float output and disabled truncation. Judgment uses the fixed evidence-first E1/B1 rubric with original source/profile evidence, at most ten clearly keyed items, hidden algorithms/scores and separate explanation requests. No holdout judging is supported. Batching/deduplication and balanced ordering are prepared offline; each actual request and response has a reproducible hash.

The initial seed ledger is restored only when there is no prior run or reservation. Every later dispatch requires the latest full state paired with the latest crash reservation from this workflow on main. Missing/expired/ambiguous checkpoints, untrusted paths, corrupt hashes or unresolved usage fail closed. The concurrency group serializes this experiment. Before any provider step, upload the whole-budget crash marker; each request additionally atomically reserves conservative cost/tokens before dispatch. Unknown requests remain charged at their reservation. No implicit or semantic retry; terminal failures stop the packet and survive reruns. Exact successful cache hits make zero provider calls.

September 10 continuation C2 authorizes two additional focused repair/re-review iterations for persistence and recovery after the recorded C1 checkpoint. The ledger now atomically and permanently claims each logical request key with its reservation and execution metadata. Every prior row, including legacy valid, failed or uncertain rows lacking auxiliary markers, prevents automatic redispatch. There is no automatic second attempt. A complete exact cache is reusable only with its corresponding valid reconciliation and validated result. A reconciled charge without its result remains charged and recovery-required; no lost output is asserted recovered. A reservation interrupted before dispatch is conservatively retained unless an external recovery process can prove non-dispatch; this executor offers no automatic exception. Missing or inconsistent checkpoint hashes stop restoration. Do not delete rows, reset the ledger, alter request bodies merely to evade their identity, or rerun a lost result under a new allowance.

Regression coverage injects crashes before and after the claim, valid/failed reconciliation, cache, terminal marker, receipt and checkpoint writes, including uncertain transport. Three repeated workflow restorations per case make zero duplicate fixture dispatches. Concurrent ledger claims and concurrent executor invocations also retain one dispatch. Failed C1 review findings remain history; only exact-head review and required CI can establish this corrected prerequisite's merge readiness.

Limits: embeddings <=80 attempts/$0.20/10M tokens; development judge <=350 attempts/$5.376/1,433,600 reserved input and 179,200 output tokens. Each judge request <=12,000 conservatively bounded input tokens including framing and <=512 output tokens; count complete evidence, never crop decisive clauses. Existing total/stage ceilings remain additional constraints. A source-selected smaller initial packet is a wiring check, not a replacement for the 90+30 development inventory. No paid extraction or reranking is supported.

Prices verified against official pages on September 9, 2026: Sonnet 5 input/output $2/$10 per million; conservative input reservation $2.50/M. Voyage 4 lite input $0.02/M. No free/prepaid credit or prompt-cache discount is assumed. Recheck prices and actual packet bounds before dispatch. Prices: https://platform.claude.com/docs/en/about-claude/pricing and https://docs.voyageai.com/docs/pricing.

Download the exact run/attempt state artifact into private experimental outputs. It contains the shared ledger, bounded result cache, request usage receipts and a content-hashed checkpoint. It is not a public release asset. Keep raw provider results out of public ingredient bundles; derive only compatible vectors or separately attributed evaluation evidence. Copy the reconciled ledger into experimental documentation only as an explicitly non-spending mirror, retaining artifact/run provenance. No new allowance on rerun, session restart or artifact loss.

Before merge audit production triggers and verify the existing release planner remains no-op/validation-only with these additions. Ordinary PR Python/Node CI and independent exact-head review are required. No E2E/Playwright or protection edits are part of this prerequisite. The new manual workflow alone may run only after protected merge; do not send credentials to PR code.
