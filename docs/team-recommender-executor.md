# Manual public preparation/evaluation executor

## Compact judge output contract (C2 follow-up)

Real runs 34465625293 and 34465998056 exposed two output failures: an invalid verdict/reference and a 512-token response consumed entirely by adaptive thinking. Their reconciled charges and terminal requests must remain unresolved; never replay them after changing the output contract. The executor preflights every request against the restored legacy identities before any new reservation, including when an unresolved item follows a new item in the packet. Preserve failed-item denominators and submit only previously undispatched logical requests.

Sonnet 5 runs this fixed rubric with `thinking: {type: "disabled"}` and the same 512-token cap. Its output schema enumerates the supplied item IDs, allowed verdicts and evidence IDs. Post-response per-item checks remain mandatory. This is one fixed compact judge configuration, not a model sweep, a relaxed validator or an increase in budget. [Official Sonnet 5 behavior documentation](https://platform.claude.com/docs/en/models/sonnet-5/whats-new-sonnet-5), checked September 10, 2026, documents default adaptive thinking and the supported disabled setting. No sampling controls are added. Future data packets cannot select these settings.

This narrowly authorized Stage 2 prerequisite does not contain the recommender runtime, activate teams, publish a catalog, deploy, enable old Sonnet work or add a schedule. It uses only protected-main code and existing Anthropic/Voyage credentials. User authority: Stage 2 continuation C1 after experimental checkpoint 7ae6f1dce6e655a905ebd7d9d0c915cace1ab48b.

The sole ledger identity is on-demand-team-offline-v2-20260909: $10 maximum through all four stages, cumulative $6 through Stage 2 and at least $4 reserved for later stages. The exact existing zero-request ledger is pinned as the initial handoff. This is a transfer of execution ownership, not a second allowance. Before first dispatch mark the experimental local ledger read-only for spending; all paid work thereafter uses this workflow and its restored checkpoint. Local copies/results are mirrors only. Do not dispatch any independent local/cloud executor.

Push a bounded public JSON packet to docs/team-recommender/packets/HASH.json on the experimental branch. The filename is its complete SHA256. Dispatch team-recommender-offline.yml at main with the full packet commit and hash. The workflow fetches that one data file; it never checks out, imports, executes or obtains endpoints/prompts/models from the experimental branch. The request schema, fixed rubric/model routes, source-ID allowlists, public profile snapshot and all budget limits are trusted main dependencies.

Supported operations are embeddings and development-judge only. Embeddings preserve exact text, query/document roles, 1024 dimensions, float output and disabled truncation. Judgment uses the fixed evidence-first E1/B1 rubric with original source/profile evidence, at most ten clearly keyed items, hidden algorithms/scores and separate explanation requests. No holdout judging is supported. Batching/deduplication and balanced ordering are prepared offline; each actual request and response has a reproducible hash.

The initial seed ledger is restored only when there is no prior run or reservation. Every later dispatch requires the latest full state paired with the latest crash reservation from this workflow on main. Missing/expired/ambiguous checkpoints, untrusted paths, corrupt hashes or unresolved usage fail closed. The concurrency group serializes this experiment. Before any provider step, upload the whole-budget crash marker; each request additionally atomically reserves conservative cost/tokens before dispatch. Unknown requests remain charged at their reservation. No implicit or semantic retry; terminal failures stop the packet and survive reruns. Exact successful cache hits make zero provider calls.

September 10 continuation C2 authorizes two additional focused repair/re-review iterations for persistence and recovery after the recorded C1 checkpoint. The ledger now atomically and permanently claims each logical request key with its reservation and execution metadata. Every prior row, including legacy valid, failed or uncertain rows lacking auxiliary markers, prevents automatic redispatch. There is no automatic second attempt. A complete exact cache is reusable only with its corresponding valid reconciliation and validated result. A reconciled charge without its result remains charged and recovery-required; no lost output is asserted recovered. A reservation interrupted before dispatch is conservatively retained unless an external recovery process can prove non-dispatch; this executor offers no automatic exception. Missing or inconsistent checkpoint hashes stop restoration. Do not delete rows, reset the ledger, alter request bodies merely to evade their identity, or rerun a lost result under a new allowance.

Regression coverage injects crashes before and after the claim, valid/failed reconciliation, cache, terminal marker, receipt and checkpoint writes, including uncertain transport. Three repeated workflow restorations per case make zero duplicate fixture dispatches. Concurrent ledger claims and concurrent executor invocations also retain one dispatch. Failed C1 review findings remain history; only exact-head review and required CI can establish this corrected prerequisite's merge readiness.

Limits: embeddings <=80 attempts/$0.20/10M tokens; development judge <=350 attempts/$5.376/1,433,600 reserved input and 179,200 output tokens. Each judge request <=12,000 conservatively bounded input tokens including framing and <=512 output tokens; count complete evidence, never crop decisive clauses. Existing total/stage ceilings remain additional constraints. A source-selected smaller initial packet is a wiring check, not a replacement for the 90+30 development inventory. No paid extraction or reranking is supported.

Prices verified against official pages on September 9, 2026: Sonnet 5 input/output $2/$10 per million; conservative input reservation $2.50/M. Voyage 4 lite input $0.02/M. No free/prepaid credit or prompt-cache discount is assumed. Recheck prices and actual packet bounds before dispatch. Prices: https://platform.claude.com/docs/en/about-claude/pricing and https://docs.voyageai.com/docs/pricing.

Download the exact run/attempt state artifact into private experimental outputs. It contains the shared ledger, bounded result cache, request usage receipts and a content-hashed checkpoint. It is not a public release asset. Keep raw provider results out of public ingredient bundles; derive only compatible vectors or separately attributed evaluation evidence. Copy the reconciled ledger into experimental documentation only as an explicitly non-spending mirror, retaining artifact/run provenance. No new allowance on rerun, session restart or artifact loss.

Before merge audit production triggers and verify the existing release planner remains no-op/validation-only with these additions. Ordinary PR Python/Node CI and independent exact-head review are required. No E2E/Playwright or protection edits are part of this prerequisite. The new manual workflow alone may run only after protected merge; do not send credentials to PR code.


## D1 diagnostic judge amendment

D1 is a new, substantive development question contract. It separately grades source suitability, aspect-person relevance, call-person relevance, group usefulness, matched-size comparison and explanation faithfulness. It uses the unchanged retained registry evidence, labels/types and summaries pinned in `profile-fields-d1.json`; no researcher retrieval or enrichment is permitted. The original prompt, failed requests, body keys, ledger and cache remain intact. A metadata rename does not create a new logical request.

Each D1 request contains at most three items, source-backed aspect substrings, complete bounded source context and exact frozen profile fields. Algorithms, scores and prior decisions are forbidden. Responses have per-item references and concise reasons, with a 512-token limit and disabled adaptive thinking. Oversized evidence fails before dispatch; do not remove decisive evidence to force a fit. Begin with a small paid sanity packet after zero-paid schema checks. No automatic format/semantic retry.

D1 has at most 200 new judge requests, 1,360,000 conservative input-token bounds, 102,400 output-token bounds and $4.424 maximum summed reservations. This replaces no historical record or dollar balance: all earlier charged or uncertain requests still count toward the existing $6 cumulative Stage 2 ceiling and $10 total. At the verified $1.082518 prior spend, even the full new judge envelope plus the existing $0.20 maximum embedding envelope is below $6. Purpose sublimits are additional ceilings, not quotas or allowances. Original B1 requests retain their consumed B1 envelope; D1 must use the corrected protocol. Stage 3 remains unapproved.

Only the existing protected-main manual workflow can execute these public hash-bound packets. There are no workflow, permission, provider, schedule, production runtime or deployment changes. After merge reconcile the exact helper commits into the experiment without importing unrelated presentation changes. Restore the latest paired cloud reservation/state before any new dispatch. Never maintain an independent local spending copy.
# D1 response-format correction, 2026-09-10

Two D1 sanity requests (runs 34494191625 and 34494473553) were charged and rejected by the free-text string-length contract. No accepted result exists. Their terminal requests and costs remain in the single experiment ledger. Do not retry them.

For untouched D1 evaluation items, `protocol: D1F` retains the same scientific questions, evidence, labels and 512-token output cap, and uses a schema-enforced finite reason code instead of a free-text reason. Codes identify specific connection, method transfer, interest, generic evidence, missing information, irrelevance, coherent scope, broad scope, nonresearch, useful overlap, unsupported explanation or a tie. The evidence reference still points to the supplied evidence. A code is a compact rationale, not new scientific evidence.

Both formats reconstruct the counterpart's exact request identity before dispatch. Any previously paid or uncertain counterpart stops execution, in either direction, rather than becoming another paid attempt. Exact successful same-format caches remain reusable. Build a packet containing only never-dispatched items; keep excluded failures visible in the experiment report. No new provider, budget, workflow, production runtime or permission is introduced.
# D2 bounded existing-context revision (September 10, 2026)

The user authorized one further Stage 2 ranking/representation revision. The same authorization `on-demand-team-offline-v2-20260909`, cloud checkpoint owner, $10 all-stage ceiling, $6 cumulative Stage 2 ceiling and 690 lifetime request cap remain. Historical legacy/D1 request, token, reservation and failure records stay consumed. No recommender code, source expansion, researcher enrichment, schedule, permissions or publication is authorized by this helper.

`D2-context-v1` document batches admit only inputs deterministically reconstructed from the existing trusted, hash-bound public registry tables. Associated exact evidence/claim type/label form one compact context input; a nonempty unchanged research summary forms one separate per-person input. No arbitrary branch descriptions are accepted. The model space and provider are unchanged. Context vectors are numerical representations, not new evidence or confirmation of expertise. At most five context requests and $0.02 of conservative reservations are allowed, inside the existing cumulative embedding bounds.

New missing D2 judgments use the unchanged D1F rubric/schema and exact original evidence policy. The allowed purposes are call-person, group usefulness, matched comparison, one bounded swap audit and explanation audit; no new source interpretation or rubric. Changing purpose leaves the provider body/cache key unchanged. Existing exact successful results reuse their cache; paid-failed, cacheless and uncertain requests cannot automatically replay. Reconcile exact item identities offline before batching; do not regroup previously paid-failed or completed items to purchase them again.

The finite D2 judge portion is at most 120 requests, 1,100,000 conservative input units, 61,440 output tokens and $3.3644 in summed pre-dispatch reservations. This is a bounded portion of the existing authorized remaining balance, not fresh dollar credit. Prior consumption is preserved in separate immutable rows and the shared cumulative dollar/request tests. At the verified D1 checkpoint ($2.138302, zero reservations), the maximum additional $3.3844 preserves at least $4.477298 overall. These bounds are ceilings, not quotas. Prepare exact packet counts/hashes before dispatch, restore the latest authoritative paired checkpoint and stop on any unmet limit. No separate local spending copy or Stage 3 execution.

D2 review remediation (PR #221): per-item dispatch claims are now authoritative across differently shaped batches. Whole-packet preflight prevents partial spending when a later item overlaps, and the same identity check runs inside the atomic reservation lock. Embedding identity is exact text plus query/document role within the fixed space. D1/D1F scientific item identity excludes batch IDs and local evidence aliases, retains source/profile/target evidence and comparison side assignment, and is independent of D1/D2 purpose labels. Exact complete request caches still reuse without dispatch; rebatching a previously claimed item fails closed, regardless of valid/failed/uncertain status or missing auxiliary writes.

The hash-only `prior-items-d2.json` index covers 162 historical D1 requests / 332 item occurrences from ledger SHA `23ea6c322f9d555212dec88cebaf0a1f5dd438be27a502c17b5f7acca87f537d`. Each entry reproduces the original public packet hash, trusted body hash and ledger key, including paid failures. `rebuild_historical_index` reproduces this table read-only from that exact ledger and public packet directory. Unknown historical item identities block dispatch; the migration does not rewrite old ledger rows or claim lost answers were recovered.
