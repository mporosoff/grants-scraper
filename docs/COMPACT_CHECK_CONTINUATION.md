# One compact independent check — September 19 continuation

September 21 status: this request reached a terminal provider grammar rejection
in run35613118796. Its full hold remains charged conservatively. The owner's
later relaxed retry instruction is implemented by the separately frozen,
one-request [wire repair](COMPACT_CHECK_WIRE_REPAIR.md) inside this SAME $0.50
ceiling. The original logical request is permanently closed; the historical
instructions and evidence below remain unchanged.

This is the separately authorized continuation of the retained Luna assessment,
not renewed authority for the two historical operations. The existing cloud
ledger `on-demand-team-offline-v2-20260909` is the sole spending owner.
The frozen configuration is `config/contextual_team/compact-check-continuation-v1.json`.

The operation `cb-cc-independent-check` retains all 12 people, all 24 contribution
questions, complete original evidence and the unchanged Sonnet 5 rubric with
12,000 output tokens and thinking disabled. Only native schema representation
differs. The canonical validator and historical contracts are unchanged. The
retained Luna result is checked by hash but is never included in judge evidence.

Before execution, use the release dependency planner and verify current source
availability and active workflows. A main merge that would unexpectedly start
source regeneration remains held; do not disable automation to merge it.
Require clean exact-head review and Python/browser/frozen gates. Only after
protected merge, dispatch the existing **Team recommender offline** workflow on
`main` once, with `contextual_check` set to:

```json
{"compact_check_continuation":"compact-check-continuation-20260919-v1"}
```

Leave all other workflow inputs empty. Record and monitor that same run. Do not
create a second dispatch for a failed or ambiguous request. No new schedule,
service graph, public team, profile, source interpretation, embedding or
registry publication is part of this operation.

Preparation restores the newest authoritative checkpoint using the existing
reservation/state transport. It verifies the complete 685-row history, original
terminal main run, exact receipt and diagnostic, and absence of another active
spending owner. It appends an exact quarantine receipt/event for
`d24a0654d72e486793519bead019b064`. Neither that request nor the earlier unknown
request is rewritten, released or replayed. Their combined $0.351328 remains
included in conservative spending. Other uncertainty still blocks dispatch.

The frozen full request is 58,847 UTF-8 bytes. One input token per byte, plus
20% and 1,024 framing tokens, reserves 71,641 input tokens and the complete
12,000-token output. At the official September 19 Sonnet 5 rates of $2/$10 per
million input/output tokens, the conservative reservation is **$0.263282**.
No native count is necessary or dispatched. This is below the new **$0.50 / one
attempt** ceiling inside the existing $10/690 ledger, preserving at least
$1.267862 and two attempts. Existing global token/provider limits still apply.
If prices/settings no longer match at execution, stop before dispatch; do not
alter the frozen packet or increase the authorization silently.

Reservation is irreversible. A failed, uncertain, or reconciled request with a
lost cache is recovery-required; restoration, a different key, or a repeated
workflow cannot purchase it again. A complete exact successful cache is reused.
The result and diagnostics remain private artifacts, not public release assets.

After success validate all 24 verdicts and derive 12 person outcomes under the
existing rubric, then compare with retained Luna outcomes. On failure preserve
diagnostics, charges/holds and missing verdicts and stop the scientific lane.
Green fixtures and schema equivalence do not prove provider grammar acceptance.
This check cannot establish population quality, explanation faithfulness or
cold end-to-end latency. Public activation and the 107 legacy-package registry
decision remain unauthorized.

## September 21 recovery authorization and evidence-identity correction

Owner instruction after failed preparation run35610297158:

> I am relaxing the no-retry rule. Keep working on this until everything is fixed and all changes are merged and live

This supersedes the earlier stop at that preparation failure and authorizes
continued repair, exact-head review, protected merge and resumed execution.
That run failed before reservation/provider execution and produced no state
artifact. The original cloud owner, both unknown holds and unused scientific
request remain unchanged. This is not a new budget or a reset of paid requests.
The same operation/packet and existing request, dollar and reserve limits apply.
Uncertain dispatches still cannot be replayed or re-keyed automatically.

The corrected guard verifies the retained diagnostic's exact file SHA256
(`9f5cc1a7b775fb4e442545308ca037b2b2481ce6e2b1e6a3be3d38f65457c62b`)
and its canonical JSON identity
(`30cd6ebf824c14c3267e0aea79071c056bb9f67ae5b5c24bd37c572dceea191c`),
which is the identity recorded in the original receipt. Neither historical file
is rewritten. Regression fixtures contain only the bounded failure metadata,
without scientific evidence, final answers, raw provider envelopes or secrets;
their real identity constants are not substituted by the fixture.
