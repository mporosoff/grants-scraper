# Complete-pair checker grammar repair — September 21

This is the owner's authorized continuation of the same finite check. It does
not renew the $0.50 allowance, release unknown charges, replay a paid request,
regenerate the saved Luna assessment, or approve serving experimental teams.
The prior request and its failed schema remain reproducible.

## Observed failure and narrow repair

PR266 fixed the historical file-hash/canonical-identity guard. Its protected-main
run35613118796 passed preparation but received terminal HTTP400: compiled grammar
too large. Request6a6e93add95848dcb02f9ccaf158a2eb remains reserved_unknown at
$0.263282 because no usage was returned. Checkpoint artifact10644862881 is the
new authoritative owner. Its 686 request rows and both earlier quarantine
events are frozen. The exact diagnostic and receipt are checked independently
by raw file hash and their canonical linkage; historical bytes are unchanged.

The new wire contract flattens the 24 fixed person/contribution questions to
required qNN keys. Each question still constrains its own person's claim
addresses in the native schema. Short addresses resolve bijectively to original
claim IDs/revisions. An ordered zero-to-three claim array resolves to the three
original slots. The fixed source reference is supplied by the exact question
mapping instead of generated by the model. No inference or fuzzy reference
repair occurs in this translation. The original complete-pair validator still
checks every resolved result, ownership, duplicate claims and positive support.

All 12 people, complete original input/evidence, 24 questions, scientific rubric,
Sonnet 5, disabled thinking and 12,000 output tokens are retained. Luna's output
is checked for exact reuse but hidden from the checker. No truncation, sample,
batch split, new model or scoring change is introduced. The additional prompt
changes only serialization. The full request is 67,812 UTF-8 bytes; the native
schema is 16,721 bytes. Serialized bytes are not a measure of compiled grammar
complexity. This removes nested claim-slot products and repeated long identity
enums. Local conformance is not proof that the provider accepts the grammar.

The provider's [structured-output documentation](https://platform.claude.com/docs/en/build-with-claude/structured-outputs)
identifies internal grammar complexity and recommends reducing nesting. A native
token count estimates complete input size; it does not establish grammar acceptance.

## Durable authority and accounting

`config/contextual_team/compact-wire-repair-v1.json` freezes exact input, contract,
body, prior diagnostic and retained-assessment identities. The sole new purpose
is `cb-cc-wire-repair`, explicitly linked by repair_of to the terminal failed
request. An exact quarantine event permanently closes that old logical request
without changing its row or hold. Any additional uncertainty blocks execution.
No generic retry authorization, new spending owner, provider or endpoint exists.

Starting accounting: $7.346880 known charges plus $0.614610 unknown holds equals
$7.961490 conservative usage; 686/690 metered attempts and 187/190 native counts.
One exact complete-packet native count is permitted; an interrupted count cannot
be repeated. Count plus 20% and 1,024 tokens must fit 40,000 input tokens. At the
[September 21 Sonnet 5 prices](https://platform.claude.com/docs/en/models/sonnet-5/overview)
of $2 input/$10 output per million, reserve $0.200000 for 40,000/12,000 tokens.
If the bound fails, stop before inference without omitting evidence.

The preceding $0.263282 hold plus this $0.200000 reservation totals $0.463282,
inside the SAME $0.50 continuation ceiling. Worst-case remaining balance is
$1.838510 and three metered attempts, above the protected $1.267862/two-attempt
reserve. Existing global token, provider, concurrency and transport limits apply.
Successful exact caches and counts are reused; lost/failed/uncertain requests
cannot be re-keyed, replayed, or turned into attempt2.

## Execution and validation

The existing protected-main-only manual workflow and credential isolation remain
unchanged. After clean independent exact-head review, required Python/browser/
frozen gates, trigger audit and protected merge, dispatch once with only:

```json
{"compact_wire_repair":"compact-wire-repair-20260921-v1"}
```

Record the dispatch, follow that run, verify the paired reservation/state/result
artifacts and all historical row/file identities, and reconcile actual usage.
Repeated workflow dispatch uses the complete exact cache or stops for recovery.
No additional dispatch follows an ambiguous request. No E2E, public activation,
registry publication, legacy-package withdrawal or new source generation.

Focused fixtures cover all questions, owned references, missing/extra/duplicate
data, refusals/output limits, native-count interruption, concurrency, irreversible
request/cache write boundaries, restoration, budget and exact manual routing.
Private copied-real-state rehearsal uses unchanged identity constants and the
actual saved Luna cache, with network blocked and fixture answers/counters. It
is wiring evidence only, not a scientific judgment or paid execution.
