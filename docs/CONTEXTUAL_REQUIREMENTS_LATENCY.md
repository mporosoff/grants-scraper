# Private requirements and latency repair

The owner authorized one narrow comparison and, conditionally, one revised DOE
workflow. The existing $10/690 experiment remains the only spending owner.
Starting authoritative run34795980969: $7.232138,680 attempts, zero outstanding.
`config/contextual_team/requirements-latency-v1.json` locks eight operations with
a $1.496005 conservative envelope, below the new $1.50/8 ceilings. Preserve at
least $1.267862 and two attempts. Historical Phase2 purposes are closed.

Only reviewed protected-main code may run with keys. Use the existing manual
`team-recommender-offline.yml`, `contextual_check` input:

1. `{"requirements_latency":"preflight"}`: zero metered work. Reproduce the exact
   ECLIPSE input, count complete packets, reuse all155 profile counts, bound future
   text in bytes, and persist the same ledger/checkpoint. No synthetic scientific
   result is created. Native counts retain the existing190-request lifetime cap.
2. `{"requirements_latency":"S"}` and `{"requirements_latency":"L"}`, once each.
   Same current ECLIPSE interpretation, twelve documents, source and conditions;
   flat reference serialization removes repeated schema tables, not evidence.
   S explicitly uses adaptive thinking and medium effort; L uses the existing
   Responses serializer, low reasoning/verbosity, strict JSON, `store:false`.
3. `{"requirements_latency":"comparison-check"}`, once. The fixed checker sees
   the bounded deduplicated union plus every person's evidence, with no arm,
   timing, score or generation rationale. All verdicts/references are required.
4. Inspect returned evidence, missed reasonable connections, costs and timing.
   No candidate is selected by default. A complete route needs credible science
   and at least approximately2x assessment speed relative to231.551469 seconds.
   Unsupported or unresolved automatic relationships block selection. Historical
   high-effort output is an exposed reference, not ground truth. If neither
   qualifies, stop paid DOE work and report the completed comparison.
5. Freeze one choice with `{"requirements_latency":"select","route":"S",
   "comparison_sha256":"<exact saved comparison-check identity>"}` (or L).
   This operation spends nothing and cannot replace an existing different choice.
6. Only then use normal Build in the Access-protected application preview for
   `363302:a-1`. This new release permits one canonical job, no expansion. It
   reuses exact document vectors and purchases only changed query rows, followed
   by contextual assessment and a separate verifier under the selected route.
7. `{"requirements_latency":"final-check"}`, once: source requirement semantics,
   primary group, all primary members and actual explanations. A negative result
   is retained; no favorable rejudging, extra human packet or public launch.

Every operation is once per named lifecycle, independently of its body/cache key.
Lost successful results, reconciled failures and uncertain dispatches never
authorize another attempt. Both providers reserve/reconcile in ExperimentLedger.
Only the appropriate protected execution step receives the provider credential;
the generic gateway's retries, retention, limits and allowance are untouched.

## Requirement contract and consumers

`contextual_team_requirements` derives `required` from an explicit kind and
applicability. A sponsor constraint requires exact source support. A necessary
planning contribution belongs to one approach chosen before named people.
Optional/unselected directions and unresolved conditions remain context, not
admission evidence or missing skills. Administrative conditions remain separate.
Retrieval, assessment, downgrade-only verification, graph readiness and the
private preview composer use the same active-contribution set. Genuine
conjunctions, single-contribution science and shared methods remain possible.

Legacy contracts/graphs remain immutable. V2 graphs carry the selected approach,
policy, exact source/registry/roster, route/effort and stage fingerprints. They
cannot borrow old scientific receipts. Ordinary edits never invoke a provider.

The Worker still coordinates GitHub Actions; it does not run inference. Pip's
existing dependency cache and faster read-only polling during the first60seconds
remove avoidable overhead without changing hosting or durable accounting. Polls
retain the prior ten-minute bound and never create a request. The owner goals
are15–30seconds verified cold, provisional60seconds upper guardrail; actual
timing is required. No acknowledgement/spinner substitutes for a verified graph.

Only the restricted prerequisite deploys. Public application/registry and50/150
activation remain off. GET `/admin/api/contextual/controls?release_id=<new lock>`
reports its separate cached/new-paid switch and one-workflow limit. Existing
Phase2 controls and historical jobs remain unchanged.

Prices checked2026-09-14: Sonnet5 $2/$10 per million input/output; Luna $.20/$1.20;
Voyage4large $.12. [Sonnet documentation](https://platform.claude.com/docs/en/models/sonnet-5/overview)
and [Luna documentation](https://developers.openai.com/api/docs/models/gpt-5.6-luna).
Cached credits are not free metered work. No paid prompt-cache creation is enabled.

## Exact-head review remediation

PR246 review at `5e83f23d9c385087394d55a6e56ea534abdb2dd4` identified
dated OpenAI response identity rejection and an unsafe comparison-size overlap
adjustment. The first remediation batch uses the existing `compatible_model`
contract for OpenAI, preserves the actual returned model in cache and receipt,
and applies the same identity validation on replay and comparison reads.
Incompatible responses remain charged failures and cannot be repurchased.

The complete union bound now derives from the actual canonical per-contribution
contract. ECLIPSE has two contributions, at most eight relationships per arm,
eight retained reference relationships, and twelve people: at most36 questions.
The native schema's overall limit matches this underlying contract. Preflight
counts all36 schema/question structures with complete original evidence, then
adds conservative UTF-8 bounds for every possible replacement identity. It
never subtracts overlapping examples. Disjoint-arm, Unicode/output-reference,
oversized-preflight, dated-model replay and crash fixtures cover this invariant.
No paid request preceded the review or its remediation.
