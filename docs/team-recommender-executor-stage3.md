# Manual frozen E2 validation

This adds only the Stage 3 route explicitly authorized after the local Stage 2 closeout `f4f38b32294c8e922c25a7c67d518be27f5f8e33`. It does not merge, enable or deploy the experimental recommender. Existing Stage 2 receipts and request identities remain intact.

The existing `team-recommender-offline.yml` manual workflow is unchanged. Dispatch on protected main with the exact experimental data-packet commit and SHA256, using the existing `embeddings` or `development-judge` transport operation. The latter name is historical: `S3-E2-complete-v1` requests are charged and recorded as **stage 3**, never as development observations. The workflow downloads packets as data; it does not check out the experimental branch, import packet code or accept endpoints. Secrets remain confined to the two existing provider steps. No schedules, publications, service state changes or subscriber work are added.

`inputs-stage3.json` is a fixed public-data allowlist: exact source/context/aspect ownership, 155 complete retained person documents, the unchanged registry generation, and E2 query text identities. It contains bounded public excerpts and registry information, not raw notices, private caches, model responses or recommendation outputs. The file is hash-bound in code and policy. Queries use only Voyage `voyage-4-large`, query role, 1024 dimensions, exact D3 UTF-8 preprocessing, float output and truncation disabled. Identical purchased rows retain their original paid-work identity. There is no new profile preparation.

The complete-evidence projection uses the same D1F semantic rubric and Sonnet 5. It includes full unchanged stored summaries and all distinct active evidence statements, labels and types for the union of candidate people. It rejects omitted, retired, mixed or altered records, sibling contexts, algorithm/score fields, and explanations in relevance tasks. Explanation audits require every quoted passage to be present. Questions are bounded to three per packet, 12,000 conservative input units and 512 output tokens; indivisible oversized questions stay unresolved. Comparisons retain matched size and explicit blinded A/B assignments. Relevance and group grades cannot replace one another. Output identity, per-item labels and evidence references remain checked after generation.

One ledger remains authoritative: `on-demand-team-offline-v2-20260909`. Stage 3 adds at most $4.00 and 184 requests, including at most 20 query requests (64 rows, 20,000 input units each; 400,000 total; $0.048 reservation envelope) and 164 judge requests. The earlier lower 1,064,960 judge-input ceiling is retained, together with 83,968 output tokens and the existing conservative dollar ceiling. All lifetime embedding/token/inventory limits remain. The global $10/690 ceilings are unchanged; the Stage 3 boundary also preserves $2.864667 and at least 25 lifetime slots for Stage 4. These are ceilings, not quotas or repeated allowances. Actual authorized sampling can be smaller; missing denominators must be reported.

Restore the latest paired reservation/state artifact before dispatch. Never initialize a second spending copy, replace historical usage, replay an uncertain request or rebatch an already claimed scientific item. The durable reservation remains an irreversible dispatch claim. Reconciled results missing an exact cache, invalid responses, interrupted writes and ambiguous transport require recovery, not another paid attempt. A complete exact successful result is reused. All fixture crash tests use temporary ledgers and fake provider responses; they do not constitute scientific judgments.

Before real judging, the experimental owner must commit the candidate/input/protocol lock and seal exact item allocation and packet hashes before inspecting judge results. All held-out outcomes remain validation evidence, never training data. Preparation-only source metadata does not constitute a recommendation or a quality pass. Stage 4 and deployment require separate approval.
